# 39.tripulante-encoding-mojibake-hotfix

## Bloco Executado

Hotfix textual localizado da aba `Tripulantes`, restrito a `frontend/src/features/tripulantes/form-page.js`.

## Inventario Material

O backup produtivo imediatamente anterior ao hotfix continha `28` linhas com mojibake real em `features/tripulantes/form-page.js`.

Grupos corrigidos:

- subtitulo do cabecalho: `edicao`;
- status de disponibilidade: `Indisponivel na operacao`, `Disponivel para operacao`;
- secao de identidade: `Identificacao`, `Codigo ANAC`;
- secao operacional: `Operacao`, `funcao`, `relatorios`;
- secao visual: `observacoes`, `Informacoes`, `sera`, `gestao`;
- CTA principal: `Salvar alteracoes`;
- feedbacks de validacao: `Arquivo invalido`, `digitos`, `Campo obrigatorio`;
- mensagens de exclusao/remocao: `vinculos historicos`, `excluido`, `deixara`, `relatorios`.

O backup anterior da frente de foto ainda continha tambem `3` linhas com separador corrompido equivalente a `Ã‚Â·`.

## Causa Material

A causa ficou localizada em literais ja corrompidas no arquivo `form-page.js`.

Nao foi evidenciado defeito global de build:

- `frontend/src/features/tripulantes/list-page.js`: `0` ocorrencias reais de `Ã`, `Â` ou `ƒ`;
- `frontend/src/features/tripulantes/avatar.js`: `0` ocorrencias reais de `Ã`, `Â` ou `ƒ`;
- `frontend/src/features/tripulantes/form-page.js` atual: `0` ocorrencias reais de `Ã`, `Â` ou `ƒ`.

## Mudanca Aplicada

- Corrigidas apenas as strings quebradas em `form-page.js`.
- Preservada a semantica original de labels, subtitulos, feedbacks e CTAs.
- Nenhuma logica de upload, preview, permissao, endpoint ou storage foi alterada.
- O teste de contrato da tela agora bloqueia retorno de mojibake no fonte da superficie.

## Prova Material

- Fonte corrigido: `frontend/src/features/tripulantes/form-page.js`.
- Teste atualizado: `tests/contract/test_frontend_third_surface_visual_foundation.py`.
- Backup produtivo pre-hotfix: `C:\srv\controle-treinamentos\frontend-backup\prod-tripulante-encoding-20260427143742`.
- Build produtivo: `C:\srv\controle-treinamentos\frontend\prod`.
- Evidencia estruturada: `docs/migration/evidence/39.tripulante-encoding-mojibake-hotfix.json`.

## Teste / Check Focal

- `tests/contract/test_frontend_third_surface_visual_foundation.py::test_tripulante_form_detail_preserves_functional_contracts`
- Resultado: `1 passed`.

Build focal:

- `runtime/tmp/tripulante-encoding-form-page-final-check-20260427144309`

## Regressao Minima

- Nenhum endpoint alterado.
- Nenhum contrato alterado.
- Nenhuma regra de storage alterada.
- Nenhuma logica de upload/preview alterada.
- Nenhum arquivo saudavel foi recodificado.

## Criterio de Fechamento

`fechado` se `form-page.js` ficar sem ocorrencias reais de `Ã`, `Â` ou `ƒ`, preservar os textos corretos esperados e nao contaminar arquivos vizinhos saudaveis.

## Divida Rebaixada ou Remanescente

Rebaixada:

- mojibake visivel na aba `Tripulantes`;
- separadores corrompidos historicos do fluxo de foto.

Remanescente:

- foto com referencia existente mas arquivo indisponivel continua sendo pendencia real de integridade, nao textual;
- refinamento visual amplo da aba permanece fora deste hotfix.
