# 31.2.1 - Baseline dos gates estruturais da Frente 31

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `31.2.1-baseline-gates-estruturais` |
| prioridade | P0 |
| objetivo | medir e registrar o baseline real dos gates estruturais da Frente 31. |
| natureza | baseline documental; nao e bloco de correcao ampla. |
| decisao | fechado como baseline medido; gates nao estao todos verdes. |

## 2. Mudanca aplicada

Atualizado o registro documental do baseline estrutural da Frente 31.

Nao houve alteracao de codigo de produto, teste, wrapper, archive, legacy, contrato funcional ou limpeza de artefato local.

## 3. Prova material

### Gate 1 - repo hygiene pytest

Comando executado:

```powershell
.\.venv\Scripts\python.exe -m pytest tests\ops\test_repo_hygiene_validation.py -q
```

Resultado bruto:

```text
...........                                                              [100%]
11 passed in 1.72s
```

Leitura: teste unitario do validador de hygiene verde.

### Gate 2 - architecture boundaries pytest

Comando executado:

```powershell
.\.venv\Scripts\python.exe -m pytest tests\architecture\test_architecture_boundaries.py -q
```

Resultado bruto relevante:

```text
.F                                                                       [100%]
FAILED tests/architecture/test_architecture_boundaries.py::test_critical_blueprints_are_split_to_smaller_modules
AssertionError: assert ['C:\\apps\\controle-treinamentos\\backend\\src\\controle_treinamentos\\blueprints\\admin\\routes.py:748>725'] == []
1 failed, 1 passed in 0.09s
```

Falha encontrada: `backend/src/controle_treinamentos/blueprints/admin/routes.py:748>725`.

Baseline conhecido: limite arquitetural vermelho em arquivo grande. Esta etapa nao corrige essa fronteira.

### Gate 3 - repo hygiene real

Comando executado:

```powershell
.\.venv\Scripts\python.exe ops\scripts\repo\validate_repo_hygiene.py --root .
```

Resultado bruto resumido:

```text
"success": false
"root": "C:\\apps\\controle-treinamentos"
violations: 35
generated_artifact: 35
primeiros paths:
backend/__pycache__
backend/src/__pycache__
backend/src/controle_treinamentos/__pycache__
backend/src/controle_treinamentos/api/__pycache__
backend/src/controle_treinamentos/api/http/__pycache__
```

Falha encontrada: diretorios `__pycache__` em `backend/`, `ops/` e `tests/`.

Baseline conhecido: hygiene real vermelho por artefatos gerados locais. Esta etapa nao limpa esses artefatos para nao abrir escopo lateral.

## 4. Teste / check focal

| gate | resultado | leitura |
| --- | --- | --- |
| `tests/ops/test_repo_hygiene_validation.py` | `11 passed` | comportamento do validador coberto e verde. |
| `tests/architecture/test_architecture_boundaries.py` | `1 failed, 1 passed` | falha arquitetural conhecida em `admin/routes.py:748>725`. |
| `ops/scripts/repo/validate_repo_hygiene.py --root .` | `success=false`, 35 violacoes | baseline real vermelho por `__pycache__` local/gerado. |

## 5. Regressao minima

Nao foi declarada regressao verde.

Resultado minimo da Frente 31 neste bloco:

- `test_repo_hygiene_validation.py` passou;
- `test_architecture_boundaries.py` falhou por baseline conhecido;
- `validate_repo_hygiene.py --root .` falhou por artefatos gerados locais;
- nenhuma falha foi corrigida ou mascarada nesta etapa.

## 6. Registro em migration

Este documento registra:

- comando executado;
- resultado bruto relevante;
- falhas encontradas;
- baseline conhecido;
- itens nao corrigidos de proposito.

O que nao foi corrigido de proposito:

- `admin/routes.py:748>725`, porque corrigir exigiria refatoracao arquitetural fora deste bloco;
- `__pycache__` em `backend/`, `ops/` e `tests/`, porque limpeza local pertence a bloco proprio de artefatos/hygiene e nao ao baseline.

## 7. Indexacao no README

O indice `docs/migration/README.md` aponta este documento como bloco P0 de baseline estrutural da Frente 31.

## 8. Criterio de fechamento

| criterio | status |
| --- | --- |
| tres gates solicitados medidos | fechado |
| resultado bruto registrado | fechado |
| falha arquitetural classificada como baseline conhecido | fechado |
| hygiene real vermelho registrado sem limpeza lateral | fechado |
| ausencia de falso verde | fechado |

Decisao do bloco: `fechado`.

Fechado significa baseline medido e registrado. Nao significa gates verdes.

## 9. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| `admin/routes.py:748>725` | remanescente | tratar apenas em bloco/frente propria de arquitetura, sem reabrir Frente 30. |
| `__pycache__` em `backend/`, `ops/` e `tests/` | remanescente | limpar apenas em bloco de artefatos locais/hygiene, com verificacao de paths. |
| wrappers alto risco | nao tocado | seguem para `31.3.1`. |
| wrappers auxiliares | nao tocado | seguem para `31.3.2`. |
| archive/snapshots, local build e legacy vivo | nao tocados | seguem para blocos proprios da Frente 31. |
