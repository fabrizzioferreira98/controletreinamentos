# 32.3.10.b - Materializacao minima do env real do gate final

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `32.3.10.b-materializacao-minima-env-gate-final` |
| objetivo | carregar materialmente as variaveis obrigatorias do gate final no processo correto, sem vazar segredo e sem alterar candidato final. |
| candidato final | `release_20260420_2932_210420` |
| ambiente | `homolog` |
| pacote externo | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420` |
| pre-condicao | `32.3.10.a` deveria ter localizado as quatro variaveis em fonte viva real. |
| resultado da pre-condicao | nao atendida; `32.3.10.a` encerrou como `BLOQUEADO`. |
| resultado do bloco | `BLOQUEADO` |
| `GO_OPERACIONAL_FINAL` emitido | nao |
| assinatura gerada | nao |

Esta etapa nao altera candidato, codigo de produto, manifest, checklist ou pacote externo. Nenhum valor fake, placeholder, template, backup morto, archive, quarentena ou inferencia manual foi usado.

## 2. Mudanca aplicada

Foi executado somente preflight material do `run_release_strict.py` em processo transitorio, carregando a fonte viva HML ja confirmada:

```text
C:\srv\controle-treinamentos\env\hml.env
```

O carregamento foi minimo e controlado: as variaveis foram importadas apenas no processo do preflight. Nao houve persistencia em escopo `User` ou `Machine`, nao houve alteracao de arquivo operacional e nao houve reemissao de manifest/checklist.

| variavel | acao material |
| --- | --- |
| `DATABASE_URL` | carregada do `hml.env` vivo apenas no processo transitorio do preflight. |
| `E2E_DATABASE_URL` | nao carregada; fonte viva real nao foi localizada na `32.3.10.a`. |
| `E2E_LOGIN` | nao carregada; fonte viva real nao foi localizada na `32.3.10.a`. |
| `E2E_PASSWORD` | nao carregada; fonte viva real nao foi localizada na `32.3.10.a`. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | nao carregada; fonte viva real nao foi localizada na `32.3.10.a`. |

## 3. Prova material

### 3.1 Base objetiva herdada da 32.3.10.a

Documento de pre-condicao:

```text
docs\migration\32.3.10.a-localizacao-ampliada-fontes-reais-e2e-assinatura.md
```

Classificacao herdada:

| variavel | classificacao na 32.3.10.a | efeito nesta etapa |
| --- | --- | --- |
| `E2E_DATABASE_URL` | `ausente` | nao pode ser materializada sem inventar fonte. |
| `E2E_LOGIN` | `ausente` | nao pode ser materializada sem inventar fonte. |
| `E2E_PASSWORD` | `ausente` | nao pode ser materializada sem inventar segredo. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `ausente` | nao pode ser materializada sem inventar chave. |

### 3.2 Preflight material do gate

Foi chamada a funcao oficial de pre-condicao do proprio gate:

```text
from ops.scripts.release.run_release_strict import _required_env
```

Saida redigida, sem valores sensiveis:

```json
{
  "source_env_loaded": "C:\\srv\\controle-treinamentos\\env\\hml.env",
  "gate_preflight_ok": false,
  "missing_required_env": [
    "E2E_DATABASE_URL",
    "E2E_LOGIN",
    "E2E_PASSWORD",
    "RELEASE_EVIDENCE_SIGNING_KEY"
  ],
  "checked": {
    "DATABASE_URL": {
      "present": true,
      "length": 81
    },
    "E2E_DATABASE_URL": {
      "present": false,
      "length": 0
    },
    "E2E_LOGIN": {
      "present": false,
      "length": 0
    },
    "E2E_PASSWORD": {
      "present": false,
      "length": 0
    },
    "RELEASE_EVIDENCE_SIGNING_KEY": {
      "present": false,
      "length": 0
    }
  }
}
```

### 3.3 Leitura objetiva

| item | resultado |
| --- | --- |
| `hml.env` vivo carregado no processo do preflight | sim |
| `DATABASE_URL` presente no processo | sim, comprimento `81` |
| quatro variaveis exigidas pela 32.3.10.b presentes | nao |
| `missing_required_env` zerou | nao |
| gate liberado para execucao completa | nao |
| valores sensiveis impressos | nao |

## 4. Teste / check

| check | resultado |
| --- | --- |
| preflight material do `run_release_strict.py` executado | sim |
| funcao oficial usada | `_required_env()` |
| fonte viva real usada | `C:\srv\controle-treinamentos\env\hml.env` |
| variaveis verificadas no processo | `DATABASE_URL`, `E2E_DATABASE_URL`, `E2E_LOGIN`, `E2E_PASSWORD`, `RELEASE_EVIDENCE_SIGNING_KEY` |
| `DATABASE_URL` | presente |
| `E2E_DATABASE_URL` | ausente |
| `E2E_LOGIN` | ausente |
| `E2E_PASSWORD` | ausente |
| `RELEASE_EVIDENCE_SIGNING_KEY` | ausente |
| `missing_required_env` | `["E2E_DATABASE_URL", "E2E_LOGIN", "E2E_PASSWORD", "RELEASE_EVIDENCE_SIGNING_KEY"]` |
| resultado | `BLOQUEADO` |

## 5. Criterio de fechamento

| criterio | status |
| --- | --- |
| usar apenas fontes vivas reais confirmadas | atendido |
| carregar env no processo correto do preflight | atendido parcialmente: `hml.env` carregado, mas quatro variaveis nao existem na fonte. |
| nao vazar segredo | atendido |
| nao alterar codigo de produto | atendido |
| nao alterar manifest/checklist | atendido |
| nao assinar pacote | atendido |
| nao declarar GO | atendido |
| executar preflight material do `run_release_strict.py` | atendido |
| zerar `missing_required_env` | nao atendido |

Decisao do bloco: `BLOQUEADO`.

O criterio de `PASS` nao foi atendido, porque o preflight oficial ainda acusa variaveis obrigatorias ausentes.

## 6. Divida rebaixada ou remanescente

| item | status | justificativa | encaminhamento |
| --- | --- | --- | --- |
| materializacao minima de `DATABASE_URL` | rebaixada/fechada | `DATABASE_URL` foi carregada do `hml.env` vivo no processo transitorio do preflight. | manter uso controlado da fonte viva HML para o gate. |
| `E2E_DATABASE_URL` | remanescente bloqueante | ausente em fonte viva real; nao foi carregada. | provisionar em fonte operacional viva de homolog. |
| `E2E_LOGIN` | remanescente bloqueante | ausente em fonte viva real; nao foi carregada. | provisionar usuario real de E2E homolog. |
| `E2E_PASSWORD` | remanescente bloqueante | ausente em fonte viva real; nao foi carregada. | provisionar segredo real correspondente. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | remanescente bloqueante | ausente em fonte viva real; nao foi carregada. | provisionar chave real de assinatura HMAC. |
| gate operacional final | remanescente bloqueante | `_required_env()` segue retornando `missing_required_env`. | rerodar gate somente apos as quatro variaveis existirem em fonte viva real. |
| assinatura final | remanescente bloqueante | sem `RELEASE_EVIDENCE_SIGNING_KEY` real nao ha assinatura valida. | assinar somente apos chave real estar provisionada. |
