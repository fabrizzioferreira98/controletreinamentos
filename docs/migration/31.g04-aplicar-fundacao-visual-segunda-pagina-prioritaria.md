# 31.G04 - Aplicar fundacao visual na segunda pagina prioritaria

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `G04-aplicar-fundacao-visual-segunda-pagina-prioritaria` |
| data | 2026-04-22 |
| superficie prioritaria | `treinamentos/relatorios` |
| objetivo | aplicar tokens e primitives de `shared/ui` na segunda superficie funcional prioritaria, sem drift funcional e sem redesign amplo. |
| escopo | treinamentos por tripulante, cadastro raiz, relatorios de habilitacoes/produtividade, headers, paineis, filtros, formularios, tabelas, contexto/evidencias e CSS escopado. |
| fora do escopo | dashboard/tripulantes, SSR, runtime/deploy, design system completo, motion sofisticado e componentizacao ornamental. |
| decisao | `fechado` |

## 2. Leitura visual inicial

| achado | classificacao |
| --- | --- |
| `pages-treinamentos-relatorios.js` ja era wrapper compativel e fino | base arquitetural preservada |
| `features/treinamentos/list-page.js` concentrava superficie viva de selecao, workbench, batch e registros de treinamentos | alvo principal do G04 |
| `features/training-root/page.js` concentrava cadastro raiz, tabs, filtros, formularios e tabelas | alvo complementar do G04 |
| `features/relatorios/*.js` concentravam habilitacoes, produtividade, filtros, exports e impressao | alvo complementar do G04 |
| `features/relatorios/report-ui.js` concentrava estados/contexto/evidencias compartilhadas dos relatorios | alvo compartilhado do G04 |
| CSS local ja tinha familias densas para treinamentos/relatorios, mas ainda pouco conectadas a `shared/ui` | `divida_visual_controlada` reduzida |
| redesign amplo continua fora do bloco | `nao_redesign` preservado |

## 3. Mudanca aplicada

| arquivo | mudanca |
| --- | --- |
| `frontend/src/features/treinamentos/list-page.js` | adiciona `training-reports-page-shell`, `training-program-page-shell`, `priority-page-surface`, `ui-stack`, `priority-page-header`, `ui-surface`, `ui-stack-sm`, superficies no workbench e cards `ui-surface`. |
| `frontend/src/features/training-root/page.js` | adiciona `training-root-page-shell`, `training-root-filter-panel`, `training-root-panel`, `ui-surface`, `ui-stack`, cards `ui-surface`, formularios `ui-stack-sm` e tabelas `training-reports-table-wrap`. |
| `frontend/src/features/relatorios/habilitacoes-page.js` | adiciona `report-priority-page-shell`, `priority-page-header`, `training-report-panel`, `ui-surface`, `ui-feedback`, `ui-stack-sm`, cards `ui-surface` e tabela escopada. |
| `frontend/src/features/relatorios/produtividade-page.js` | adiciona `report-priority-page-shell`, `priority-page-header`, `training-report-panel`, `ui-surface`, `ui-feedback`, `ui-stack-sm`, cards `ui-surface`, historico `ui-surface` e tabela escopada. |
| `frontend/src/features/relatorios/report-ui.js` | adiciona `ui-surface` e `ui-feedback` em estados, contexto e evidencias compartilhadas. |
| `frontend/src/app.css` | adiciona bloco G04 escopado a treinamentos/relatorios usando tokens de spacing, surface, radius, shadow, estados e transicao. |
| `tests/contract/test_frontend_training_reports_visual_foundation.py` | adiciona check focal de adocao visual e preservacao de contratos funcionais. |
| `docs/architecture/FRONTEND_ARCHITECTURE.md` | documenta a adocao visual da segunda superficie prioritaria. |
| `frontend/README.md` | registra a politica de priority page foundation para treinamentos/relatorios. |

## 4. Prova material

| prova | resultado |
| --- | --- |
| treinamentos | contem `training-reports-page-shell training-program-page-shell priority-page-surface ui-stack`, `priority-page-header ui-surface`, paineis `ui-surface`, filtros `ui-stack-sm`, workbench `ui-surface` e cards `ui-surface`. |
| cadastro raiz | contem `training-root-page-shell priority-page-surface ui-stack`, `training-root-filter-panel ui-surface`, `training-root-panel ui-surface`, cards `ui-surface` e tabelas `training-reports-table-wrap`. |
| relatorios | contem `report-priority-page-shell priority-page-surface ui-stack`, `training-report-panel ui-surface`, `state-note ui-feedback`, filtros `ui-stack-sm`, cards `ui-surface` e tabelas `training-reports-table-wrap`. |
| report-ui compartilhado | contem `report-state-panel ui-surface`, loading `ui-feedback`, contexto/evidencias `ui-surface`. |
| CSS G04 | contem `.training-reports-page-shell`, `.training-program-panel.ui-surface`, `.training-root-panel.ui-surface`, `.training-report-panel.ui-surface`, `var(--space-panel-gap)`, `var(--radius-surface)`, `var(--shadow-surface)` e `var(--color-state-default-surface)`. |
| contratos preservados | endpoints de treinamentos, batch, reset/continue, tabs de cadastro raiz, submits explicitos, filtros, exports, impressao, PDF e conferencias permanecem. |

## 5. Checks

Checks executados com `PYTHONDONTWRITEBYTECODE=1`:

| check | resultado |
| --- | --- |
| `tests/contract/test_frontend_training_reports_visual_foundation.py` | PASS |
| `tests/contract/test_frontend_feature_boundaries.py` | PASS |
| `tests/contract/test_frontend_page_module_boundaries.py` | PASS |
| `tests/contract/test_frontend_ux_functional_guards.py` | PASS |
| rodada focal inicial | `26 passed` |

Validacao final do bloco:

| check | resultado |
| --- | --- |
| rodada focal ampliada final | `72 passed` |
| build frontend com `--output-dir <temp fora do checkout>` | PASS em `C:\Users\FABRIZ~1.ARA\AppData\Local\Temp\controle-treinamentos-g04-cadc4fcf260c479f908adc8915d89261` |
| CSS final do build | contem `G04: training/reports`, `.training-reports-page-shell`, `.training-program-panel.ui-surface`, `.training-root-panel.ui-surface`, `.training-report-panel.ui-surface`, `var(--shadow-surface)` e `var(--color-state-default-surface)` |
| hygiene validator final | `success=true`, sem violacoes |

O validator de hygiene ficou vermelho durante a rodada final somente por `__pycache__` gerado/local fora das excecoes. Foram removidos apenas residuos locais de bytecode fora de `.venv/` e `archive/`: `32` diretorios `__pycache__` e `0` arquivos `.pyc/.pyo` soltos. A validacao final com `python -B` ficou verde.

## 6. Criterio de fechamento

| criterio | status |
| --- | --- |
| fundacao visual aplicada materialmente em treinamentos/relatorios | fechado |
| rotas/loaders/guards preservados | fechado |
| services/state/compat nao reabertos | fechado |
| dashboard/tripulantes fora do bloco | fechado |
| checks focais verdes | fechado |

Decisao objetiva do bloco: `fechado`.

## 7. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| CSS historico de treinamentos/relatorios ainda existe | `divida_visual_controlada` | continuar conversao por blocos incrementais de pagina/feature. |
| tabelas e formularios densos ainda tem familias visuais proprias | remanescente planejado | tratar em bloco especifico de componentes/tabelas/formularios, se virar prioridade. |
| dashboard/tripulantes | fora do G04 | preservar resultado do G03 sem reabrir a superficie. |
| redesign amplo/design system | fora do G04 | nao autorizado por este bloco. |
