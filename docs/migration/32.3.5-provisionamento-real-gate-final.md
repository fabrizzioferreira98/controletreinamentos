# 32.3.5 - Provisionamento real do gate operacional final

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `32.3.5-provisionamento-real-gate-final` |
| objetivo | sair do estado `BLOQUEADO` por `missing_required_env` e demonstrar materialmente que o gate final pode rodar ate o fim, se houver fonte real para as cinco variaveis obrigatorias. |
| candidato final | `release_20260420_2932_210420` |
| ambiente | `homolog` |
| gate oficial | `ops\scripts\release\run_release_strict.py` |
| resultado do bloco | `BLOQUEADO` |
| `GO_OPERACIONAL_FINAL` emitido | nao |

Este bloco nao altera codigo de produto, nao altera o candidato final, nao cria novo `release_id`, nao emite assinatura falsa e nao reabre frentes anteriores.

## 2. Mudanca aplicada

Foi feita verificacao e tentativa de carregamento das variaveis obrigatorias a partir da fonte viva de homolog usada pelo servico `CT-App-Hml`:

```text
C:\srv\controle-treinamentos\env\hml.env
```

Fonte operacional que aponta para esse env:

```text
C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.xml
```

Trecho operacional verificado:

```text
-EnvFile "C:\srv\controle-treinamentos\env\hml.env"
```

Resultado do provisionamento no processo do preflight:

| variavel | fonte real encontrada | carregada no processo do preflight | tamanho | observacao |
| --- | --- | --- | ---: | --- |
| `DATABASE_URL` | `C:\srv\controle-treinamentos\env\hml.env` | sim | `81` | Fonte viva de homolog. Valor nao impresso. |
| `E2E_DATABASE_URL` | nao encontrada | nao | `0` | Nao foi derivada nem preenchida por conveniencia. |
| `E2E_LOGIN` | nao encontrada | nao | `0` | Nao foi usado login historico ou template como substituto. |
| `E2E_PASSWORD` | nao encontrada | nao | `0` | Nao foi usado valor de exemplo, segredo historico ou senha inventada. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | nao encontrada em fonte viva homolog | nao | `0` | Nao foi usada `SECRET_KEY` do app nem chave historica/quarentenada para assinatura. |

Fontes avaliadas:

| fonte | classificacao | decisao |
| --- | --- | --- |
| `C:\srv\controle-treinamentos\env\hml.env` | fonte viva de homolog | Usada apenas para `DATABASE_URL`; nao contem `E2E_*` nem `RELEASE_EVIDENCE_SIGNING_KEY`. |
| Backups `C:\srv\controle-treinamentos\env\hml.env*` | backups reais do env HML | Confirmam o mesmo padrao: `DATABASE_URL` presente; `E2E_*` e chave de assinatura ausentes. |
| `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.xml` | fonte operacional do servico HML | Confirma que o runtime HML carrega `hml.env`. |
| `.env.example` e workflows CI | template/contrato | Nao usados por conter placeholders ou referencia a `secrets.*`, nao valores materiais locais. |
| `repo-quarantine\...\ .secrets\*` | material historico/quarentenado | Nao usado; nao e fonte viva atual do homolog da Frente 32. |
| `SECRET_KEY` do app em `hml.env` | segredo real de aplicacao | Nao usado como `RELEASE_EVIDENCE_SIGNING_KEY`; isso seria reaproveitamento sem contrato e poderia gerar assinatura indevida. |

## 3. Prova material

### 3.1 Hashes das fontes e artefatos usados

| item | caminho | SHA-256 |
| --- | --- | --- |
| env vivo HML | `C:\srv\controle-treinamentos\env\hml.env` | `c8df9499bfb524426a40c2b53b7481dda5cf2ba253ca485c7636f94fc5df3381` |
| servico HML | `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.xml` | `3ee9f7673c79e47835f0737589e8c363c62a786c62374ebef72a66d98f13b001` |
| manifest do candidato | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json` | `9cac45d73bb46591ff68202e159aa90b879323b17b947d12c41d87a22b77fddd` |
| checklist do candidato | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md` | `55654e85e3a5fee511678cc1435043e59dd0e5ba188502d64ebddee22a7f1d53` |

### 3.2 Verificacao redigida do env vivo HML

Variaveis exigidas pelo `run_release_strict.py`:

| variavel | presente no `hml.env` | tamanho |
| --- | --- | ---: |
| `DATABASE_URL` | `true` | `81` |
| `E2E_DATABASE_URL` | `false` | `0` |
| `E2E_LOGIN` | `false` | `0` |
| `E2E_PASSWORD` | `false` | `0` |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `false` | `0` |

Variaveis de contexto HML presentes, mas nao substitutas do gate:

| variavel | presente | tamanho | uso nesta etapa |
| --- | --- | ---: | --- |
| `APP_ENV` | `true` | `7` | Contexto do runtime, nao substitui variavel obrigatoria do gate. |
| `APP_RELEASE_ID` | `true` | `36` | Contexto do runtime, nao substitui `release_id` do candidato. |
| `APP_RELEASE_INSTANCE_ID` | `true` | `36` | Contexto do runtime, nao substitui gate. |
| `METRICS_TOKEN` | `true` | `40` | Nao e uma das cinco pre-condicoes obrigatorias do `run_release_strict.py`. |
| `SECRET_KEY` | `true` | `64` | Nao usada como chave de assinatura de evidencia. |

## 4. Teste / check

### 4.1 Comando focal executado

O preflight foi executado com `DATABASE_URL` carregado do `hml.env` vivo e sem preenchimento artificial das demais variaveis:

```powershell
.\.venv\Scripts\python.exe ops\scripts\release\run_release_strict.py --base-url http://127.0.0.1:8102 --evidence-manifest C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json --regression-checklist C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md --evidence-max-age-hours 24
```

Preflight de variaveis no processo:

| variavel | presente | tamanho |
| --- | --- | ---: |
| `DATABASE_URL` | `true` | `81` |
| `E2E_DATABASE_URL` | `false` | `0` |
| `E2E_LOGIN` | `false` | `0` |
| `E2E_PASSWORD` | `false` | `0` |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `false` | `0` |

Resultado bruto:

```text
FAIL: variaveis obrigatorias ausentes: E2E_DATABASE_URL, E2E_LOGIN, E2E_PASSWORD, RELEASE_EVIDENCE_SIGNING_KEY
run_release_strict_exit=1
```

Log estruturado relevante:

```json
{
  "event": "release_gate_blocked",
  "component": "run_release_strict",
  "release_id": "release_20260420_2932_210420",
  "release_environment": "homolog",
  "reason": "missing_required_env",
  "missing_env": [
    "E2E_DATABASE_URL",
    "E2E_LOGIN",
    "E2E_PASSWORD",
    "RELEASE_EVIDENCE_SIGNING_KEY"
  ]
}
```

### 4.2 Classificacao do resultado

| classificacao | motivo |
| --- | --- |
| `BLOQUEADO` | O gate ainda parou em `missing_required_env`. |
| nao `PASS` | O gate nao chegou ao fim com todas as pre-condicoes reais presentes. |
| nao `FAIL` funcional | O produto nao foi reprovado apos execucao completa; a parada continua sendo pre-condicao ausente. |
| nao `GO_OPERACIONAL_FINAL` | Esta etapa nao emite GO e o gate nao esta verde. |

## 5. Criterio de fechamento

| criterio | status |
| --- | --- |
| Verificar as cinco variaveis obrigatorias | fechado |
| Carregar variaveis reais disponiveis em fonte viva HML | fechado parcialmente: `DATABASE_URL` |
| Nao usar fake, placeholder, template ou segredo inventado | fechado |
| Nao usar segredo historico/quarentenado como se fosse fonte viva | fechado |
| Preflight deixar de bloquear por `missing_required_env` | nao atendido |
| Bloqueio remanescente demonstrado objetivamente | atendido |
| Nao alterar codigo de produto | fechado |
| Nao alterar candidato final | fechado |
| Nao emitir assinatura falsa | fechado |
| README indexado | fechado apos atualizacao |

Decisao do bloco: `BLOQUEADO`.

Esta etapa nao fecha como provisionamento real bem-sucedido, porque quatro variaveis obrigatorias continuam ausentes em fonte real de homolog. Fecha apenas como tentativa real auditada e bloqueio remanescente demonstrado sem mascarar verde.

## 6. Divida rebaixada ou remanescente

| item | status | justificativa | encaminhamento |
| --- | --- | --- | --- |
| `DATABASE_URL` ausente | rebaixada/fechada | Fonte viva HML possui `DATABASE_URL`, carregada no preflight. | Manter origem em `C:\srv\controle-treinamentos\env\hml.env`. |
| `E2E_DATABASE_URL` | remanescente bloqueante | Ausente no env vivo HML e backups HML. | Provisionar valor real homolog/E2E no cofre ou env operacional correto. |
| `E2E_LOGIN` | remanescente bloqueante | Ausente no env vivo HML e backups HML. | Provisionar login tecnico/canonico real de homolog. |
| `E2E_PASSWORD` | remanescente bloqueante | Ausente no env vivo HML e backups HML. | Provisionar segredo real correspondente ao login tecnico/canonico. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | remanescente bloqueante | Ausente na fonte viva HML; `SECRET_KEY` do app nao foi reutilizada. | Provisionar chave real de assinatura de evidencia e so entao validar/assinar manifest pela trilha correta. |
| Gate operacional final | remanescente bloqueante | `run_release_strict.py` ainda retorna `missing_required_env`. | Rerodar apos as quatro variaveis remanescentes estarem presentes em fonte real. |
| `GO_OPERACIONAL_FINAL` | nao emitido | Gate final nao esta materialmente verde. | Emitir apenas em etapa propria, apos gate, manifest/checklist e assinatura ficarem coerentes. |
