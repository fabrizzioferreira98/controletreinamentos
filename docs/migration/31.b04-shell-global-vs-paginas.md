# 31.B04 - Shell global vs paginas

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `B04-shell-global-vs-paginas` |
| data | 2026-04-22 |
| objetivo | inventariar entradas do shell global, cruzar com o router oficial do frontend e classificar cada superficie exposta sem presumir que link no shell e rota SPA. |
| escopo | `frontend/src/shell.js`, `frontend/src/app.js`, loaders oficiais, redirects/guards relevantes e docs vivas da fronteira shell/SPA/SSR. |
| fora do escopo | redesign visual, migracao SSR para SPA, remocao de links, refatoracao de componentes, runtime/deploy, wrappers, archive e legacy fora da classificacao necessaria. |
| decisao | `fechado` |

## 2. Inventario do shell

O inventario veio de `frontend/src/shell.js`.

| destino | origem exposta | formato |
| --- | --- | --- |
| `#/dashboard` | menu `Dashboard > Painel principal` | hash route |
| `/painel-tv` | menu `Dashboard > Painel TV` | path backend |
| `/produtividade/painel-tv` | menu `Dashboard > Painel produtividade` | path backend |
| `/missoes` | menu `Operacoes > Missoes` | path backend |
| `/pernoites` | menu `Operacoes > Pernoites` | path backend |
| `/bases` | menu `Operacoes > Bases` | path backend |
| `#/relatorios/habilitacoes` | menu `Relatorios > Habilitacoes` | hash route |
| `#/relatorios/individual` | menu `Relatorios > Relatorio individual` | hash route |
| `#/relatorios/produtividade` | menu `Relatorios > Produtividade` | hash route |
| `#/tripulantes` | menu `Cadastros > Tripulantes` | hash route |
| `#/treinamentos` | menu `Cadastros > Treinamentos` | hash route |
| `/equipamentos` | menu `Cadastros > Equipamentos` | path backend |
| `#/treinamentos/raiz` | menu `Cadastros > Raiz de treinamentos` | hash route |
| `/usuarios` | menu `Admin > Usuarios` | path backend |
| `/usuarios/novo` | menu `Admin > Novo usuario` | path backend |
| `/monitoramento` | menu `Admin > Monitoramento` | path backend |
| `/manual/usuario.pdf` | menu `Admin > Manual do usuario` | path backend/documental |
| `/notificacoes-email` | menu `Admin > Notificacoes por email` e atalho do topo | path backend |
| `/backups` | menu `Admin > Backups` | path backend |
| `/auditoria` | menu `Admin > Auditoria` | path backend |

## 3. Cruzamento com router

Rotas hash estaticas registradas em `frontend/src/app.js` e presentes no shell:

| destino | status |
| --- | --- |
| `#/dashboard` | registrado como SPA |
| `#/relatorios/habilitacoes` | registrado como SPA |
| `#/relatorios/individual` | registrado como SPA |
| `#/relatorios/produtividade` | registrado como SPA |
| `#/tripulantes` | registrado como SPA |
| `#/treinamentos` | registrado como SPA |
| `#/treinamentos/raiz` | registrado como SPA |

Rotas dinamicas registradas em `frontend/src/app.js`, mas nao expostas como entrada primaria do shell neste bloco:

| padrao | status |
| --- | --- |
| `#/tripulantes/<id>` | SPA dinamica, fora do inventario primario do shell |
| `#/treinamentos/<id>` | SPA dinamica, fora do inventario primario do shell |

Loaders oficiais observados:

| prefixo | loader |
| --- | --- |
| `tripulantes` | `pages-dashboard-tripulantes.js` |
| `treinamentos` | `pages-treinamentos-relatorios.js` |

Conclusao: shell nao e fonte de verdade de pertencimento a SPA. O pertencimento a SPA exige hash route registrada no router oficial.

## 4. Classificacao final

| destino | classificacao | owner atual | condicao de saida quando nao for SPA |
| --- | --- | --- | --- |
| `#/dashboard` | `spa_viva` | frontend | n/a |
| `/painel-tv` | `backend_ssr_compat` | backend dashboard + frontend shell | publicar substituto SPA/API ou manter como painel backend explicitamente classificado. |
| `/produtividade/painel-tv` | `backend_ssr_compat` | backend relatorios + frontend shell | publicar substituto SPA/API ou manter como painel backend explicitamente classificado. |
| `/missoes` | `legacy_vivo_controlado` | operacoes + governanca legacy | API/frontend substituto publicado, rotas HTML fora do boundary corrente e contratos migrados. |
| `/pernoites` | `legacy_vivo_controlado` | operacoes + governanca legacy | API/frontend substituto publicado, rotas HTML fora do boundary corrente e contratos migrados. |
| `/bases` | `backend_ssr_compat` | backend bases + frontend shell | publicar substituto SPA/API para gestao de bases e migrar link/testes. |
| `#/relatorios/habilitacoes` | `spa_viva` | frontend | n/a |
| `#/relatorios/individual` | `spa_viva` | frontend | n/a |
| `#/relatorios/produtividade` | `spa_viva` | frontend | n/a |
| `#/tripulantes` | `spa_viva` | frontend | n/a |
| `#/treinamentos` | `spa_viva` | frontend | n/a |
| `/equipamentos` | `backend_ssr_compat` | backend cadastros + frontend shell | publicar substituto SPA/API para equipamentos e migrar link/testes. |
| `#/treinamentos/raiz` | `spa_viva` | frontend | n/a |
| `/usuarios` | `backend_ssr_compat` | backend admin + frontend shell | publicar substituto SPA/API para usuarios e migrar link/testes. |
| `/usuarios/novo` | `backend_ssr_compat` | backend admin + frontend shell | publicar substituto SPA/API para usuarios e migrar link/testes. |
| `/monitoramento` | `backend_ssr_compat` | backend admin/ops + frontend shell | publicar substituto SPA/API ou manter como painel operacional backend classificado. |
| `/manual/usuario.pdf` | `externo_operacional` | docs produto + backend admin/ops | mover para superficie documental canonica ou manter como PDF operacional explicitamente classificado. |
| `/notificacoes-email` | `backend_ssr_compat` | backend admin/ops + frontend shell | publicar substituto SPA/API para notificacoes e migrar link/topbar/testes. |
| `/backups` | `backend_ssr_compat` | backend admin/ops + frontend shell | publicar substituto SPA/API ou manter como painel operacional backend classificado. |
| `/auditoria` | `backend_ssr_compat` | backend admin + frontend shell | publicar substituto SPA/API para auditoria e migrar link/testes. |

Resumo quantitativo:

| classificacao | quantidade |
| --- | ---: |
| `spa_viva` | 7 |
| `backend_ssr_compat` | 10 |
| `legacy_vivo_controlado` | 2 |
| `externo_operacional` | 1 |
| `ambigua_pendente` | 0 |

## 5. Mudanca aplicada

Foi alterado `docs/architecture/FRONTEND_ARCHITECTURE.md` para registrar:

- shell global nao define pertencimento a SPA;
- classificacoes oficiais para entradas do shell;
- inventario atual do shell com owner e condicao de saida;
- regra para nova entrada de shell ser classificada no mesmo change.

Foi adicionado `tests/contract/test_frontend_shell_navigation_boundaries.py` para garantir:

- o conjunto de hrefs expostos pelo shell tem classificacao explicita;
- itens `spa_viva` estao registrados como hash routes estaticas em `frontend/src/app.js`;
- a doc viva contem cada item classificado e a categoria `ambigua_pendente`.

Nenhum link foi removido. Nenhuma pagina SSR foi migrada. Nenhum comportamento visual foi alterado.

## 6. Checks

Checks executados com `PYTHONDONTWRITEBYTECODE=1`:

| check | resultado |
| --- | --- |
| `tests/contract/test_frontend_shell_navigation_boundaries.py` | PASS |
| `tests/contract/test_frontend_compat_redirects.py` | PASS |
| `tests/contract/test_frontend_ux_functional_guards.py` | PASS |
| total focal | `25 passed` |
| `ops/scripts/repo/validate_repo_hygiene.py --root .` | `success=true`, sem violacoes |

Os checks Python geraram `__pycache__` em uma tentativa intermediaria. Foram removidos apenas residuos locais de bytecode fora de `.venv/` e `archive/`: `32` diretorios `__pycache__` e `0` arquivos `.pyc/.pyo` soltos. A validacao final ficou verde.

## 7. Criterio de fechamento

| criterio | status |
| --- | --- |
| inventario de entradas do shell | fechado |
| cruzamento com router oficial | fechado |
| classificacao por item | fechado |
| owner e condicao de saida para nao-SPA | fechado |
| SSR compat/legacy nao apresentados como SPA | fechado |
| ambiguidades remanescentes | `0` |

Decisao objetiva: `fechado`.

## 8. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| links backend SSR no shell | remanescente controlado | manter classificados ate existir substituto SPA/API e migracao material. |
| `/missoes` e `/pernoites` | legacy vivo controlado | nao remover neste bloco; seguir condicao de saida de legacy vivo. |
| `/manual/usuario.pdf` | externo operacional | manter como superficie documental/operacional explicitamente classificada. |
| rotas dinamicas `#/tripulantes/<id>` e `#/treinamentos/<id>` | SPA viva fora do menu primario | nao sao pendencia do shell global. |
| baseline `admin/routes.py:748>725` | remanescente fora do bloco | nao foi tratado nem mascarado. |
