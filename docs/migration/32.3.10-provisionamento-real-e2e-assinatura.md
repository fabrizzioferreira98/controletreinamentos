# 32.3.10 - Provisionamento real de E2E e assinatura final

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `32.3.10-provisionamento-real-e2e-assinatura` |
| objetivo | eliminar os bloqueios materiais remanescentes de pre-condicao do gate operacional final da Frente 32, localizando e carregando variaveis reais de E2E e assinatura em fonte viva de homolog. |
| candidato final | `release_20260420_2932_210420` |
| ambiente | `homolog` |
| resultado do bloco | `BLOQUEADO` |
| `GO_OPERACIONAL_FINAL` emitido | nao |
| assinatura gerada | nao |

Esta etapa nao altera candidato final, nao altera codigo de produto, nao usa backup morto, template, exemplo, quarentena, placeholder, segredo inventado ou inferencia manual.

## 2. Mudanca aplicada

Nao houve provisionamento material porque nenhuma das quatro variaveis obrigatorias foi encontrada em fonte viva real de homolog.

| item | acao |
| --- | --- |
| `E2E_DATABASE_URL` | nao provisionada; classificada como `ausente`. |
| `E2E_LOGIN` | nao provisionada; classificada como `ausente`. |
| `E2E_PASSWORD` | nao provisionada; classificada como `ausente`. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | nao provisionada; classificada como `ausente`. |
| processo do gate | preflight oficial permanece `missing_required_env`. |
| pacote externo | nao alterado. |
| codigo de produto | nao alterado. |

## 3. Prova material

### 3.1 Fontes vivas verificadas

| fonte | papel operacional | SHA-256 |
| --- | --- | --- |
| `C:\srv\controle-treinamentos\env\hml.env` | env ativo de homolog apontado pelo servico `CT-App-Hml` | `c8df9499bfb524426a40c2b53b7481dda5cf2ba253ca485c7636f94fc5df3381` |
| `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.xml` | service XML ativo de homolog | `3ee9f7673c79e47835f0737589e8c363c62a786c62374ebef72a66d98f13b001` |
| `C:\apps\controle-treinamentos\scripts\windows\Invoke-AppService.ps1` | wrapper historico chamado pelo service XML | `96f39d14692a6b678c0cab3b2a1252ff58915ef55be552e85e20b702ff0faa40` |
| `C:\apps\controle-treinamentos\ops\windows\scripts\Invoke-AppService.ps1` | wrapper operacional que importa o env ativo | `ab54c913f9ac7e9c600f65e79bc29920e512812b84ea148adc61be94332624c6` |
| `C:\apps\controle-treinamentos\ops\windows\scripts\Common.ps1` | funcao operacional `Import-ControleEnvFile` | `f83515f604c344d3f2ca702615a0dbd0dfe0846907f5565e0225a88f4ca8f05d` |
| `C:\srv\controle-treinamentos\tasks\backup-hml.ps1` | task operacional HML | `149d8681e5f39b2782a9f66679b714591387a27cfaca5fdb99f53953b99f238f` |
| `C:\srv\controle-treinamentos\tasks\dbcheck-hml.ps1` | task operacional HML | `fb99cc185b2fa4654418e1674e6e77ea00e0d449277a60e39488b780a2621a2b` |
| `C:\srv\controle-treinamentos\tasks\worker-hml.ps1` | task operacional HML | `0f4246ea8ff96ae677bbf6ce9ea748067407e3e1f4210dab1f88714bdb66b0a8` |
| `C:\srv\controle-treinamentos\frontend\hml\config.js` | config frontend HML ativa | `96a9760c6c49f1d91aabc976866ff265c0615140c3420346634331882cb77fe0` |
| `C:\srv\controle-treinamentos\env\alerts_external_2934a.webhook` | segredo operacional vivo de alerta externo, nao E2E/assinatura | `b6a64313817e7e4d4477925ba916795e4ae097c70ca83b357654661596be05e4` |

O inventario vivo encontrou apenas `env`, `services`, `tasks` e configuracao HML ativa como fontes aplicaveis. Foram excluidos explicitamente backups, quarentena, templates, arquivos de exemplo e material morto.

### 3.2 Cadeia viva de carregamento HML

| etapa | leitura material |
| --- | --- |
| servico Windows | `CT-App-Hml` esta instalado como `Controle Treinamentos - App Hml`. |
| service XML | chama `scripts\windows\Invoke-AppService.ps1` com `-EnvironmentName hml` e `-EnvFile "C:\srv\controle-treinamentos\env\hml.env"`. |
| wrapper historico | delega para `ops\windows\scripts\Invoke-AppService.ps1`. |
| wrapper operacional | chama `Import-ControleEnvFile -EnvFile $resolvedEnvFile`. |
| fonte carregada | `C:\srv\controle-treinamentos\env\hml.env`. |

### 3.3 Classificacao individual das variaveis

| variavel | classificacao | origem/fonte verificada | presenca | comprimento carregado | observacao |
| --- | --- | --- | --- | ---: | --- |
| `E2E_DATABASE_URL` | `ausente` | `hml.env`, service XML, wrappers, tasks HML, frontend config HML, env process/user/machine | nao | `0` | nao ha valor material para carregar no processo do gate. |
| `E2E_LOGIN` | `ausente` | `hml.env`, service XML, wrappers, tasks HML, frontend config HML, env process/user/machine | nao | `0` | nao ha usuario tecnico/canonico real publicado em fonte viva. |
| `E2E_PASSWORD` | `ausente` | `hml.env`, service XML, wrappers, tasks HML, frontend config HML, env process/user/machine | nao | `0` | nao ha segredo real correspondente em fonte viva. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `ausente` | `hml.env`, service XML, wrappers, tasks HML, frontend config HML, env process/user/machine | nao | `0` | nao ha chave real para assinatura HMAC final. |

### 3.4 Evidencia de ausencia no `hml.env` ativo

O `hml.env` ativo contem `DATABASE_URL` e `METRICS_TOKEN`, mas nao contem as quatro variaveis desta etapa.

| variavel | presente no `hml.env` ativo |
| --- | --- |
| `DATABASE_URL` | `true` |
| `METRICS_TOKEN` | `true` |
| `E2E_DATABASE_URL` | `false` |
| `E2E_LOGIN` | `false` |
| `E2E_PASSWORD` | `false` |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `false` |

## 4. Teste / check

### 4.1 Preflight redigido apos importacao do env HML

Foi usado o mesmo algoritmo de importacao do wrapper operacional, carregando `C:\srv\controle-treinamentos\env\hml.env` no processo e imprimindo somente presenca/comprimento.

Resultado:

| variavel | classificacao | carregada no processo | comprimento | origem |
| --- | --- | --- | ---: | --- |
| `E2E_DATABASE_URL` | `ausente` | `false` | `0` | `C:\srv\controle-treinamentos\env\hml.env` |
| `E2E_LOGIN` | `ausente` | `false` | `0` | `C:\srv\controle-treinamentos\env\hml.env` |
| `E2E_PASSWORD` | `ausente` | `false` | `0` | `C:\srv\controle-treinamentos\env\hml.env` |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `ausente` | `false` | `0` | `C:\srv\controle-treinamentos\env\hml.env` |

### 4.2 Preflight oficial do gate

Foi chamada apenas a funcao de pre-condicao do proprio `run_release_strict.py`, sem executar GO, sem gerar assinatura e sem alterar pacote:

```text
from ops.scripts.release.run_release_strict import _required_env
```

Resultado redigido:

```json
{
  "source": "C:\\srv\\controle-treinamentos\\env\\hml.env",
  "gate_preflight_ok": false,
  "missing_required_env": [
    "E2E_DATABASE_URL",
    "E2E_LOGIN",
    "E2E_PASSWORD",
    "RELEASE_EVIDENCE_SIGNING_KEY"
  ],
  "checked": {
    "E2E_DATABASE_URL": {"present": false, "length": 0},
    "E2E_LOGIN": {"present": false, "length": 0},
    "E2E_PASSWORD": {"present": false, "length": 0},
    "RELEASE_EVIDENCE_SIGNING_KEY": {"present": false, "length": 0}
  }
}
```

## 5. Criterio de fechamento

| criterio | status |
| --- | --- |
| `E2E_DATABASE_URL` em fonte viva real | nao atendido |
| `E2E_LOGIN` em fonte viva real | nao atendido |
| `E2E_PASSWORD` em fonte viva real | nao atendido |
| `RELEASE_EVIDENCE_SIGNING_KEY` em fonte viva real | nao atendido |
| quatro variaveis carregaveis no processo do gate | nao atendido |
| valor fake/placeholder evitado | atendido |
| candidato final preservado | atendido |
| codigo de produto preservado | atendido |
| GO nao declarado | atendido |
| README indexado | atendido apos atualizacao |

Decisao do bloco: `BLOQUEADO`.

O criterio de fechamento em `PASS` nao foi atendido, porque as quatro variaveis permanecem ausentes em fonte viva real e nao podem ser carregadas no processo do gate.

## 6. Divida rebaixada ou remanescente

| item | status | justificativa | encaminhamento |
| --- | --- | --- | --- |
| fonte viva real de homolog | rebaixada/fechada como inventario | a cadeia `CT-App-Hml.xml -> Invoke-AppService -> hml.env` foi confirmada. | usar `hml.env` ou secret store vivo equivalente como fonte futura. |
| `E2E_DATABASE_URL` | remanescente bloqueante | ausente em fontes vivas verificadas. | provisionar valor real de homolog em fonte operacional viva. |
| `E2E_LOGIN` | remanescente bloqueante | ausente em fontes vivas verificadas. | provisionar usuario real de E2E homolog. |
| `E2E_PASSWORD` | remanescente bloqueante | ausente em fontes vivas verificadas. | provisionar segredo real correspondente. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | remanescente bloqueante | ausente em fontes vivas verificadas. | provisionar chave real para assinatura HMAC. |
| gate operacional final | remanescente bloqueante | `_required_env()` segue retornando `missing_required_env`. | rerodar o gate somente apos provisionamento real. |
| assinatura final | remanescente bloqueante | sem chave real nao ha assinatura material valida. | assinar apenas apos chave real estar presente. |
