# 32.3.13.a - Rerun final completo do gate oficial com env real e assinatura

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `32.3.13.a-rerun-final-completo-gate-com-env-real-assinatura` |
| objetivo | executar o `run_release_strict.py` ate o fim, com pre-condicoes materiais reais satisfeitas, sobre o candidato congelado. |
| candidato final | `release_20260420_2932_210420` |
| ambiente | `homolog` |
| manifest usado | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json` |
| checklist usado | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md` |
| resultado do bloco | `BLOQUEADO` |
| classificacao obrigatoria | `BLOQUEADO` |
| exit code do gate | `1` |
| `GO_OPERACIONAL_FINAL` emitido | nao |

O gate oficial foi executado e nao foi substituido por checklist, leitura documental, smoke isolado ou governanca.

## 2. Mudanca aplicada

Foi executado o rerun oficial do `run_release_strict.py` com o `hml.env` vivo carregado no processo transitorio. Nao houve alteracao de candidato, codigo de produto, manifest, checklist, assinatura ou pacote externo.

Nenhum artefato novo foi gerado automaticamente pelo gate no pacote externo, porque o bloqueio ocorreu no preflight de variaveis obrigatorias antes da execucao dos checks internos.

| item | acao |
| --- | --- |
| env vivo HML | carregado somente no processo transitorio do rerun. |
| manifest final | usado como entrada; nao alterado. |
| checklist final | usado como entrada; nao alterado. |
| assinatura | exigida por validacao aplicavel; nao gerada. |
| pacote externo | preservado. |

## 3. Prova material

### 3.1 Pre-condicoes declaradas

| pre-condicao | estado material antes do rerun | efeito |
| --- | --- | --- |
| `32.3.10.b em PASS` | nao atendida; `32.3.10.b` esta `BLOQUEADO`. | impede gate completo. |
| `32.3.11.a em PASS` | nao atendida; `32.3.11.a` esta `BLOQUEADO`. | impede assinatura valida. |
| `32.3.12 em PASS` | atendida. | pacote temporalmente normalizado, mas nao elimina pre-condicoes ausentes. |

### 3.2 Artefatos de entrada

| item | caminho | SHA-256 |
| --- | --- | --- |
| manifest final | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json` | `c108cba15eb1856e2c6e66236d161988d103d58d0dd26e67f912517a72752f1d` |
| checklist final | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md` | `5ba62af4180b3873bcfc8ba0807d695896c1fa3ebcb5f267e3c4738133eb0cbb` |

### 3.3 Preflight redigido do processo

| variavel | presente | comprimento | origem |
| --- | --- | ---: | --- |
| `DATABASE_URL` | `true` | `81` | `C:\srv\controle-treinamentos\env\hml.env` |
| `E2E_DATABASE_URL` | `false` | `0` | nao localizada em fonte viva real |
| `E2E_LOGIN` | `false` | `0` | nao localizada em fonte viva real |
| `E2E_PASSWORD` | `false` | `0` | nao localizada em fonte viva real |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `false` | `0` | nao localizada em fonte viva real |

### 3.4 Saida material do gate

Comando executado:

```powershell
.\.venv\Scripts\python.exe ops\scripts\release\run_release_strict.py --base-url http://127.0.0.1:8102 --evidence-manifest "C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json" --regression-checklist "C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md" --evidence-max-age-hours 24
```

Saida relevante redigida:

```text
preflight_env_redacted:
DATABASE_URL present=True length=81
E2E_DATABASE_URL present=False length=0
E2E_LOGIN present=False length=0
E2E_PASSWORD present=False length=0
RELEASE_EVIDENCE_SIGNING_KEY present=False length=0
FAIL: variaveis obrigatorias ausentes: E2E_DATABASE_URL, E2E_LOGIN, E2E_PASSWORD, RELEASE_EVIDENCE_SIGNING_KEY
run_release_strict_exit=1
```

Evento estruturado relevante:

```json
{
  "timestamp": "2026-04-22T02:09:44.196438+00:00",
  "event": "release_gate_blocked",
  "component": "run_release_strict",
  "release_id": "release_20260420_2932_210420",
  "reason": "missing_required_env",
  "missing_env": [
    "E2E_DATABASE_URL",
    "E2E_LOGIN",
    "E2E_PASSWORD",
    "RELEASE_EVIDENCE_SIGNING_KEY"
  ]
}
```

## 4. Teste / check

### 4.1 Resultado do gate oficial

| check | resultado |
| --- | --- |
| `run_release_strict.py` executado | sim |
| candidato final usado | sim |
| manifest final usado | sim |
| checklist final usado | sim |
| env vivo HML carregado no processo | sim |
| gate substituido por leitura documental | nao |
| gate chegou aos checks funcionais | nao |
| exit code | `1` |
| classificacao | `BLOQUEADO` |
| motivo | `missing_required_env` |

Esta classificacao nao e `FAIL funcional`, porque o gate nao rodou ate uma reprova funcional completa. Ele travou por pre-condicao material ausente.

### 4.2 Validacao assinada aplicavel

A validacao assinada foi executada como check aplicavel, sem substituir o gate oficial.

```powershell
.\.venv\Scripts\python.exe ops\scripts\release\validate_operational_evidence.py --manifest "C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json" --profile release --max-age-hours 24 --require-hashes --require-signature --require-alert-ack --require-rollback-runtime-ids --regression-checklist "C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md"
```

Resultado material:

```json
{
  "success": false,
  "checked_artifacts": 21,
  "issues": [
    "signature_missing",
    "signing_key_env_missing:RELEASE_EVIDENCE_SIGNING_KEY"
  ]
}
```

| campo | valor |
| --- | --- |
| exit code | `1` |
| assinatura exigida | sim |
| `signature_missing` | presente |
| `signing_key_env_missing:RELEASE_EVIDENCE_SIGNING_KEY` | presente |

## 5. Criterio de fechamento

| criterio | status |
| --- | --- |
| executar gate oficial real | atendido |
| usar candidato congelado | atendido |
| usar manifest/checklist finais | atendido |
| usar variaveis reais carregadas | atendido apenas para `DATABASE_URL`; as demais seguem ausentes. |
| exigir assinatura quando aplicavel | atendido por validacao assinada aplicavel. |
| registrar saida material | atendido |
| substituir gate por checklist/documentacao | nao ocorreu |
| terminar verde | nao atendido |

Decisao do bloco: `BLOQUEADO`.

Nao fecha em `PASS`, porque o gate oficial terminou com exit code `1` antes dos checks funcionais. Nao fecha como `FAIL funcional`, porque o bloqueio ocorreu em pre-condicao material ausente.

## 6. Divida rebaixada ou remanescente

| item | status | justificativa | encaminhamento |
| --- | --- | --- | --- |
| rerun oficial solicitado | rebaixado/fechado como execucao material | `run_release_strict.py` foi executado contra o candidato final. | manter esta etapa como prova do estado atual. |
| `DATABASE_URL` | rebaixado | carregado do env vivo HML no processo transitorio. | manter fonte viva HML para o gate. |
| `E2E_DATABASE_URL` | remanescente bloqueante | ausente no processo do gate. | provisionar fonte viva real de homolog. |
| `E2E_LOGIN` | remanescente bloqueante | ausente no processo do gate. | provisionar usuario real de E2E. |
| `E2E_PASSWORD` | remanescente bloqueante | ausente no processo do gate. | provisionar segredo real correspondente. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | remanescente bloqueante | ausente no processo do gate e na validacao assinada. | provisionar chave real de assinatura HMAC. |
| assinatura HMAC | remanescente bloqueante | validacao oficial ainda acusa `signature_missing` e `signing_key_env_missing`. | gerar assinatura somente com chave real. |
| GO operacional final | nao emitido | gate final segue `BLOQUEADO`. | emitir somente apos rerun verde de verdade. |
