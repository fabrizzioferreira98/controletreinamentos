# 18.6.P1.5 - Aposentar/isolar artefatos operacionais residuais

## Escopo fechado

- `sync_tripulantes_snapshot.py` -> compat residual em trilha de aposentadoria
- `cleanup_operational_data.py` -> manual/destrutivo
- `run_db_consistency.py --repair` -> repair manual/perigoso
- novos usos de `sistema_controle` -> congelados

## Decisao operacional

- `backend/tools/maintenance/run_db_consistency.py` fica restrito a validacao
- repair sai da trilha `maintenance` e passa a existir apenas em `backend/tools/manual_unsafe/run_db_repair.py`
- cleanup destrutivo passa a existir apenas em `backend/tools/manual_unsafe/cleanup_operational_data.py`
- sync de tripulantes passa a existir apenas como entrada residual em `backend/tools/compat_residual/sync_tripulantes_snapshot.py`

## Congelamento de uso novo

- `sistema_controle` ganha policy explicita de chaves residuais permitidas
- hygiene de repositorio passa a bloquear novo uso de `sistema_controle` fora da allowlist atual
- release gate deixa de aceitar repair embutido como passo normal

## Condicao futura de saida

- `sync_tripulantes_snapshot.py` -> sair quando homologacao/paridade nao depender mais de snapshot reidratado de tripulantes
- `cleanup_operational_data.py` -> sair quando houver rotina segura e segmentada por classe de dado, sem delete amplo
- `run_db_repair.py` -> sair quando repair generico virar migrations corretivas/pontuais auditaveis
- `sistema_controle` -> sair como superficie mista quando notificacoes e cache residual forem movidos para owners especificos
