# 31.4.2 - Artefatos locais de baixo risco

## 1. Bloco executado

Bloco: `31.4.2-artefatos-locais-baixo-risco`.

Escopo executado:

- `.tmp`
- `.vscode`
- `.ruff_cache`
- `.pytest_cache`
- `.mypy_cache`
- `__pycache__` fora de `.venv/`, `archive/` e `frontend/dist/`
- `*.pyc` e `*.pyo` fora de `.venv/`, `archive/` e `frontend/dist/`
- `archive/local-artifacts/.DS_Store`

Fora de escopo preservado:

- `.venv/`
- `archive/` legitimo
- `frontend/dist/`
- `admin/routes.py`
- wrappers e legacy

## 2. Mudanca aplicada

Limpeza fisica aplicada somente a artefatos gerados locais:

| item | estado antes | acao |
| --- | --- | --- |
| `.tmp` | ausente | nenhuma remocao necessaria |
| `.vscode` | ausente | nenhuma remocao necessaria |
| `.ruff_cache` | ausente | nenhuma remocao necessaria |
| `.pytest_cache` | ausente | nenhuma remocao necessaria |
| `.mypy_cache` | ausente | nenhuma remocao necessaria |
| `archive/local-artifacts/.DS_Store` | ausente | nenhuma remocao necessaria |
| `__pycache__` fora de areas permitidas | 32 diretorios | removidos |
| `*.pyc` / `*.pyo` fora de areas permitidas | 134 arquivos | removidos |

Nenhum arquivo de produto, wrapper, legacy, archive legitimo, `.venv/` ou `frontend/dist/` foi alterado.

## 3. Prova material

Inventario antes da limpeza:

```text
.tmp=False
.vscode=False
.ruff_cache=False
.pytest_cache=False
.mypy_cache=False
archive\local-artifacts\.DS_Store=False
pycache_outside_allowed=32
bytecode_outside_allowed=134
```

Remocao aplicada apos verificacao de paths dentro de `C:\apps\controle-treinamentos` e fora de `.venv/`, `archive/` e `frontend/dist/`:

```text
removed_dirs=32
removed_bytecode_files=134
```

Inventario final:

```text
.tmp=False
.vscode=False
.ruff_cache=False
.pytest_cache=False
.mypy_cache=False
archive\local-artifacts\.DS_Store=False
pycache_outside_allowed=0
bytecode_outside_allowed=0
```

## 4. Teste / check

Comandos executados:

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B -m pytest tests\ops\test_repo_hygiene_validation.py -q -p no:cacheprovider
$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B ops\scripts\repo\validate_repo_hygiene.py --root .
```

Resultados:

```text
tests/ops/test_repo_hygiene_validation.py -> 11 passed
validate_repo_hygiene.py --root . -> success=true, violations=[]
__pycache__ fora de .venv/archive/frontend/dist -> 0
*.pyc/*.pyo fora de .venv/archive/frontend/dist -> 0
```

## 5. Criterio de fechamento

Fechado.

O bloco removeu apenas residuos locais de baixo risco, manteve `.venv/`, archive legitimo e `frontend/dist/` intactos, e deixou hygiene verde.

## 6. Divida rebaixada ou remanescente

| item | status |
| --- | --- |
| Residuos locais de baixo risco | rebaixados; contagem final zerada fora das areas permitidas. |
| `admin/routes.py:748>725` | remanescente fora deste bloco; nao corrigido. |
| Wrappers e legacy | nao tocados; permanecem nos blocos proprios. |
| Caches futuros | se reaparecerem, tratar como violacao local e remover com a mesma guarda de path. |
