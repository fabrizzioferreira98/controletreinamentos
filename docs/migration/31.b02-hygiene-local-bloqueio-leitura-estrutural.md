# 31.B02 - Hygiene local que bloqueia leitura estrutural

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `B02-hygiene-local-bloqueio-leitura-estrutural` |
| data | 2026-04-22 |
| objetivo | remover somente residuos locais gerados que bloqueavam a leitura estrutural do repositorio. |
| escopo | `.tmp`, `__pycache__`, `.pyc` e `.pyo` fora das excecoes permitidas. |
| fora do escopo | frontend visual, design system, wrappers, legacy, archive, `frontend/dist`, `.venv`, codigo de produto e `admin/routes.py:748>725`. |
| decisao | `fechado` |

## 2. Inventario objetivo antes da limpeza

| grupo | contagem | classificacao | observacao |
| --- | ---: | --- | --- |
| `.tmp` | 1 pasta, 26 itens internos | `residuo_local_gerado` | continha JSONs de post-deploy smoke e copias geradas de frontend para `frontend-hml` e `frontend-prod`. |
| `__pycache__` fora das excecoes | 41 diretorios | `residuo_local_gerado` | bytecode Python gerado em backend, frontend scripts, ops e tests. |
| `.pyc` fora das excecoes | 138 arquivos | `residuo_local_gerado` | bytecode Python gerado dentro dos `__pycache__`. |
| `.pyo` fora das excecoes | 0 arquivos | `residuo_local_gerado` | nada a remover. |
| `.venv` | preservado | `excecao_permitida` | ambiente local permitido pela governanca. |
| `archive` | preservado | `excecao_permitida` | historico classificado fora do escopo. |
| `frontend/dist` | preservado | `excecao_permitida` | artefato gerado permitido; nao e fonte viva nem alvo deste bloco. |

## 3. Limpeza restrita aplicada

Foram removidos apenas:

- `.tmp`;
- 41 diretorios `__pycache__` fora das excecoes permitidas;
- os 138 `.pyc` contidos nesses diretorios.

A primeira tentativa de remocao retornou `Acesso negado` para os alvos do workspace. A limpeza foi reexecutada de forma restrita, com validacao previa de que todos os alvos resolvidos estavam dentro de `C:\apps\controle-treinamentos` e fora das areas preservadas.

Nao houve remocao de `archive`, `.venv`, `frontend/dist`, wrappers, legacy, docs historicas ou codigo de produto.

## 4. Estado depois

| grupo | contagem depois |
| --- | ---: |
| `.tmp` | ausente |
| `__pycache__` fora das excecoes | 0 |
| `.pyc` fora das excecoes | 0 |
| `.pyo` fora das excecoes | 0 |

## 5. Check focal

Comando executado:

```powershell
.venv\Scripts\python.exe ops\scripts\repo\validate_repo_hygiene.py --root .
```

Resultado:

```json
{
  "success": true,
  "root": "C:\\apps\\controle-treinamentos",
  "violations": []
}
```

## 6. Regressao minima

Este bloco nao alterou codigo de produto, contrato HTTP, frontend fonte, wrappers, legacy, archive ou comandos canonicos. Por isso, a validacao focal adequada foi o repo hygiene validator.

O baseline arquitetural `admin/routes.py:748>725` permanece conhecido e fora deste bloco. Ele nao foi corrigido, mascarado ou reclassificado como verde.

## 7. Criterio de fechamento

| criterio | status |
| --- | --- |
| inventario material de `.tmp`, `__pycache__`, `.pyc` e `.pyo` | fechado |
| excecoes `.venv`, `archive` e `frontend/dist` preservadas | fechado |
| remocao limitada a residuos locais gerados | fechado |
| validator rerodado sem `--report-only` | fechado |
| hygiene verde sem violacoes | fechado |
| baseline arquitetural separado preservado | fechado |

Decisao objetiva: `fechado`.

## 8. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| `admin/routes.py:748>725` | remanescente | tratar somente em bloco arquitetural proprio, sem misturar com hygiene local. |
| `frontend/dist` | preservado | continua artefato gerado permitido, nao fonte viva. |
| `.venv` | preservado | continua excecao local permitida. |
| `archive` | preservado | continua historico classificado, fora de limpeza local. |
| `__pycache__` futuro | rebaixado | se reaparecer apos testes locais, tratar como residuo gerado e limpar antes de declarar hygiene verde. |
