# 31.3.2 - Wrappers auxiliares

## Bloco executado

Bloco: `31.3.2-wrappers-auxiliares`.

Escopo fechado:

- `scripts/database/run_db_consistency.py`
- `scripts/jobs/run_notifications.py`

Superficies canonicas comparadas, mas fora do bloco como wrapper:

- `backend/tools/maintenance/run_db_consistency.py`
- `backend/tools/maintenance/run_notifications.py`

## Mudanca aplicada

Nenhum wrapper foi removido.

Classificacao formal por item:

| item | papel | comando canonico real | evidencia | classificacao 31.3.2 | decisao |
| --- | --- | --- | --- | --- | --- |
| `scripts/database/run_db_consistency.py` | wrapper historico auxiliar tolerado | `backend/tools/maintenance/run_db_consistency.py` | header `COMPAT`, delegacao fina, listado em `scripts/README.md`, protegido por `tests/unit/test_legacy_windows_compat.py`; rotina manual/atalho externo nao auditado | `compat_residual_congelado` | manter; nao remover sem prova externa/manual |
| `scripts/jobs/run_notifications.py` | wrapper historico auxiliar tolerado | `backend/tools/maintenance/run_notifications.py` | header `COMPAT`, delegacao fina, listado em `scripts/README.md`, protegido por teste; chamada externa/manual nao auditada | `compat_residual_congelado` | manter; nao remover sem prova externa/manual |
| `backend/tools/maintenance/run_db_consistency.py` | comando canonico real | ele mesmo | citado por `docs/operations/canonical-commands.md` como caminho oficial de consistencia de banco | `nao_wrapper_ou_fora_do_bloco` | preservar como trilha canonica |
| `backend/tools/maintenance/run_notifications.py` | comando canonico real | ele mesmo | citado por `docs/operations/canonical-commands.md` como caminho oficial de notificacoes manuais | `nao_wrapper_ou_fora_do_bloco` | preservar como trilha canonica |

Resultado da triagem:

- `compat_vivo_permitido`: nenhum item neste bloco, porque nenhum consumidor externo real foi comprovado como ativo.
- `compat_residual_congelado`: dois wrappers auxiliares.
- `sem_consumidor_real_removivel`: nenhum item, porque ausencia objetiva de consumidor externo/manual nao foi comprovada.
- `nao_wrapper_ou_fora_do_bloco`: comandos canonicos usados apenas como referencia de destino.

Atualizacoes documentais aplicadas:

- `scripts/README.md` ratifica os wrappers auxiliares como `compat_residual_congelado`;
- `docs/governance/removal-backlog.md` ratifica o status de bloqueio da 31.3.2;
- este registro define a taxonomia final da 31.3.2 para estes wrappers.

## Prova material

Evidencias materiais:

- os dois wrappers em `scripts/` existem;
- os dois wrappers declaram `COMPAT` no cabecalho;
- os dois wrappers delegam para a trilha canonica;
- `scripts/README.md` lista wrapper, destino canonico, consumidor atual, pre-condicao e risco;
- `docs/operations/canonical-commands.md` aponta os comandos oficiais fora de `scripts/`;
- busca interna por referencias encontrou usos em docs/backlog/hygiene/testes e nao encontrou chamada operacional interna que obrigue `scripts/`;
- a ausencia de consumidor externo/manual nao foi provada, logo nenhum wrapper recebeu `sem_consumidor_real_removivel`.

## Teste / check

Checks aplicaveis:

- check de referencias internas para os dois wrappers auxiliares;
- `tests/unit/test_legacy_windows_compat.py` valida existencia dos shims;
- `validate_repo_hygiene.py --root .` valida header `COMPAT`, alvo canonico e fila de remocao em `scripts/README.md`.

Resultados executados:

- check de referencias internas via `Select-String`: referencias encontradas em docs/backlog/hygiene/testes e nos proprios wrappers; nenhuma chamada operacional interna obriga `scripts/` como entrada principal;
- `$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B -m pytest tests\unit\test_legacy_windows_compat.py -q -p no:cacheprovider`: `1 passed`;
- `$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B ops\scripts\repo\validate_repo_hygiene.py --root .`: `success=true`, `violations=[]`;
- contagem de `__pycache__` fora de `.venv/` e `archive/`: `0`.

## Criterio de fechamento

Fechado.

Cada wrapper auxiliar tem classificacao formal, comando canonico separado, evidencia e decisao. Nenhum item ficou implicitamente removivel e nenhum comando canonico foi confundido com wrapper.

## Divida rebaixada ou remanescente

Divida remanescente nao-bloqueante para a Frente 31:

- auditar consumidores externos/manuais reais: atalhos locais, runbooks copiados, rotinas manuais e jobs fora do repo;
- remover wrapper somente em bloco futuro com prova de ausencia/migracao de consumidor externo/manual e atualizacao do teste de shim.
