# 31.3.1 - Wrappers alto risco

## Bloco executado

Bloco: `31.3.1-wrappers-alto-risco`.

Escopo fechado:

- `scripts/backup/run_backups.py`
- `scripts/jobs/run_jobs_worker.py`
- `scripts/windows/Invoke-AppService.ps1`
- `scripts/windows/Invoke-OperationalPython.ps1`

Superficies canonicas comparadas, mas fora do bloco como wrapper:

- `backend/tools/maintenance/run_backups.py`
- `backend/tools/maintenance/run_jobs_worker.py`
- `ops/windows/scripts/Invoke-AppService.ps1`
- `ops/windows/scripts/Invoke-OperationalPython.ps1`

## Mudanca aplicada

Nenhum wrapper foi removido.

Classificacao formal por item:

| item | papel | comando canonico real | evidencia | classificacao 31.3.1 | decisao |
| --- | --- | --- | --- | --- | --- |
| `scripts/backup/run_backups.py` | wrapper historico tolerado | `backend/tools/maintenance/run_backups.py` | header `COMPAT`, delegacao fina, listado em `scripts/README.md`, protegido por `tests/unit/test_legacy_windows_compat.py`; consumidor externo de backup/atalho nao auditado | `compat_residual_congelado` | manter; nao remover sem prova externa |
| `scripts/jobs/run_jobs_worker.py` | wrapper historico tolerado | `backend/tools/maintenance/run_jobs_worker.py` | header `COMPAT`, delegacao fina, listado em `scripts/README.md`, protegido por teste; Task Scheduler/rotina externa nao auditados | `compat_residual_congelado` | manter; nao remover sem prova externa |
| `scripts/windows/Invoke-AppService.ps1` | wrapper historico tolerado | `ops/windows/scripts/Invoke-AppService.ps1` | header `COMPAT`, delegacao fina, listado em `scripts/README.md`, protegido por teste; servico/atalho Windows externo nao auditado | `compat_residual_congelado` | manter; nao remover sem prova externa |
| `scripts/windows/Invoke-OperationalPython.ps1` | wrapper historico tolerado | `ops/windows/scripts/Invoke-OperationalPython.ps1` | header `COMPAT`, delegacao fina, listado em `scripts/README.md`, protegido por teste; tarefa agendada/rotina externa nao auditada | `compat_residual_congelado` | manter; nao remover sem prova externa |
| `backend/tools/maintenance/run_backups.py` | comando canonico real | ele mesmo | citado por `docs/operations/canonical-commands.md` como caminho oficial de backup | `nao_wrapper_ou_fora_do_bloco` | preservar como trilha canonica |
| `backend/tools/maintenance/run_jobs_worker.py` | comando canonico real | ele mesmo | citado por `docs/operations/canonical-commands.md` como caminho oficial de worker | `nao_wrapper_ou_fora_do_bloco` | preservar como trilha canonica |
| `ops/windows/scripts/Invoke-AppService.ps1` | comando canonico real | ele mesmo | citado por `docs/operations/canonical-commands.md` como subida operacional Windows/self-hosted | `nao_wrapper_ou_fora_do_bloco` | preservar como trilha canonica |
| `ops/windows/scripts/Invoke-OperationalPython.ps1` | comando canonico real | ele mesmo | citado por docs Windows/self-hosted e usado como destino canonico de wrapper | `nao_wrapper_ou_fora_do_bloco` | preservar como trilha canonica |

Resultado da triagem:

- `compat_vivo_permitido`: nenhum item neste bloco, porque nenhum consumidor externo real foi comprovado como ativo.
- `compat_residual_congelado`: quatro wrappers de alto risco.
- `sem_consumidor_real_removivel`: nenhum item, porque ausencia objetiva de consumidor externo nao foi comprovada.
- `nao_wrapper_ou_fora_do_bloco`: comandos canonicos usados apenas como referencia de destino.

Atualizacoes documentais aplicadas:

- `scripts/README.md` ratifica os wrappers de alto risco como `compat_residual_congelado`;
- `docs/governance/removal-backlog.md` ratifica o status de bloqueio da 31.3.1;
- este registro substitui qualquer leitura anterior de `compat_vivo_permitido` para este bloco.

## Prova material

Evidencias materiais:

- os quatro wrappers em `scripts/` existem;
- os quatro wrappers declaram `COMPAT` no cabecalho;
- os quatro wrappers delegam para a trilha canonica;
- `scripts/README.md` lista wrapper, destino canonico, consumidor atual, pre-condicao e risco;
- `docs/operations/canonical-commands.md` aponta os comandos oficiais fora de `scripts/`;
- busca interna por referencias encontrou usos em docs/backlog/hygiene/testes, nos proprios wrappers e no validador de hygiene;
- resultado explicito de consumidor interno: nao foi encontrada chamada operacional interna viva que obrigue `scripts/` como entrada principal para estes quatro wrappers;
- a ausencia de consumidor externo nao foi provada, logo nenhum wrapper recebeu `sem_consumidor_real_removivel`.

Busca executada:

```powershell
Select-String -SimpleMatch em arquivos textuais vivos, excluindo `.venv`, `archive` e `frontend/dist`.
```

Observacao de ambiente: `rg` foi tentado primeiro, mas retornou `Acesso negado`; a busca material foi feita com `Select-String`.

Resultado por wrapper:

| wrapper | consumidor interno operacional | consumidor externo |
| --- | --- | --- |
| `scripts/backup/run_backups.py` | nao encontrado; referencias internas ficam em docs, backlog, testes, validador e no proprio wrapper | ausencia nao provada |
| `scripts/jobs/run_jobs_worker.py` | nao encontrado; referencias internas ficam em docs, backlog, testes, validador e no proprio wrapper | ausencia nao provada |
| `scripts/windows/Invoke-AppService.ps1` | nao encontrado; referencias internas ficam em docs, backlog, testes, validador e no proprio wrapper | ausencia nao provada |
| `scripts/windows/Invoke-OperationalPython.ps1` | nao encontrado; referencias internas ficam em docs, backlog, testes, validador e no proprio wrapper | ausencia nao provada |

## Teste / check

Checks aplicaveis:

- check de referencias internas para os quatro wrappers;
- `tests/unit/test_legacy_windows_compat.py` valida existencia dos shims;
- `validate_repo_hygiene.py --root .` valida header `COMPAT`, alvo canonico e fila de remocao em `scripts/README.md`.

Resultados executados:

- check de referencias internas via `Select-String`: referencias encontradas em docs/backlog/hygiene/testes, validador e nos proprios wrappers; nenhuma chamada operacional interna obriga `scripts/` como entrada principal;
- `.\.venv\Scripts\python.exe -B -m pytest tests\unit\test_legacy_windows_compat.py -q -p no:cacheprovider`: `1 passed`;
- `$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B ops\scripts\repo\validate_repo_hygiene.py --root .`: `success=true`, `violations=[]`;
- contagem de `__pycache__` fora de `.venv/` e `archive/`: `0`.

Nota de hygiene: havia 35 diretorios `__pycache__` gerados fora de `.venv/` e `archive/`; foram removidos como artefatos gerados locais apos verificacao de paths dentro do workspace, sem alterar codigo de produto.

## Criterio de fechamento

Fechado.

Cada item de alto risco tem classificacao formal, comando canonico separado, evidencia e decisao. Nenhum item ficou implicitamente removivel e nenhum comando canonico foi confundido com wrapper.

## Divida rebaixada ou remanescente

Divida remanescente nao-bloqueante para a Frente 31:

- auditar consumidores externos reais: Task Scheduler, servicos Windows, atalhos, jobs manuais e rotinas fora do repo;
- remover wrapper somente em bloco futuro com prova de ausencia/migracao de consumidor externo e atualizacao do teste de shim.
