# 32.3.10.a - Localizacao ampliada das fontes reais faltantes de E2E e assinatura

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `32.3.10.a-localizacao-ampliada-fontes-reais-e2e-assinatura` |
| objetivo | localizar, em fontes vivas reais de homolog, as variaveis ainda ausentes de E2E e assinatura. |
| candidato final | `release_20260420_2932_210420` |
| ambiente | `homolog` |
| pacote externo | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420` |
| resultado do bloco | `BLOQUEADO` |
| `GO_OPERACIONAL_FINAL` emitido | nao |
| assinatura gerada | nao |

Esta etapa nao altera candidato, codigo de produto, manifest, checklist ou pacote externo. Tambem nao usa template, backup morto, archive, arquivo de exemplo, placeholder, segredo inventado ou valor inferido.

## 2. Mudanca aplicada

Foi criada apenas documentacao de migration para registrar a busca ampliada e a classificacao das variaveis faltantes.

| item | acao |
| --- | --- |
| `E2E_DATABASE_URL` | nao localizada em fonte viva real; classificada como `ausente`. |
| `E2E_LOGIN` | nao localizada em fonte viva real; classificada como `ausente`. |
| `E2E_PASSWORD` | nao localizada em fonte viva real; classificada como `ausente`. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | nao localizada em fonte viva real; classificada como `ausente`. |
| candidato final | preservado. |
| codigo de produto | preservado. |
| manifest/checklist | preservados. |
| pacote externo | preservado. |

## 3. Prova material

### 3.1 Fontes vivas realmente auditadas

| fonte auditada | tipo | leitura material | resultado para as 4 variaveis |
| --- | --- | --- | --- |
| `Process`, `User`, `Machine` via `[Environment]::GetEnvironmentVariable()` | ambiente ativo Windows | presenca/comprimento consultados sem imprimir valores | todas ausentes |
| servico Windows ativo `CT-App-Hml` | servico homolog | `Running`, `Auto`, binario em `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.exe` | variaveis ausentes na cadeia de carregamento |
| `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.xml` | service XML ativo homolog | SHA-256 `3ee9f7673c79e47835f0737589e8c363c62a786c62374ebef72a66d98f13b001` | nomes das variaveis nao encontrados |
| `C:\apps\controle-treinamentos\scripts\windows\Invoke-AppService.ps1` | wrapper chamado pelo service XML | wrapper material da cadeia HML | nomes das variaveis nao encontrados |
| `C:\apps\controle-treinamentos\ops\windows\scripts\Invoke-AppService.ps1` | wrapper operacional | importa env operacional por `Import-ControleEnvFile` | nomes das variaveis nao encontrados |
| `C:\apps\controle-treinamentos\ops\windows\scripts\Common.ps1` | helper operacional | contem a funcao de importacao de env | nomes das variaveis nao encontrados |
| `C:\srv\controle-treinamentos\env\hml.env` | env ativo homolog | SHA-256 `c8df9499bfb524426a40c2b53b7481dda5cf2ba253ca485c7636f94fc5df3381` | nomes das variaveis nao encontrados |
| `C:\srv\controle-treinamentos\env\alerts_external_2934a.webhook` | segredo operacional vivo de alerta externo | SHA-256 `b6a64313817e7e4d4477925ba916795e4ae097c70ca83b357654661596be05e4` | nao contem variaveis E2E/assinatura |
| `C:\srv\controle-treinamentos\tasks\backup-hml.ps1` | rotina operacional HML no diretorio vivo | SHA-256 `149d8681e5f39b2782a9f66679b714591387a27cfaca5fdb99f53953b99f238f` | nomes das variaveis nao encontrados |
| `C:\srv\controle-treinamentos\tasks\dbcheck-hml.ps1` | rotina operacional HML no diretorio vivo | SHA-256 `fb99cc185b2fa4654418e1674e6e77ea00e0d449277a60e39488b780a2621a2b` | nomes das variaveis nao encontrados |
| `C:\srv\controle-treinamentos\tasks\worker-hml.ps1` | rotina operacional HML no diretorio vivo | SHA-256 `0f4246ea8ff96ae677bbf6ce9ea748067407e3e1f4210dab1f88714bdb66b0a8` | nomes das variaveis nao encontrados |
| Task Scheduler / `schtasks` | tasks ativas Windows | consulta elevada nao retornou task relevante de `controle-treinamentos` | sem fonte viva adicional |
| `C:\srv-data\controle-treinamentos\homolog\runtime\restore_pg_*` | diretorios de runtime homolog | arquivos `pg_hba.conf`, `pg_ident.conf`, `postgresql.auto.conf`, `postgresql.conf` verificados por nome | nomes das variaveis nao encontrados |
| `C:\srv\controle-treinamentos\caddy\Caddyfile` | proxy ativo | SHA-256 `a63f6049017dd5a405cb3fe746b8d1e4086c1e3c22b28b86d4c95ab8d19ab1f0` | nao e secret store; nomes nao encontrados |
| servico Windows ativo `CT-Caddy` | proxy ativo | `Running`, `Auto`, binario em `C:\srv\controle-treinamentos\services\CT-Caddy\CT-Caddy.exe` | nao e fonte de E2E/assinatura |

### 3.2 Fontes vivas irmas ou nao aplicaveis descartadas

| fonte descartada | motivo objetivo do descarte |
| --- | --- |
| `C:\srv\controle-treinamentos\services\CT-App-Prod\CT-App-Prod.xml` | servico ativo de producao, nao homolog; nomes das variaveis tambem nao encontrados. |
| `C:\srv\controle-treinamentos\env\prod.env` | env ativo de producao, nao pode abastecer gate homolog; nomes das variaveis tambem nao encontrados. |
| `C:\srv\controle-treinamentos\tasks\backup-prod.ps1` | rotina de producao, fora do aceite homolog; nomes das variaveis nao encontrados. |
| `C:\srv\controle-treinamentos\tasks\dbcheck-prod.ps1` | rotina de producao, fora do aceite homolog; nomes das variaveis nao encontrados. |
| `C:\srv\controle-treinamentos\tasks\worker-prod.ps1` | rotina de producao, fora do aceite homolog; nomes das variaveis nao encontrados. |
| `C:\srv\controle-treinamentos\config-backup` | backup/config historico, explicitamente nao vivo para provisionamento. |
| `C:\srv\controle-treinamentos\repo-quarantine` | quarentena, explicitamente fora de fonte real utilizavel. |
| `C:\srv\controle-treinamentos\caddy\Caddyfile.bak-*` e `Caddyfile.fixbak.*` | backups do proxy, nao fonte viva. |
| `C:\srv-data\controle-treinamentos\homolog\evidence\*` | evidencias historicas do pacote, nao secret store vivo para provisionar gate. |
| `C:\srv-data\controle-treinamentos\hml\logs` e `uploads` | saida operacional, nao fonte de segredo/config para gate. |
| ferramentas em `C:\srv\controle-treinamentos\tools` | binarios/documentacao de ferramenta, nao fonte operacional de segredo. |
| servicos Windows de sistema com nomes contendo "Controle" em PT-BR | servicos do Windows sem relacao com o produto. |

### 3.3 Classificacao individual das variaveis

| variavel | classificacao | origem presente | comprimento | prova de ausencia |
| --- | --- | --- | ---: | --- |
| `E2E_DATABASE_URL` | `ausente` | nenhuma fonte viva real auditada | `0` | ausente em env ativo HML, service XML, wrappers, tasks HML, Task Scheduler, runtime homolog e escopos `Process`/`User`/`Machine`. |
| `E2E_LOGIN` | `ausente` | nenhuma fonte viva real auditada | `0` | ausente em env ativo HML, service XML, wrappers, tasks HML, Task Scheduler, runtime homolog e escopos `Process`/`User`/`Machine`. |
| `E2E_PASSWORD` | `ausente` | nenhuma fonte viva real auditada | `0` | ausente em env ativo HML, service XML, wrappers, tasks HML, Task Scheduler, runtime homolog e escopos `Process`/`User`/`Machine`. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `ausente` | nenhuma fonte viva real auditada | `0` | ausente em env ativo HML, service XML, wrappers, tasks HML, Task Scheduler, runtime homolog e escopos `Process`/`User`/`Machine`. |

Nenhuma variavel ficou como `presente_em_fonte_viva_real`. Nenhuma variavel ficou como `ambigua`: as fontes auditadas nao apontam para valor alternativo utilizavel, apenas ausencia.

## 4. Teste / check

### 4.1 Check focal de ambiente ativo

Foi executado check redigido de presenca/comprimento, sem imprimir valores:

| escopo | `E2E_DATABASE_URL` | `E2E_LOGIN` | `E2E_PASSWORD` | `RELEASE_EVIDENCE_SIGNING_KEY` |
| --- | --- | --- | --- | --- |
| `Process` | ausente, comprimento `0` | ausente, comprimento `0` | ausente, comprimento `0` | ausente, comprimento `0` |
| `User` | ausente, comprimento `0` | ausente, comprimento `0` | ausente, comprimento `0` | ausente, comprimento `0` |
| `Machine` | ausente, comprimento `0` | ausente, comprimento `0` | ausente, comprimento `0` | ausente, comprimento `0` |

### 4.2 Check de fontes operacionais ampliadas

| check | resultado |
| --- | --- |
| servicos Windows ativos auditados | `CT-App-Hml`, `CT-App-Prod`, `CT-Caddy`; apenas `CT-App-Hml` aplicavel a homolog. |
| Task Scheduler / `schtasks` auditado | nenhuma task relevante encontrada para fornecer variaveis E2E/assinatura. |
| wrappers operacionais da cadeia HML auditados | nenhum define as variaveis faltantes. |
| env vivo HML auditado | nao contem as quatro variaveis. |
| rotinas HML em diretorio vivo auditadas | nao contem as quatro variaveis. |
| runtime homolog auditado | nao contem as quatro variaveis. |
| fontes descartadas registradas | sim. |
| valores sensiveis impressos no documento | nao. |

### 4.3 Efeito no gate

O ultimo gate oficial materializado em `32.3.13` permanece aplicavel como leitura do estado atual:

| item | valor |
| --- | --- |
| artefato | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\documentary\gate_final_strict_32_3_13.json` |
| SHA-256 | `a994517c981e213ece642c3dd107eb4c6562aa9f91980943cc3e812984c92100` |
| status | `BLOQUEADO` |
| reason | `missing_required_env` |
| variaveis faltantes | `E2E_DATABASE_URL`, `E2E_LOGIN`, `E2E_PASSWORD`, `RELEASE_EVIDENCE_SIGNING_KEY` |

## 5. Criterio de fechamento

| criterio | status |
| --- | --- |
| procurar em fontes vivas reais alem das ja verificadas | atendido |
| listar cada fonte viva auditada | atendido |
| classificar cada variavel individualmente | atendido |
| registrar fontes descartadas por historicas/nao vivas/nao homolog | atendido |
| nao vazar segredo | atendido no documento |
| nao alterar candidato/codigo/manifest/checklist/pacote | atendido |
| localizar as quatro variaveis em fonte viva real utilizavel pelo gate | nao atendido |

Decisao do bloco: `BLOQUEADO`.

O criterio de `PASS` nao foi atingido porque nenhuma das quatro variaveis foi localizada em fonte viva real de homolog utilizavel pelo gate.

## 6. Divida rebaixada ou remanescente

| item | status | justificativa | encaminhamento |
| --- | --- | --- | --- |
| localizacao ampliada de fontes | rebaixada/fechada como auditoria | fontes vivas ampliadas foram auditadas e descartes foram registrados. | usar este documento como baseline de ausencia. |
| `E2E_DATABASE_URL` | remanescente bloqueante | ausente em fontes vivas reais auditadas. | provisionar em fonte operacional viva de homolog. |
| `E2E_LOGIN` | remanescente bloqueante | ausente em fontes vivas reais auditadas. | provisionar usuario E2E real de homolog. |
| `E2E_PASSWORD` | remanescente bloqueante | ausente em fontes vivas reais auditadas. | provisionar segredo real correspondente. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | remanescente bloqueante | ausente em fontes vivas reais auditadas. | provisionar chave real de assinatura HMAC em fonte viva. |
| gate operacional final | remanescente bloqueante | continua bloqueado por `missing_required_env`. | rerodar apenas apos provisionamento real das quatro variaveis. |
| assinatura final | remanescente bloqueante | sem chave real nao ha assinatura HMAC materialmente valida. | gerar/validar assinatura somente apos chave real existir. |
