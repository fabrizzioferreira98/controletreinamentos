# 34.2.13 - Hardening runtime de links e superficies responsivas

## Bloco executado

Execucao focal apos QA runtime da Frente 34 para eliminar os `REVIEW` remanescentes em links/rotas navegaveis cobertos pelo harness responsivo.

## Superficie tratada

- Owner frontend: `frontend/src/app.css`.
- Evidencia runtime: `runtime/responsive-qa-34/`.
- Rotas validadas: `#/dashboard`, `#/tripulantes`, `#/tripulantes/1`, `#/relatorios/produtividade/jornadas`, `#/relatorios/produtividade/extrato-periodo` e rota inexistente controlada.

## Mudanca aplicada

- Links, botoes e `.button-link` dentro da area de conteudo passaram a ter largura maxima controlada e quebra segura de texto.
- Inputs invisiveis de toggle foram limitados para nao gerarem overflow horizontal fantasma.
- Uploads de arquivo em detalhe de tripulante passaram a respeitar largura util.
- Sticky actions de formularios de entidade deixaram de usar margem negativa em viewport menor.
- Calendario do dashboard em mobile deixou de depender de grade fixa de 500px e passou a adaptar a grade ao viewport.

## Prova material

- `frontend/src/app.css` concentra os ajustes transversais sem alterar rotas, handlers, payloads ou backend.
- `frontend/dist/` foi regenerado pelo build canonico para publicar os ajustes fora do build isolado de QA.
- Build publicado no root produtivo servido pelo Caddy: `C:/srv/controle-treinamentos/frontend/prod`.
- Backup pre-deploy preservado em `C:/srv/controle-treinamentos/frontend/backup-prod-20260425-101107-responsive-34`.
- Validacao local via Caddy/HTTPS para `controle.brasilvida.app.br` retornou `200 OK`, `index.html` com cache buster `20260425-100353` e `app.css` com os ajustes responsivos.
- `runtime/responsive-qa-34/responsive_qa_report.md` registra a matriz runtime final.
- `runtime/responsive-qa-34/screenshots/` contem 36 capturas por breakpoint/rota.

## Teste / check focal

- Build frontend para `runtime/frontend-build-34-runtime-qa`.
- Build frontend canonico para `frontend/dist`.
- Deploy estatico para producao via `robocopy` de `frontend/dist` para `C:/srv/controle-treinamentos/frontend/prod`: 40 arquivos copiados, 0 falhas.
- Caddy escutando `192.168.25.33:443` e servindo o build atualizado.
- Contratos responsivos: `50 passed`.
- Runtime visual: `36 checks`, `36 PASS`, `0 REVIEW`.

## Regressao minima

- Mudancas restritas a CSS e ao harness de QA local.
- Nenhum contrato, endpoint, permissao, rota ou regra de dominio foi alterado.
- A navegacao principal, acoes por linha, upload e links de detalhe permanecem funcionais.

## Criterio de fechamento

O bloco fecha quando todos os links/rotas cobertos pelo runtime visual passam sem overflow nao controlado, sem perda de acao principal e sem erro de rota em desktop, notebook, tablet e mobile.

## Divida rebaixada ou remanescente

- Rebaixada: reviews de detalhe de tripulante, calendario mobile do dashboard e sticky actions mobile.
- Remanescente: acesso publico HTTPS de `controle.brasilvida.app.br` ainda falha em `201.16.254.25:443` a partir do servidor, apesar de o Caddy local/LAN servir o build corretamente; tratar rota de borda/pfSense/NAT fora da frente de frontend.
- Remanescente: validar futuramente com backend/dados reais de homolog para complementar o runtime local com fixtures.
