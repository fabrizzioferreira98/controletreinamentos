# 86 - Limpeza reporting/PDF/calculos de Painel TV e Produtividade

## Escopo

Limpeza de lineage remanescente apos as remocoes frontend (`84`) e backend (`85`), cobrindo relatorios, PDFs, exports, calculos auxiliares, polling, snapshots operacionais, artefatos QA e documentacao viva ligada a Painel TV e Produtividade.

## Remocoes aplicadas

- Snapshot operacional obsoleto `ops/forensics_missoes_rendered.html` removido; ele continha links SSR antigos, menu de Painel TV/Produtividade e coluna textual de Produtividade.
- Guard de limpeza QA em `ops/scripts/qa/hml_runtime_polish_smoke.py` deixou de consultar `produtividade_conferencias`, evitando dependencia operacional em tabela exclusiva da frente retirada.
- Artefato estatico `ops/ui-validation/mission-crew-validation.html` teve microcopy de produtividade removida.
- Bytecode gerado em `backend/src/controle_treinamentos/**/__pycache__` removido para eliminar strings antigas de endpoints, services e calculos ja excluidos do codigo fonte.
- Registros historicos `docs/migration/35.tv-panel-*.md` foram rebaixados para `docs/migration/retired-platforms/`, deixando de aparecer como itens vivos no indice principal.
- README raiz e `docs/migration/README.md` atualizados para registrar apenas as remocoes e nao mais listar implementacoes historicas do painel como leitura operacional corrente.
- `frontend/dist` regenerado pelo build oficial para eliminar CSS/JS gerado antigo com classes stale de produtividade.

## Lineage validado

- `backend/src/controle_treinamentos/reports`, `contracts`, `application`, `repositories`, `service_layers`, rotas SSR/API e templates nao possuem superficie executavel remanescente de Painel TV/Produtividade fora de schema historico.
- Endpoints e exports exclusivos ja haviam sido removidos no bloco `85`; esta fase validou que nao restaram botoes/exportadores/PDF helpers vivos apontando para eles.
- Polling remanescente encontrado pertence apenas a monitoramento vivo e assets terceiros; nao ha polling de `/api/v1/tv` ou de relatorios de produtividade no codigo fonte vivo.
- As referencias restantes em `backend/src/controle_treinamentos/db/schema.py` e `ops/scripts/database/cleanup_operational_data.py` sao schema/dados historicos, sem endpoint, tela, PDF, export ou calculo ativo.

## Validacao

- Busca focal em backend/frontend/ops/docs/tests, excluindo `docs/migration`, `docs/archive`, `archive`, `frontend/dist`, `__pycache__` e binarios, reduziu ocorrencias vivas a schema historico, cleanup manual de dados e teste arquitetural negativo.
- Busca em relatorios/PDFs/exports vivos nao encontrou `Painel TV`, `Produtividade`, `painel-tv`, `/api/v1/tv`, `tv_vencimentos`, `tv_produtividade` ou `relatorio_produtividade`.
- Suite focal executada: `tests/architecture/test_removed_backend_surfaces.py`, `tests/contract/test_pdf_export_contracts.py`, `tests/integration/test_reports_pdf.py`, `tests/contract/test_api_relatorios_contract.py`, `tests/contract/test_api_dashboard_contract.py`, `tests/ops/test_load_test_authenticated_entrypoints.py` com `21 passed`.
- Check manual do mapa Flask para `/painel-tv`, `/api/v1/tv`, `/produtividade` e `/api/v1/relatorios/produtividade` retornou `[]`.
- Limpeza final de `backend/src/controle_treinamentos/**/__pycache__` confirmada com `0` arquivos `.pyc`.
- Build oficial executado com `.venv\Scripts\python.exe frontend\scripts\build_frontend.py --env-file frontend\.env.example`; busca posterior em `frontend/dist` nao encontrou referencias removidas.

## Fechamento

Fechado para reporting, PDFs, exports, calculos, polling e lineage executavel. A divida remanescente e somente de schema/dados historicos, preservada ate decisao explicita de retencao/remocao de tabelas.
