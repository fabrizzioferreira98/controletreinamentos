# 32.resume.real - Auditoria e provisionamento real do gate operacional final

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `32.resume.real-auditoria-provisionamento-gate-final` |
| natureza | auditoria material e tentativa controlada de materializacao transitoria do env do gate |
| data/hora local | `2026-04-22T09:34:16-03:00` |
| candidato final | `release_20260420_2932_210420` |
| ambiente | `homolog` |
| pacote externo | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420` |
| gate oficial | `ops\scripts\release\run_release_strict.py` |
| preflight oficial | `ops.scripts.release.run_release_strict._required_env()` |
| resultado | `BLOQUEADO` |
| `GO_OPERACIONAL_FINAL` emitido | nao |
| codigo de produto alterado | nao |
| candidato/manifest/checklist/pacote alterados | nao |

Esta etapa nao usa segredo inventado, template, backup, archive, exemplo, producao fora do escopo, placeholder de CI ou fonte indireta sem consumo pelo processo do gate.

## 2. Fontes vivas auditadas

| fonte | tipo | prova redigida | resultado para as quatro variaveis |
| --- | --- | --- | --- |
| `Process`, `User`, `Machine` | variaveis de ambiente do host local | consulta por `[Environment]::GetEnvironmentVariable()` | `E2E_DATABASE_URL`, `E2E_LOGIN`, `E2E_PASSWORD`, `RELEASE_EVIDENCE_SIGNING_KEY` ausentes; `present=false`, `length=0`. |
| `C:\srv\controle-treinamentos\env\hml.env` | env operacional vivo de homolog | SHA-256 `C8DF9499BFB524426A40C2B53B7481DDA5CF2BA253CA485C7636F94FC5DF3381` | contem `DATABASE_URL` com `present=true`, `length=81`; nao contem as quatro variaveis bloqueantes. |
| servico Windows `CT-App-Hml` | servico homolog ativo | `Running`, `Auto`, binario `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.exe` | cadeia ativa existe, mas nao materializa as quatro variaveis. |
| `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.xml` | service XML homolog ativo | SHA-256 `3EE9F7673C79E47835F0737589E8C363C62A786C62374EBEF72A66D98F13B001` | nomes das quatro variaveis nao encontrados. |
| `C:\apps\controle-treinamentos\scripts\windows\Invoke-AppService.ps1` | wrapper de servico | SHA-256 `96F39D14692A6B678C0CAB3B2A1252FF58915EF55BE552E85E20B702FF0FAA40` | nomes das quatro variaveis nao encontrados. |
| `C:\apps\controle-treinamentos\ops\windows\scripts\Invoke-AppService.ps1` | wrapper operacional | SHA-256 `AB54C913F9AC7E9C600F65E79BC29920E512812B84EA148ADC61BE94332624C6` | nomes das quatro variaveis nao encontrados. |
| `C:\apps\controle-treinamentos\ops\windows\scripts\Common.ps1` | helper operacional de env | SHA-256 `F83515F604C344D3F2CA702615A0DBD0DFE0846907F5565E0225A88F4CA8F05D` | nomes das quatro variaveis nao encontrados. |
| `C:\srv\controle-treinamentos\tasks\backup-hml.ps1` | task HML local | SHA-256 `149D8681E5F39B2782A9F66679B714591387A27CFACA5FDB99F53953B99F238F` | nomes das quatro variaveis nao encontrados. |
| `C:\srv\controle-treinamentos\tasks\dbcheck-hml.ps1` | task HML local | SHA-256 `FB99CC185B2FA4654418E1674E6E77EA00E0D449277A60E39488B780A2621A2B` | nomes das quatro variaveis nao encontrados. |
| `C:\srv\controle-treinamentos\tasks\worker-hml.ps1` | task HML local | SHA-256 `0F4246EA8FF96AE677BBF6CE9EA748067407E3E1F4210DAB1F88714BDB66B0A8` | nomes das quatro variaveis nao encontrados. |
| Task Scheduler | tasks ativas Windows | consulta elevada sem linhas relevantes para `controle-treinamentos` ou variaveis do gate | nenhuma fonte viva adicional encontrada. |
| Credential Manager local | possivel secrets store do host | `cmdkey /list` filtrado por produto/hml/e2e/release/sign | nenhuma credencial relevante encontrada. |
| `C:\ProgramData` | possivel secrets/config store local | busca por diretorios relacionados a produto/hml/homolog/secret | nenhum diretorio relevante encontrado. |
| `C:\srv-data\controle-treinamentos\homolog\runtime` | runtime homolog | varredura redigida por nomes das variaveis | nomes das quatro variaveis nao encontrados. |
| roots vivos `C:\srv\controle-treinamentos`, `C:\srv-data\controle-treinamentos`, `C:\ProgramData\controle-treinamentos`, `C:\ProgramData\ControleTreinamentos` | arquivos operacionais vivos, excluindo backup/archive/template/quarentena/exemplo | busca por atribuicoes exatas das quatro variaveis | nenhuma atribuicao aceitavel encontrada. |

Fontes identificadas mas rejeitadas:

| fonte | motivo de rejeicao |
| --- | --- |
| `.github\workflows\release-strict-gate.yml` | contem `${{ secrets.* }}`, ponteiro de CI sem materializacao local; nao e fonte viva consumida pelo processo local do gate. |
| `.github\workflows\promotion-ci.yml` | contem `${{ secrets.* }}`, ponteiro de CI sem materializacao local; nao e fonte viva consumida pelo processo local do gate. |
| `C:\srv\controle-treinamentos\env\*.bak*` | backup historico, explicitamente nao aceitavel. |
| `C:\srv\controle-treinamentos\env\prod.env` | producao fora do escopo homolog. |
| `archive`, `repo-quarantine`, exemplos/templates | fora da trilha viva homolog. |

## 3. Classificacao das quatro variaveis

| variavel | classificacao | present | length | source |
| --- | --- | --- | ---: | --- |
| `E2E_DATABASE_URL` | `ausente` | `false` | `0` | ausente em fontes vivas homolog auditadas; placeholder de CI descartado como nao aceitavel. |
| `E2E_LOGIN` | `ausente` | `false` | `0` | ausente em fontes vivas homolog auditadas; placeholder de CI descartado como nao aceitavel. |
| `E2E_PASSWORD` | `ausente` | `false` | `0` | ausente em fontes vivas homolog auditadas; placeholder de CI descartado como nao aceitavel. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `ausente` | `false` | `0` | ausente em fontes vivas homolog auditadas; placeholder de CI descartado como nao aceitavel. |

Nao houve variavel classificada como `presente_em_fonte_viva_real`. Nao houve variavel classificada como `ambigua_nao_aceitavel` porque os ponteiros de CI foram explicitamente descartados e nao materializam valor no processo local.

## 4. Materializacao transitoria

Foi carregado no processo transitorio somente o env vivo homolog aceitavel:

```text
C:\srv\controle-treinamentos\env\hml.env
```

Preflight oficial redigido:

```json
{
  "source_env_loaded": "C:\\srv\\controle-treinamentos\\env\\hml.env",
  "gate_preflight_ok": false,
  "missing_required_env": [
    "E2E_DATABASE_URL",
    "E2E_LOGIN",
    "E2E_PASSWORD",
    "RELEASE_EVIDENCE_SIGNING_KEY"
  ],
  "checked": {
    "DATABASE_URL": {
      "present": true,
      "length": 81
    },
    "E2E_DATABASE_URL": {
      "present": false,
      "length": 0
    },
    "E2E_LOGIN": {
      "present": false,
      "length": 0
    },
    "E2E_PASSWORD": {
      "present": false,
      "length": 0
    },
    "RELEASE_EVIDENCE_SIGNING_KEY": {
      "present": false,
      "length": 0
    }
  }
}
```

Leitura objetiva:

| item | resultado |
| --- | --- |
| `DATABASE_URL` | materializada a partir de `hml.env`, `present=true`, `length=81`. |
| quatro variaveis bloqueantes | nao materializadas; seguem `present=false`, `length=0`. |
| `missing_required_env` | nao zerou. |
| valor sensivel exposto | nao. |

## 5. Assinatura HMAC

| check | resultado |
| --- | --- |
| `RELEASE_EVIDENCE_SIGNING_KEY` materialmente presente | nao |
| `harden_release_manifest.py` executado | nao |
| `signature_hmac_sha256` gerada nesta etapa | nao |
| validacao `--require-signature` executada nesta etapa | nao |
| classificacao | `BLOQUEADO` |

A trilha de assinatura nao foi executada porque a chave real nao existe em fonte viva homolog auditada. Executar assinatura sem chave real violaria a regra de nao inventar segredo.

## 6. Teste / check

| fase | check | resultado |
| --- | --- | --- |
| Fase 1 | auditoria de fontes vivas reais | executada. |
| Fase 1 | quatro variaveis localizadas em fonte viva real | nao. |
| Fase 2 | materializacao transitoria do env vivo HML | executada para `DATABASE_URL`. |
| Fase 2 | preflight oficial `_required_env()` | executado. |
| Fase 2 | `missing_required_env` zerou | nao. |
| Fase 3 | assinatura HMAC real | nao executada; chave ausente. |
| Fase 4 | gate oficial completo | nao executado; preflight bloqueado. |

## 7. Resultado do gate

| item | valor |
| --- | --- |
| classificacao final | `BLOQUEADO` |
| motivo | `missing_required_env` |
| variaveis faltantes | `E2E_DATABASE_URL`, `E2E_LOGIN`, `E2E_PASSWORD`, `RELEASE_EVIDENCE_SIGNING_KEY` |
| exit code do preflight | `0` para o comando de verificacao redigida; `gate_preflight_ok=false` na funcao oficial |
| gate completo rodado | nao |
| `PASS` | nao |
| `FAIL_FUNCIONAL` | nao |

Nao e `FAIL_FUNCIONAL`, porque o gate nao chegou aos checks funcionais. E bloqueio material de pre-condicao.

## 8. Artefatos gerados ou atualizados

| artefato | acao |
| --- | --- |
| `docs\migration\32.resume.real-auditoria-provisionamento-gate-final.md` | criado. |
| `docs\migration\README.md` | atualizado com indexacao da etapa. |
| pacote externo do candidato | nao alterado. |
| manifest/checklist/release_decision | nao alterados. |
| codigo de produto | nao alterado. |

## 9. Criterio de fechamento

| criterio | status |
| --- | --- |
| auditar fontes vivas reais | atendido |
| nao inventar segredo | atendido |
| nao usar template/backup/archive/exemplo/producao/CI placeholder | atendido |
| registrar `present` e `length` sem expor valor | atendido |
| materializar variaveis encontradas legitimamente | atendido parcialmente: apenas `DATABASE_URL` existe no `hml.env`; as quatro bloqueantes nao existem. |
| preflight oficial executado | atendido |
| assinatura HMAC real gerada | nao atendido; chave ausente. |
| gate oficial completo executado | nao atendido; preflight bloqueado. |

Decisao da etapa: `BLOQUEADO`.

O fechamento e valido como bloqueio honestamente demonstrado: as fontes vivas foram auditadas, segredo nao foi inventado, a falta objetiva foi registrada e nenhum escopo lateral foi aberto.

## 10. Divida rebaixada ou remanescente

| item | status | justificativa | proxima acao legitima |
| --- | --- | --- | --- |
| auditoria material das fontes vivas | rebaixada/fechada | fontes locais/homolog conhecidas e provaveis foram auditadas sem expor segredo. | usar este documento como baseline redigido. |
| `DATABASE_URL` | rebaixada | presente em `hml.env` vivo e materializavel no processo. | manter fonte viva HML. |
| `E2E_DATABASE_URL` | remanescente bloqueante | ausente em fonte viva real homolog auditada. | provisionar externamente em fonte viva consumida pelo gate. |
| `E2E_LOGIN` | remanescente bloqueante | ausente em fonte viva real homolog auditada. | provisionar usuario E2E real homolog. |
| `E2E_PASSWORD` | remanescente bloqueante | ausente em fonte viva real homolog auditada. | provisionar segredo correspondente sem expor valor. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | remanescente bloqueante | ausente em fonte viva real homolog auditada. | provisionar chave HMAC real homolog. |
| assinatura HMAC | remanescente derivada | depende da chave real. | executar hardening oficial somente apos chave existir. |
| gate oficial completo | remanescente derivado | depende do preflight sem `missing_required_env`. | rerodar somente apos as quatro variaveis estarem materializadas. |
