# 31.release-final.2 - Validacao oficial da Frente 31

## 1. BLOCO EXECUTADO

| campo | valor |
| --- | --- |
| bloco | `31.release-final.2` |
| objetivo | validar oficialmente a Frente 31 pela regua correta de governanca/compat/legacy/archive/topologia. |
| resultado esperado | `PASS_DE_GOVERNANCA`, nao PASS de release operacional. |
| fora do escopo | executar `run_release_strict.py` como gate geral da 31, declarar deploy, validar CD/produto ou reabrir blocos fechados. |
| decisao | fechado como validacao oficial de governanca. |

## 2. DIAGNOSTICO DO CRITERIO DE ACEITE

`run_release_strict.py` nao e criterio geral de aceite da Frente 31.

Base objetiva:

- `31.0` abre a frente como governanca de compatibilidade, legado vivo, archive/topologia, excecoes locais e backlog de remocao futura.
- `31.A` corrige formalmente a regua de fechamento e separa `GO DE GOVERNANCA` de release operacional.
- `31.2.0` define blocos executaveis de wrappers, archive, artefatos locais, build gerado, legacy vivo e backlog.
- `31.final` e `31.6.c` declaram que o fechamento nao e GO de release operacional.

Historico preservado:

```powershell
.\.venv\Scripts\python.exe ops\scripts\release\run_release_strict.py --base-url http://127.0.0.1 --evidence-manifest docs\migration\31.release-final.manifest.json --regression-checklist docs\migration\31.release-final.checklist.md
```

Resultado historico: `FAIL` por ausencia de variaveis operacionais de release:

- `DATABASE_URL`;
- `E2E_DATABASE_URL`;
- `E2E_LOGIN`;
- `E2E_PASSWORD`;
- `RELEASE_EVIDENCE_SIGNING_KEY`.

Esse `FAIL` nao foi apagado, nao virou `PASS` e nao bloqueia o aceite geral da 31 porque pertence a release operacional fora do escopo da frente.

## 3. MUDANCA APLICADA

Atualizada esta validacao oficial para registrar a rodada material de checks da Frente 31.

Atualizados tambem:

- `docs/migration/31.release-final.manifest.json`, para hash e status de verificacao;
- `docs/migration/README.md`, para manter o indice coerente com a validacao oficial final.

Nao houve mudanca de codigo de produto, wrapper, archive, legacy ou contrato funcional.

## 4. PROVA MATERIAL

| eixo validado | prova | resultado |
| --- | --- | --- |
| blocos previstos fechados | `31.0`, `31.1`, `31.2.0`, `31.2.1`, `31.A`, `31.3.1`, `31.3.2`, `31.4.1`, `31.4.2`, `31.5.1`, `31.6.1`, `31.7.1`, `31.release-final.1` | ok |
| hygiene verde | `validate_repo_hygiene.py --root .` | `success=true`, `violations=[]` |
| wrappers congelados formalmente | `31.3.1`, `31.3.2`, `scripts/README.md`, `removal-backlog.md` | `compat_residual_congelado`, sem remocao por presuncao |
| archive classificado | `31.4.1`, `archive/MANIFEST.md` | historico congelado; nested repo nao e segunda raiz viva |
| artefatos locais limpos | `31.4.2` e hygiene real | bytecode/cache fora das excecoes: `0` |
| excecoes locais governadas | `31.5.1` | `.venv/` e `frontend/dist/` nao sao topologia oficial nem fonte viva |
| legacy vivo ratificado | `31.6.1`, `legacy/LIVE_LEGACY.md` | permanencia, evidencia viva, bloqueio de feature nova e condicao de saida por item |
| backlog refinado | `31.7.1`, `removal-backlog.md` | sem autorizacao de remocao e sem inflar severidade |
| baseline arquitetural vermelho | `tests/architecture/test_architecture_boundaries.py` | `admin/routes.py:748>725` conhecido, explicito e nao mascarado |
| manifest package | `31.release-final.manifest.json` | `manifest-package-validation=OK` |

## 5. TESTE / CHECK

Checks executados:

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B -m pytest tests\unit\test_legacy_windows_compat.py tests\ops\test_repo_hygiene_validation.py -q -p no:cacheprovider
$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B -m pytest tests\architecture\test_architecture_boundaries.py -q -p no:cacheprovider
$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B ops\scripts\repo\validate_repo_hygiene.py --root .
manifest-package-validation sobre docs\migration\31.release-final.manifest.json
busca documental por stale/NO-GO indevido
```

Resultados:

| check | resultado | leitura |
| --- | --- | --- |
| `tests/unit/test_legacy_windows_compat.py tests/ops/test_repo_hygiene_validation.py` | `12 passed` | wrappers/compat e validador de hygiene verdes |
| `tests/architecture/test_architecture_boundaries.py` | `1 failed, 1 passed` | falha conhecida `admin/routes.py:748>725`; baseline vermelho explicito |
| `validate_repo_hygiene.py --root .` | `success=true`, `violations=[]` | hygiene vivo verde |
| contagem final fora de `.venv`, `archive` e `frontend/dist` | `__pycache__=0`, `.pyc/.pyo=0` | sem residuo local vivo |
| manifest-package-validation | `OK` | `missing_artifacts=0`, `hash_mismatches=0`, `readme_missing_final_entries=0` |
| stale/NO-GO indevido | `OK` | `run_release_strict.py` fica como FAIL historico nao aplicavel; sem falso PASS |

## 6. DECISAO FORMAL

Resultado oficial: `PASS_DE_GOVERNANCA`.

Esta decisao:

- valida a Frente 31 por governanca, compat, legacy, archive/topologia, excecoes locais e backlog refinado;
- nao e PASS de release operacional;
- nao autoriza deploy;
- nao substitui `run_release_strict.py` quando uma frente futura tocar release operacional;
- nao declara verde o baseline arquitetural `admin/routes.py:748>725`.

## 7. DIVIDA REBAIXADA OU REMANESCENTE

| item | status | encaminhamento |
| --- | --- | --- |
| `admin/routes.py:748>725` | remanescente explicito | tratar em bloco/frente propria de arquitetura, sem reabrir a 31. |
| `run_release_strict.py` historico `FAIL` | preservado como fato operacional fora do escopo geral da 31 | volta a ser obrigatorio em release operacional futuro. |
| wrappers com consumidor externo nao comprovado ausente | remanescente controlado | manter `compat_residual_congelado`; remover apenas com prova de migracao/ausencia. |
| archive historico e nested repo copy | remanescente congelado | remover apenas com perda objetiva de valor de auditoria. |
| `.venv/` e `frontend/dist/` | excecoes locais governadas | nao tratar como topologia oficial ou fonte viva. |
| legacy vivo | remanescente controlado | saida somente pelas condicoes objetivas de `legacy/LIVE_LEGACY.md`. |
