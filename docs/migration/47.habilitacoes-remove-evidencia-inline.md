# 47 - Remocao da coluna Evidencia no consolidado de habilitacoes

## Bloco executado

Remocao controlada da coluna/acao inline `Evidencia` na tabela do relatorio `#/relatorios/habilitacoes`.

## Superficie real

- Frontend SPA: `frontend/src/features/relatorios/habilitacoes-page.js`.
- Rota: `#/relatorios/habilitacoes`.
- Publicacao estatica: `C:\srv\controle-treinamentos\frontend\prod`.

## Mudanca aplicada

- Removida a coluna `Acoes` da tabela do consolidado.
- Removido o link por linha `Evidencia` para `#/treinamentos/:id`.
- Ajustados `colspan` e estado vazio de `5` para `4` colunas.
- Mantidos exports do relatorio no painel inferior.
- Mantidos contratos, endpoints, dados de treinamento e anexos.

## Prova material

- Build gerado em `C:\apps\controle-treinamentos\frontend\runtime\tmp\habilitacoes-remove-evidencia-build-20260428`.
- Publicacao espelhada via `ops/windows/scripts/Publish-Frontend.ps1`.
- Backup produtivo: `C:\srv\controle-treinamentos\frontend-backup\prod-before-frontend-publish-20260428-091854`.
- Versao publicada: `20260428-091839`.
- `app.js` produtivo aponta para `./app/bootstrap.js?v=20260428-091839`.
- `features/relatorios/habilitacoes-page.js` produtivo nao possui mais `<th>Acoes</th>`, `data-label="Acoes"` nem link inline `Evidencia` por linha.

## Testes

Executado:

```powershell
.venv\Scripts\python.exe -m pytest tests\contract\test_frontend_training_reports_visual_foundation.py::test_reports_surface_adopts_shared_ui_without_export_or_action_drift tests\contract\test_frontend_table_form_patterns.py::test_table_patterns_are_adopted_in_priority_surfaces_without_route_or_api_drift -q
```

Resultado: `2 passed`.

## Regressao minima

- Nenhum endpoint alterado.
- Nenhum contrato HTTP alterado.
- Nenhuma permissao alterada.
- Nenhum dado removido.
- Nenhum anexo/evidencia de treinamento apagado.
- Mudanca restrita a apresentacao da tabela do relatorio.

## Criterio de fechamento

`fechado`

A coluna/acao inline `Evidencia` foi removida da tabela do consolidado de habilitacoes sem alterar a funcionalidade de exportacao ou os dados de treinamento.
