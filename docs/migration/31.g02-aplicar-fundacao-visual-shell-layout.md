# 31.G02 - Aplicar fundacao visual no shell e layout estrutural

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `G02-aplicar-fundacao-visual-shell-layout` |
| data | 2026-04-22 |
| objetivo | aplicar tokens e primitives de `shared/ui` ao shell autenticado e ao layout estrutural sem redesenhar paginas funcionais. |
| escopo | shell container, sidebar/nav, topbar, content region, spacing, estados basicos de navegacao, docs vivas e check contratual. |
| fora do escopo | redesign de dashboard/tripulantes, treinamentos/relatorios, SSR, runtime/deploy, design system completo e componentizacao ornamental. |
| decisao | `fechado` |

## 2. Leitura visual inicial

| achado | classificacao |
| --- | --- |
| shell ja estava arquiteturalmente separado em `frontend/src/shell/` | base pronta para adocao visual incremental |
| CSS estrutural original ainda vinha da base backend/static concatenada no build | `divida_residual_controlada` |
| `shared/ui` ja era concatenado antes de `app.css` | trilha segura para overrides frontend sem editar `frontend/dist` |
| shell continha hardcodes estruturais de inverse surface, spacing, topbar, overlay e camadas | alvo seguro para tokenizacao |
| paginas funcionais continuam fora do bloco | `nao_redesign` preservado |

## 3. Mudanca aplicada

| arquivo | mudanca |
| --- | --- |
| `frontend/src/shared/ui/tokens.css` | adiciona tokens semanticos para inverse surface, layout backdrop, navegacao, spacing estrutural, sizes do shell/content e shadow de painel mobile. |
| `frontend/src/shared/ui/primitives.css` | adiciona primitives neutros `ui-app-frame`, `ui-inverse-surface`, `ui-sticky-surface`, `ui-content-region` e `ui-navigation-list`. |
| `frontend/src/shared/ui/README.md` | registra que primitives estruturais podem ser usados no shell/layout sem virar regra de dominio. |
| `frontend/src/shell/render-shell.js` | aplica primitives no container do shell, sidebar, brand/nav/footer, topbar, topbar context e content region. |
| `frontend/src/app.css` | adiciona bloco G02 de overrides estruturais usando tokens, sem redesenhar paginas. |
| `tests/contract/test_frontend_shell_visual_foundation_boundaries.py` | adiciona check contratual para garantir adocao material no shell e preservacao do contrato funcional. |
| `docs/architecture/FRONTEND_ARCHITECTURE.md` | documenta a fronteira de aplicacao visual do shell/layout. |
| `frontend/README.md` | registra a adocao incremental do shell/layout. |

## 4. Prova material

| prova | resultado |
| --- | --- |
| markup do shell | contem `ui-app-frame`, `ui-inverse-surface`, `ui-navigation-list`, `ui-sticky-surface`, `ui-cluster` e `ui-content-region`. |
| CSS estrutural do frontend | contem bloco `G02` com `.app-shell.ui-app-frame`, `.sidebar.ui-inverse-surface`, `.nav.ui-navigation-list`, `.topbar.ui-sticky-surface` e `.content.ui-content-region`. |
| tokens usados | `--color-inverse-*`, `--space-layout-*`, `--space-navigation-*`, `--layer-overlay`, `--layer-modal`, `--size-sidebar-width`, `--size-content-wide`. |
| contrato de shell | ids `mobileMenuBtn`, `appSidebar`, `logout-button`, `renderNavigation(activeRoute)` e `BACKEND_LINKS.notificacoesEmail` preservados. |
| paginas funcionais | dashboard, tripulantes, treinamentos e relatorios nao foram redesenhados. |

## 5. Checks

Checks executados com `PYTHONDONTWRITEBYTECODE=1`:

| check | resultado |
| --- | --- |
| `tests/contract/test_frontend_shell_visual_foundation_boundaries.py` | PASS |
| `tests/contract/test_frontend_visual_foundation_boundaries.py` | PASS |
| `tests/contract/test_frontend_shell_navigation_boundaries.py` | PASS |
| `tests/contract/test_frontend_shell_architecture_boundaries.py` | PASS |
| `tests/contract/test_frontend_ux_functional_guards.py` | PASS |
| rodada focal inicial | `24 passed` |

Validacao final do bloco:

| check | resultado |
| --- | --- |
| rodada focal ampliada final | `65 passed` |
| build frontend com `--output-dir <temp fora do checkout>` | PASS em `C:\Users\FABRIZ~1.ARA\AppData\Local\Temp\controle-treinamentos-g02-4849497daeba4cadab4d5879461880c6` |
| CSS final do build | contem `G02: shell layout adopts`, `.app-shell.ui-app-frame`, `.sidebar.ui-inverse-surface`, `--color-inverse-surface`, `--space-navigation-gap` e `.ui-navigation-list` |
| hygiene validator final | `success=true`, sem violacoes |

O validator de hygiene ficou vermelho durante a rodada final somente por `__pycache__` gerado/local fora das excecoes. Foram removidos apenas residuos locais de bytecode fora de `.venv/` e `archive/`: `32` diretorios `__pycache__` e `0` arquivos `.pyc/.pyo` soltos. A validacao final com `python -B` ficou verde.

## 6. Criterio de fechamento

| criterio | status |
| --- | --- |
| tokens/primitives aplicados materialmente no shell | fechado |
| hardcodes estruturais do shell reduzidos por overrides tokenizados | fechado |
| shell/navigation/login preservados | fechado |
| paginas funcionais nao redesenhadas | fechado |
| docs vivas atualizadas | fechado |
| checks focais verdes | fechado |

Decisao objetiva do bloco: `fechado`.

## 7. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| base CSS backend/static ainda contem shell historico e aliases legados | `divida_residual_controlada` | desacoplamento CSS real exige bloco proprio; G02 usa overrides frontend seguros. |
| paginas dashboard/tripulantes/treinamentos/relatorios ainda tem familias visuais locais | remanescente planejado | migrar em blocos de pagina/feature posteriores. |
| primitives estruturais sao intencionais e pequenas | consolidado | nao expandir para UI kit completo sem uso real. |
| SSR/runtime/deploy | fora do G02 | nao tratados nem mascarados. |
