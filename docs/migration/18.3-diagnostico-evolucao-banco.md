# 18.3 - Matriz Operacional de Evolucao do Banco

Status: diagnostico encerrado. A etapa agora fica reduzida a decisao operacional sobre o que manter, congelar, isolar, aposentar ou substituir.

## 1. MATRIZ OPERACIONAL 18.3

| artefato | tipo | risco | acao futura |
| --- | --- | --- | --- |
| `backend/src/controle_treinamentos/db/schema.py` | estrutural | medio | manter |
| `backend/src/controle_treinamentos/db/migrations.py` | estrutural | alto | substituir |
| `backend/src/controle_treinamentos/db/training_program_seed.py` | dado | alto | substituir |
| `ops/scripts/database/migrate_tripulante_media_to_storage.py` | dado | alto | congelar |
| `ops/scripts/database/reconcile_tripulante_photos.py` | compat | alto | congelar |
| `ops/scripts/database/import_tripulantes_csv.py` | dado | alto | substituir |
| `ops/scripts/database/cleanup_operational_data.py` | cleanup | alto | isolar |
| `ops/scripts/database/sync_training_master_types.py` | dado | alto | substituir |
| `ops/scripts/database/sync_tripulantes_snapshot.py` | compat | alto | aposentar |
| `ops/scripts/database/run_db_consistency.py --repair` | cleanup | alto | isolar |
| `backend/src/controle_treinamentos/compat/` | compat | medio | aposentar |
| `legacy/data-compat/` | compat | medio | aposentar |
| `ops/scripts/backup/backup_restore_drill.py` | estrutural | medio | manter |

## 2. ITENS CONGELADOS

- `ops/scripts/database/migrate_tripulante_media_to_storage.py` -> migracao pontual de legado vivo; nao expandir escopo nem transformar em trilha recorrente
- `ops/scripts/database/reconcile_tripulante_photos.py` -> reparo de compat historica com efeito em dado ativo; manter apenas como excecao controlada, sem capacidade nova

## 3. ITENS EM TRILHA DE SUBSTITUICAO

- `backend/src/controle_treinamentos/db/migrations.py` -> mistura DDL, DML, seed e commits parciais -> condicao futura: sair da trilha principal em favor de migrations corretivas pontuais e bootstrap estrutural separado
- `backend/src/controle_treinamentos/db/training_program_seed.py` -> seed altera referencia usada por regra operacional -> condicao futura: virar carga controlada de referencia fora do bootstrap amplo
- `ops/scripts/database/import_tripulantes_csv.py` -> entrada massiva segue perigosa para duplicidade e sobrescrita -> condicao futura: ser trocado por fluxo de importacao validado, idempotente e fora da trilha de migracao
- `ops/scripts/database/sync_training_master_types.py` -> sincroniza cadastro mestre que afeta treinamentos e vencimentos -> condicao futura: virar sync/catalogo controlado ou migration corretiva dedicada, sem acoplamento ao fluxo amplo

## 4. BACKLOG CURTO DE MIGRATIONS CORRETIVAS

- separar `migrations.py` em bootstrap estrutural e migrations corretivas pontuais -> P0 -> hoje o arquivo ainda concentra mudancas heterogeneas demais na trilha principal
- extrair carga de referencia de `training_program_seed.py` e `sync_training_master_types.py` para migrations corretivas dedicadas -> P0 -> referencia operacional nao deve entrar misturada com bootstrap geral
- converter reparos genericos de `run_db_consistency.py --repair` em correcoes auditaveis e de escopo unico -> P1 -> reparo amplo demais precisa sair do fluxo generico
- fechar o caminho de saida de `sync_tripulantes_snapshot.py` por aposentadoria, sem nova migracao ampla de snapshot -> P1 -> snapshot de tripulantes nao deve seguir como mecanismo principal de evolucao de dado

## 5. CONCLUSAO DA ETAPA 18.3

- `schema.py` e `backup_restore_drill.py` continuam na trilha principal
- artefatos de compat historica ficam explicitados: `reconcile_tripulante_photos.py`, `backend/src/controle_treinamentos/compat/`, `legacy/data-compat/` e `sync_tripulantes_snapshot.py`
- artefatos perigosos saem da trilha principal por decisao operacional: `migrations.py`, `training_program_seed.py`, `import_tripulantes_csv.py`, `cleanup_operational_data.py`, `sync_training_master_types.py` e `run_db_consistency.py --repair`
- congelar agora significa nao expandir mais as rotinas de midia/foto legada
- substituir agora significa abrir troca futura por migrations corretivas pontuais ou fluxo dedicado fora da migration principal

Com isso, a 18.3 sai do modo diagnostico e entra em decisao operacional curta: o que continua foi reduzido ao minimo, o que vive por compat historica ficou visivel, e o que e perigoso ja entrou em fila objetiva de isolamento, congelamento, aposentadoria ou substituicao.
