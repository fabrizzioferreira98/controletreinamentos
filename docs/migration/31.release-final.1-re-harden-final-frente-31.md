# 31.release-final.1 - Re-harden final da Frente 31

## 1. BLOCO EXECUTADO

| campo | valor |
| --- | --- |
| bloco | `31.release-final.1` |
| objetivo | congelar materialmente o pacote final da Frente 31 pela regua correta de governanca. |
| escopo | checklist final, manifest final, indice de migration, documentos `31.*` e referencias de backlog, archive, scripts e legacy que entraram no fechamento. |
| fora do escopo | reabrir blocos fechados, corrigir `admin/routes.py`, alterar codigo de produto, remover wrappers, limpar archive, remover legacy ou executar release operacional. |
| decisao | fechado como re-harden final de governanca. |

## 2. RE-HARDEN

Re-hardening aplicado em modo documental:

- checklist final estabilizado em `31.release-final.checklist.md`;
- manifest final estabilizado em `31.release-final.manifest.json`;
- indice `docs/migration/README.md` coerente com os artefatos finais;
- hashes SHA-256 recalculados para artefatos cobertos pelo manifest;
- `manifest-package-validation` executado sobre o pacote final;
- baseline `admin/routes.py:748>725` preservado como vermelho conhecido, sem falso PASS;
- `run_release_strict.py` preservado como gate de release operacional futuro, nao como aceite geral da Frente 31.

## 3. HASHES

Fonte material dos hashes: `docs/migration/31.release-final.manifest.json`.

Politica aplicada:

- o manifest lista os artefatos finais, mas nao embute hash de si mesmo;
- hashes dos artefatos alterados neste re-harden foram recalculados;
- divergencia de hash no pacote final e criterio de falha documental;
- hash verde nao altera o status funcional de dividas remanescentes.

Resultado esperado do pacote apos este bloco: `manifest-package-validation: OK`.

## 4. COERENCIA DOCUMENTAL

| item | status |
| --- | --- |
| documentos principais da Frente 31 cobertos | ok |
| checklist final coberto | ok |
| consolidacao final coberta | ok |
| referencias de backlog e legacy cobertas | ok |
| README coerente com artefatos finais | ok |
| `GO DE GOVERNANCA` separado de release operacional | ok |
| `run_release_strict.py` nao usado como criterio geral da 31 | ok |
| baseline vermelho nao mascarado | ok |
| blocos fechados nao reabertos | ok |

## 5. STALE / FALTANTE

| item | status |
| --- | --- |
| artefato listado no manifest ausente | ausente |
| hash divergente | ausente apos recalc do manifest |
| stale pointer em README para artefato final | ausente |
| `NO-GO` operacional escondido | ausente; historico de FAIL operacional fica preservado como nao aplicavel ao aceite geral da 31 |
| artefato pendente fingindo PASS | ausente |
| `admin/routes.py:748>725` | remanescente conhecido, nao corrigido e nao marcado como PASS |
| wrappers, archive, local build, legacy e backlog | classificados com restricao de remocao/uso; nao promovidos a trilha principal |

## 6. CRITERIO DE FECHAMENTO

O bloco `31.release-final.1` fecha quando:

1. checklist final existe e declara governanca, nao release operacional;
2. manifest cobre documentos principais, checklist, consolidacao, backlog e legacy;
3. hashes foram recalculados e conferem;
4. README aponta para os artefatos finais;
5. nao ha stale pointer, NO-GO escondido ou artefato pendente fingindo PASS;
6. baseline vermelho conhecido segue explicitamente separado.

Decisao: `fechado como re-harden final da Frente 31`.
