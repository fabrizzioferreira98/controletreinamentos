# 18.6.P2.4 - Medicao real de hotspots SQL

## Ambiente medido

- data da medicao: 2026-04-17
- alvo: `C:\srv\controle-treinamentos\env\prod.env`
- banco real observado: `ct_prod_supabase_stage` em `127.0.0.1:5432`
- metodo: `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)`

## Volume real encontrado

- `tripulantes`: 48
- `pilotos`: 48
- `treinamentos`: 12
- `notificacoes_treinamento`: 0
- `treinamento_anexos_pdf`: 4
- `horas_voo_aeronave`: 24
- `tipos_treinamento`: 14
- `segmentos_teoricos`: 66

## Parametros tipicos usados

- `fetch_tripulante_list_page`: `status=ativo`, `base=Manaus`, `limit=20`, `offset=0`
- `count_treinamentos`: carga inicial sem filtros adicionais
- `fetch_training_page`: carga inicial sem filtros adicionais, `limit=20`, `offset=0`
- notificacoes: `include_details=True`, horizonte efetivo de 90 dias
- `fetch_training_program_record_list`: `tipo_treinamento_id=5`, `aeronave_modelo_snapshot='King Air B200/200/C90A/C90GT'`
- `build_habilitacoes_consolidadas_context`: `base=Manaus`, `status=vencido`, `tipo=5`

## Hotspots medidos

### 1. `fetch_tripulante_list_page`

- SQL real: query de colecao em `tripulantes` com `LEFT JOIN pilotos`, `LEFT JOIN bases`, filtro por status/base com fallback `pilotos -> tripulantes`
- plano:
  - `Seq Scan tripulantes` com 48 linhas
  - `Seq Scan pilotos` com 48 linhas
  - `Hash Join` e filtro residual por `LOWER(TRIM(COALESCE(...)))`
  - `Sort` final por `t.nome`
- medicao: `planning=10.110 ms`, `execution=0.467 ms`
- classificacao: `compat residual contaminando predicado`, mas custo atual e legitimo no volume real; sem tuning adicional

### 2. `count_treinamentos`

- SQL real: contagem agregada somente em `treinamentos`
- plano:
  - `Seq Scan treinamentos` com 12 linhas
  - `Aggregate`
- medicao: `planning=3.421 ms`, `execution=0.052 ms`
- classificacao: `custo legitimo`; o tuning local de P0.6 continua suficiente

### 3. `fetch_training_page`

- SQL real: `WITH ranked AS (...)` para ordenar/paginar antes do `LEFT JOIN equipamentos`
- plano:
  - `Seq Scan treinamentos` com 12 linhas
  - `Hash Join` com `tripulantes`
  - `Sort` de 12 linhas dentro do `ranked`
  - `Index Scan equipamentos_pkey` apenas apos `LIMIT/OFFSET`
- medicao: `planning=2.381 ms`, `execution=0.200 ms`
- classificacao: `paginacao custosa ja reduzida`; custo atual e legitimo

### 4. notificacoes (`fetch_notification_training_rows`)

- SQL real: CTE `eligible` em `treinamentos`, calculo inline de `gatilho` e `already_sent_today`
- plano:
  - `Seq Scan treinamentos` com filtro `data_vencimento <= horizon`, retornando 5 linhas
  - `Index Scan idx_notificacoes_treinamento_treinamento_gatilho_data`
  - `Sort` final por vencimento/id
- medicao: `planning=2.445 ms`, `execution=0.147 ms`
- classificacao: `indice adequado + custo legitimo`; nenhum tuning adicional

### 5. `fetch_training_program_record_list`

- SQL real observado antes do ajuste:
  - `LEFT JOIN LATERAL` em `pilotos` para matricula
  - `EXISTS` correlacionado em `horas_voo_aeronave` para `ctac_required`
- causa dominante encontrada:
  - releitura por linha da lista em `pilotos`
  - custo correlacionado repetido para derivar `ctac_required`
- tuning local aplicado:
  - trocado `LEFT JOIN LATERAL` por `LEFT JOIN pilotos p ON p.tripulante_id = c.id`
  - `ctac_required` saiu do `EXISTS` correlacionado e passou para `LEFT JOIN` em `ctac_refs`
  - nenhum indice novo
- medicao antes do ajuste, em 15 execucoes:
  - media `0.6948 ms`
  - mediana `0.6240 ms`
- medicao depois do ajuste, em 15 execucoes:
  - media `0.5053 ms`
  - mediana `0.4220 ms`
- ganho observado:
  - media `-27.3%`
  - mediana `-32.4%`
- plano depois do ajuste:
  - `Index Scan idx_pilotos_tripulante_id` no lugar do `Seq Scan + Sort + Limit` por linha
  - `Seq Scan treinamentos` com 11 linhas validas
  - `Seq Scan horas_voo_aeronave` ainda existe no caminho de CTAC, mas nao justificou indice novo no volume atual
- classificacao: `query ruim local`, corrigida sem redesign e sem indice

### 6. `build_habilitacoes_consolidadas_context`

- SQL real observado:
  - query 1: `tripulantes` + `pilotos` + `bases`, sem `JOIN treinamentos`
  - query 2: `treinamentos` + `tipos_treinamento`, filtrando `tripulante_id = ANY(...)`, `tipo_treinamento_id` e janela de status
- plano:
  - query 1: `Seq Scan tripulantes` (48), `Seq Scan pilotos` (48), `Seq Scan bases` (10), `Hash Join`, filtro final por base
  - query 2: `Seq Scan treinamentos` (12), filtro para 2 linhas, `Seq Scan tipos_treinamento`
- medicao:
  - query 1: `planning=0.278 ms`, `execution=0.110 ms`
  - query 2: `planning=0.158 ms`, `execution=0.030 ms`
- classificacao: `custo legitimo`; shape de P1.2 continua adequado

## Decisao sobre `aeronave_modelo`

- evidencias reais medidas nesta etapa nao justificam novo indice em `treinamentos.aeronave_modelo`
- evidencias reais tambem nao justificam indice funcional novo em `LOWER(COALESCE(t.aeronave_modelo, ''))`
- motivo:
  - o custo dominante que apareceu em `fetch_training_program_record_list` era shape de query, nao ausencia de indice
  - `treinamentos` tem apenas 12 linhas e `horas_voo_aeronave` tem 24 linhas no banco real medido
  - os predicados continuam function-wrapped, entao indice cego aqui continuaria sendo tuning sem prova

## O que nao foi mexido de proposito

- nenhum indice novo
- nenhuma migration
- nenhuma mudanca de schema
- nenhum redesign de paginação/listagem fora do hotspot com evidencia real

## Fechamento desta etapa

- a medicao real saiu do estado de bloqueio: houve `EXPLAIN ANALYZE` em banco real, com credencial operacional e plano observado
- os hotspots antigos de P0.6, P1.2 e notificacoes continuam sem pressao real no volume atual
- o unico tuning adicional justificado nesta etapa foi em `fetch_training_program_record_list`
