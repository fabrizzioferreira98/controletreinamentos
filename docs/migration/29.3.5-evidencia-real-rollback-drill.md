# 29.3.5 - Evidencia real de `rollback_drill`

## Objetivo do bloco

Executar a trilha oficial de `rollback_drill` para o pacote release e impedir que rollback teorico, runbook parado ou checklist preenchido sejam aceitos como drill real.

## Mudancas aplicadas

- `ops/scripts/release/run_rollback_release_drill.py`
  - integra evidencias A/B/A de rollback ao pacote release;
  - exige headers antes/apos rollback/apos forward;
  - exige smokes antes/apos rollback/apos forward;
  - chama `ops/scripts/release/build_rollback_metadata.py` para gerar `rollback_runtime_ids.json`;
  - marca `checks.rollback_drill` como `PASS` somente quando smokes e runtime ids provam ida/volta real;
  - registra tentativa `FAIL` quando os insumos reais nao existem, sem fabricar evidencia.
- `tests/ops/test_release_rollback_drill_entrypoints.py`
  - cobre o caminho verde com smokes A/B/A e runtime ids distintos;
  - cobre a falha quando nao ha evidencia real de rollback.
- `tests/ops/test_build_rollback_metadata.py`
  - corrige a referencia do teste ao helper real em `ops/scripts/release/build_rollback_metadata.py`.

## Execucao operacional

- ambiente: homolog;
- pacote canonico: `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\`;
- trilha usada:
  - `ops/scripts/release/run_rollback_release_drill.py`;
  - helper de metadados: `ops/scripts/release/build_rollback_metadata.py`.

## Resultado da tentativa

- nao foram encontrados/anexados os seis insumos minimos do drill:
  - `header_before`;
  - `header_rollback`;
  - `header_forward`;
  - `smoke_before`;
  - `smoke_rollback`;
  - `smoke_forward`.
- a reversao minima A/B/A nao foi exercitada;
- `rollback_exercised=false`;
- `checks.rollback_drill.status = FAIL`.

## Artefato produzido

- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\rollback\rollback_drill_attempt.json`
  - comprova que a trilha oficial foi acionada;
  - registra identidade do pacote;
  - registra a ausencia dos insumos A/B/A obrigatorios;
  - impede aceite por rollback teorico/documental.

## Validacao

- `python -m pytest tests/ops/test_release_rollback_drill_entrypoints.py tests/ops/test_build_rollback_metadata.py tests/ops/test_release_alerts_external_entrypoints.py tests/ops/test_load_test_authenticated_entrypoints.py tests/ops/test_release_e2e_homolog_entrypoints.py -q` -> `14 passed`
- `harden_release_manifest.py --manifest <manifest>` -> recalculou 8 hashes
- `validate_operational_evidence.py --manifest <manifest> --require-hashes --require-alert-ack --require-rollback-runtime-ids --regression-checklist <checklist>` -> `FAIL`, incluindo:
  - `check_not_pass:rollback_drill:FAIL`
  - `rollback_drill:smoke_payloads_insufficient`

## Fechamento operacional do bloco

O bloco nao esta fechado como evidencia real de rollback. A integracao ao pacote e a protecao contra falso verde foram criadas e testadas, mas o ambiente/pacote atual nao possui execucao A/B/A com smokes e runtime ids. Sem esses insumos, qualquer `PASS` seria rollback teorico.

## Divida rebaixada ou remanescente

- aceite por runbook/checklist estatico -> eliminado pelo wrapper e pelo validador;
- integracao de evidencia A/B/A ao manifest/checklist -> eliminada quando os insumos reais existirem;
- execucao real de rollback minimo em homolog -> aberta, bloqueada por ausencia de trilha operacional A/B/A executada;
- runtime ids antes/rollback/forward -> aberto ate captura real;
- pacote release completo -> continua aberto tambem por `load_authenticated_30w`, `alerts_external_e2e` e demais checks pendentes.
