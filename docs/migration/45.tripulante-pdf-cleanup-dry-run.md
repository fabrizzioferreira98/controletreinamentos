# 45 - Dry-run de limpeza total de PDFs de tripulantes

## Bloco executado

Auditoria e dry-run da operacao destrutiva controlada para remover os PDFs vinculados a tripulantes.

## Superficie mapeada

- Tabela principal: `tripulante_arquivos_pdf`.
- Endpoints preservados:
  - `GET /api/v1/tripulantes/<tripulante_id>/files`
  - `POST /api/v1/tripulantes/<tripulante_id>/files`
  - `GET /api/v1/tripulantes/<tripulante_id>/files/<file_id>`
  - `DELETE /api/v1/tripulantes/<tripulante_id>/files/<file_id>`
- Storage produtivo: `C:\srv-data\controle-treinamentos\prod\uploads`.
- Caminho documental esperado atual: `tripulantes/tripulante-{id}/documentos/*.pdf`.
- Legado ainda presente em metadados: refs `fs:` antigas com slug do tripulante.
- Blobs em banco: ainda existem registros com `arquivo_pdf`.

## Evidencia gerada

- `docs\migration\evidence\45.tripulante-pdf-cleanup-dry-run.json`

## Resumo do dry-run

- Registros em `tripulante_arquivos_pdf`: `19`.
- Registros ativos: `13`.
- Ativos com blob disponivel: `1`.
- Ativos sem blob: `12`.
- Removidos logicamente: `6`.
- Removidos logicamente com blob ainda disponivel: `2`.
- Refs `fs:` legadas: `18`.
- Refs canonicas atuais: `1`.
- Blobs em banco: `2`.
- Arquivos fisicos existentes referenciados: `2`.
- Orfaos em storage documental de tripulantes: `0`.
- Bloqueios por ACL no dry-run: `0`.
- Legado irrecuperavel confirmado por evidencia anterior: `12`.

## Estrategia proposta

Para cumprir a intencao de "vida nova", a estrategia segura proposta e:

1. manter evidencia completa pre-operacao em JSON;
2. apagar fisicamente apenas os arquivos PDF comprovadamente existentes e legiveis no storage atual;
3. remover as linhas de `tripulante_arquivos_pdf` para que a biblioteca de documentos fique vazia;
4. deixar a trilha material da operacao em `docs/migration/evidence`;
5. validar apos a execucao que a tabela ficou sem registros e que os caminhos fisicos previstos nao existem mais.

Remocao apenas logica foi rejeitada como estrategia principal porque a UI ainda pode listar registros `removido`, o que nao atende plenamente ao objetivo de "vida nova".

## Estado

`aguardando_confirmacao_explicita`

A etapa destrutiva nao foi executada neste bloco.
