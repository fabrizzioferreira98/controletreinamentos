# 29.3.4.a - `alerts_external_e2e` com entrega externa real

## Objetivo do bloco

Transformar `alerts_external_e2e` de tentativa sem webhook em execucao HTTP real, com entrega capturada, ack preenchido, destino de escalonamento preenchido e pacote release atualizado.

## Trilha configurada

- webhook provisionado por arquivo fora do repositorio: `C:\srv\controle-treinamentos\env\alerts_external_2934a.webhook`;
- entrada oficial usada: `--webhook-url-file`;
- receiver HTTP independente da aplicacao: `127.0.0.1:18394`, executado como borda de captura controlada em homolog;
- captura material: `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\alerts\alerts_external_delivery_capture.json`.

## Mudancas aplicadas

- `ops/scripts/release/run_alerts_external_release.py`
  - adiciona `--delivery-capture-file`;
  - embute `external_delivery_evidence` no artefato oficial;
  - so marca `external_delivery_exercised=true` quando a entrega HTTP passa, ha ack e a captura recebeu `event=release_alert_drill`.
- `ops/scripts/release/alerts_external_drill.py`
  - passa a tolerar BOM em `--webhook-url-file` e variavel de ambiente, evitando falha opaca antes da emissao de JSON.
- `tests/ops/test_release_alerts_external_entrypoints.py`
  - cobre captura de entrega externa;
  - cobre arquivo de webhook com BOM.

## Resultado

- `alerts_external_e2e`: `PASS`;
- HTTP status: `202`;
- `external_delivery_exercised`: `true`;
- `acknowledged_by`: `oncall-homolog`;
- `escalation_target`: `pagerduty-primary`.

## Artefatos atualizados

- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\alerts\alerts_external_drill.json`
- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\alerts\alerts_external_delivery_capture.json`
- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json`
- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md`

## Validacao

- `python -m pytest tests/ops/test_release_alerts_external_entrypoints.py -q` -> `3 passed`.
- `run_alerts_external_release.py ... --webhook-url-file ... --delivery-capture-file ...` -> `success=true`, `status=202`, `external_delivery_exercised=true`.
- `harden_release_manifest.py --manifest <manifest>` -> `success=true`, `artifacts_hashed=8`.
- `validate_operational_evidence.py --manifest <manifest> --require-alert-ack ...` -> nao reporta mais issues de `alerts_external_e2e`.

## Fechamento

O bloco esta fechado. `alerts_external_e2e` deixou de depender de configuracao estatica ou tentativa sem webhook e passou a ter entrega HTTP real capturada no pacote.

## Divida remanescente

- O pacote release geral ainda permanece `NO-GO` por `load_authenticated_30w`, `rollback_drill` e checks pendentes fora deste bloco.
