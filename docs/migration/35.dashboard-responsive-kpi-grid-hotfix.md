# 35.dashboard - Hotfix de responsividade da grade KPI

## Bloco executado

`Dashboard responsive KPI grid hotfix`.

Decisao do bloco: `fechado`.

## Mudanca aplicada

Correção pontual na responsividade da camada KPI da dashboard para breakpoint intermediario (desktop denso/notebook):

- removido o efeito de empilhamento vertical exagerado dos cards KPI causado por heranca de `grid-column: span ...` de regras antigas;
- garantido que, no escopo da nova dashboard (`.dashboard-reference-target`), os cards KPI usem ocupacao automatica da grade definida por breakpoint.

Arquivo alterado:

- `frontend/src/app.css`

Trecho material:

- `.dashboard-reference-target .dashboard-stat-grid > .dashboard-kpi-card,`
- `.dashboard-reference-target .dashboard-stat-grid > .dashboard-kpi-card--critical { grid-column: auto; }`

## Prova material

- `frontend/src/app.css`: override local aplicado no bloco `35.dashboard-reference`.

## Teste / check focal

Executado:

```powershell
.\.venv\Scripts\python.exe -B frontend\scripts\build_frontend.py --env-file frontend\.env.example --output-dir runtime\tmp\dashboard-responsive-hotfix-check
```

Resultado: build frontend gerado sem erro.

Executado:

```powershell
.\.venv\Scripts\python.exe -B -m pytest -q -p no:cacheprovider tests\contract\test_frontend_responsive_dashboard_policy.py tests\contract\test_frontend_responsive_card_grid_policy.py tests\contract\test_frontend_responsive_master_detail_policy.py tests\contract\test_frontend_priority_page_visual_foundation.py
```

Resultado: `18 passed`.

## Regressao minima

- sem alteracao em sidebar;
- sem alteracao em topbar global;
- sem alteracao de contratos, rotas, loaders, guards, permissoes ou logica de negocio.

## Registro em migration

Este arquivo formaliza o hotfix pontual de responsividade da grade KPI apos evidencia visual de empilhamento indevido.

## Indexacao no README

- indexado em `docs/migration/README.md`;
- indexado em `frontend/README.md` na secao de refinamentos locais da dashboard.

## Criterio de fechamento

`fechado`: grade KPI da dashboard voltou a respeitar a hierarquia e a ocupacao de colunas esperada nos breakpoints intermediarios.

## Divida remanescente

- validacao visual final no ambiente publico continua dependente de confirmacao externa ao host local.
