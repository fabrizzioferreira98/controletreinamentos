# 32.3.11.a - Assinatura HMAC final real do candidato de entrega

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `32.3.11.a-assinatura-hmac-final-real` |
| objetivo | gerar a assinatura HMAC final do pacote do candidato `release_20260420_2932_210420` e eliminar os bloqueios de assinatura. |
| candidato final | `release_20260420_2932_210420` |
| ambiente | `homolog` |
| pacote externo | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420` |
| pre-condicao | `RELEASE_EVIDENCE_SIGNING_KEY` presente em fonte viva real e carregada no processo. |
| resultado da pre-condicao | nao atendida. |
| resultado do bloco | `BLOQUEADO` |
| `GO_OPERACIONAL_FINAL` emitido | nao |
| assinatura gerada | nao |

Esta etapa nao troca candidato, nao retuna produto, nao altera codigo de produto e nao declara GO.

## 2. Mudanca aplicada

Nao houve assinatura material do pacote, porque `RELEASE_EVIDENCE_SIGNING_KEY` nao esta presente em fonte viva real nem carregada no processo.

Foi aplicada apenas documentacao de migration, com validacao oficial assinada para registrar o bloqueio material.

| item | acao |
| --- | --- |
| manifest | preservado. |
| checklist | preservado. |
| pacote externo | preservado. |
| assinatura HMAC | nao gerada. |
| validacao assinada | executada com `--require-signature`. |

## 3. Prova material

### 3.1 Estado da chave no ambiente

Check redigido, sem imprimir valor:

| escopo | variavel | presente | comprimento |
| --- | --- | --- | ---: |
| `Process` | `RELEASE_EVIDENCE_SIGNING_KEY` | `false` | `0` |
| `User` | `RELEASE_EVIDENCE_SIGNING_KEY` | `false` | `0` |
| `Machine` | `RELEASE_EVIDENCE_SIGNING_KEY` | `false` | `0` |

Base documental imediata:

| etapa | leitura objetiva |
| --- | --- |
| `32.3.10.a` | `RELEASE_EVIDENCE_SIGNING_KEY` nao localizada em fonte viva real. |
| `32.3.10.b` | `RELEASE_EVIDENCE_SIGNING_KEY` nao carregada no processo; `missing_required_env` nao zerou. |

### 3.2 Trilha oficial aplicavel

| componente | papel |
| --- | --- |
| `ops\scripts\release\harden_release_manifest.py` | calcula `signature_hmac_sha256` no manifest quando `RELEASE_EVIDENCE_SIGNING_KEY` esta presente. |
| `ops\scripts\release\validate_operational_evidence.py --require-signature` | exige `signature_hmac_sha256` no manifest e valida HMAC com a chave do ambiente. |

O hardening oficial nao foi executado contra o pacote nesta etapa porque a chave real esta ausente. Executa-lo sem chave nao geraria HMAC final real e poderia alterar o manifest sem eliminar o bloqueio.

### 3.3 Artefatos de referencia preservados

| item | caminho | SHA-256 |
| --- | --- | --- |
| manifest final | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json` | `c108cba15eb1856e2c6e66236d161988d103d58d0dd26e67f912517a72752f1d` |
| checklist final | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md` | `5ba62af4180b3873bcfc8ba0807d695896c1fa3ebcb5f267e3c4738133eb0cbb` |

Leitura do manifest:

| campo | estado |
| --- | --- |
| `signature_hmac_sha256` | ausente |

## 4. Teste / check

### 4.1 Validacao oficial exigindo assinatura

Comando executado:

```powershell
.\.venv\Scripts\python.exe ops\scripts\release\validate_operational_evidence.py --manifest "C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json" --profile release --max-age-hours 24 --require-hashes --require-signature --require-alert-ack --require-rollback-runtime-ids --regression-checklist "C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md"
```

Resultado material:

```json
{
  "success": false,
  "manifest": "C:\\srv-data\\controle-treinamentos\\homolog\\evidence\\release_20260420_2932_210420\\release_evidence_manifest.json",
  "profile": "release",
  "checked_artifacts": 21,
  "issues": [
    "signature_missing",
    "signing_key_env_missing:RELEASE_EVIDENCE_SIGNING_KEY"
  ]
}
```

| campo | valor |
| --- | --- |
| exit code | `1` |
| `success` | `false` |
| `checked_artifacts` | `21` |
| bloqueio 1 | `signature_missing` |
| bloqueio 2 | `signing_key_env_missing:RELEASE_EVIDENCE_SIGNING_KEY` |

## 5. Criterio de fechamento

| criterio | status |
| --- | --- |
| `RELEASE_EVIDENCE_SIGNING_KEY` presente em fonte viva real | nao atendido |
| chave carregada materialmente no processo | nao atendido |
| assinatura HMAC final gerada pela trilha oficial | nao atendido |
| validacao oficial com `--require-signature` executada | atendido |
| `signature_missing` eliminado | nao atendido |
| `signing_key_env_missing` eliminado | nao atendido |
| candidato preservado | atendido |
| codigo de produto preservado | atendido |
| GO nao declarado | atendido |

Decisao do bloco: `BLOQUEADO`.

O criterio de `PASS` nao foi atendido, porque a validacao oficial assinada ainda acusa `signature_missing` e `signing_key_env_missing:RELEASE_EVIDENCE_SIGNING_KEY`.

## 6. Divida rebaixada ou remanescente

| item | status | justificativa | encaminhamento |
| --- | --- | --- | --- |
| trilha oficial de assinatura | rebaixada/identificada | `harden_release_manifest.py` e `validate_operational_evidence.py --require-signature` sao a trilha material aplicavel. | usar somente apos chave real estar presente. |
| validacao assinada | rebaixada/executada | validacao oficial rodou e confirmou os bloqueios atuais. | manter resultado como baseline material. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | remanescente bloqueante | ausente em fonte viva real e no processo. | provisionar chave real em fonte operacional viva de homolog. |
| `signature_hmac_sha256` | remanescente bloqueante | campo ausente no manifest. | gerar via hardening oficial somente com chave real. |
| assinatura final do pacote | remanescente bloqueante | validacao oficial segue vermelha. | rerodar assinatura e validacao apos provisionamento da chave. |
