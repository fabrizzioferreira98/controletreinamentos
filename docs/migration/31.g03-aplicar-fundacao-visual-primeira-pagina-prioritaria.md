# 31.G03 - Aplicar fundacao visual na primeira pagina prioritaria

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `G03-aplicar-fundacao-visual-primeira-pagina-prioritaria` |
| data | 2026-04-22 |
| superficie prioritaria | `dashboard/tripulantes` |
| objetivo | aplicar tokens e primitives de `shared/ui` na primeira superficie funcional prioritaria, sem drift funcional e sem redesign amplo. |
| escopo | dashboard, lista de tripulantes, headers, paineis, feedback, filtros, tabela, paginacao e CSS escopado. |
| fora do escopo | treinamentos/relatorios, SSR, runtime/deploy, design system completo, motion sofisticado e componentizacao ornamental. |
| decisao | `fechado` |

## 2. Leitura visual inicial

| achado | classificacao |
| --- | --- |
| `pages-dashboard-tripulantes.js` ja era wrapper compativel e fino | base arquitetural preservada |
| `features/dashboard/page.js` concentrava superficie viva de dashboard, calendario, agenda e fila critica | alvo principal do G03 |
| `features/tripulantes/list-page.js` concentrava lista/filtros/paginacao de tripulantes | alvo complementar do G03 |
| CSS local ja tinha camada operacional para dashboard/tripulantes, mas ainda pouco conectada a `shared/ui` | `divida_visual_controlada` reduzida |
| treinamentos/relatorios continuam em bloco proprio futuro | `nao_redesign` preservado |

## 3. Mudanca aplicada

| arquivo | mudanca |
| --- | --- |
| `frontend/src/features/dashboard/page.js` | adiciona `dashboard-page-shell`, `priority-page-surface`, `ui-stack`, `priority-page-header`, `ui-surface`, `ui-feedback`, `ui-cluster` e cards `ui-surface` em paineis, agenda e resumo. |
| `frontend/src/features/tripulantes/list-page.js` | adiciona `tripulantes-page-shell`, `priority-page-surface`, `priority-page-header`, `tripulantes-list-panel`, `ui-surface`, `ui-stack`, `ui-stack-sm`, tabela escopada e paginacao `ui-cluster`. |
| `frontend/src/app.css` | adiciona bloco G03 escopado a dashboard/tripulantes usando tokens de spacing, surface, radius, shadow, estados e transicao. |
| `tests/contract/test_frontend_priority_page_visual_foundation.py` | adiciona check focal de adocao visual e preservacao de contratos funcionais. |
| `docs/architecture/FRONTEND_ARCHITECTURE.md` | documenta a adocao visual da primeira superficie prioritaria. |
| `frontend/README.md` | registra a politica de priority page foundation. |

## 4. Prova material

| prova | resultado |
| --- | --- |
| dashboard | contem `dashboard-page-shell priority-page-surface ui-stack`, `priority-page-header ui-surface`, `state-note ui-feedback`, paineis `ui-surface`, agenda/cards `ui-surface`. |
| tripulantes | contem `tripulantes-page-shell priority-page-surface ui-stack`, `tripulantes-list-panel ui-surface ui-stack`, filtros `ui-stack-sm`, tabela escopada e paginacao `ui-cluster`. |
| CSS G03 | contem `.priority-page-surface`, `.dashboard-page-shell`, `.tripulantes-page-shell`, `var(--space-panel-gap)`, `var(--radius-surface)`, `var(--shadow-surface)`, `var(--color-state-default-surface)` e `var(--transition-state)`. |
| contratos preservados | endpoints de dashboard, filtros/paginacao de tripulantes, exclusao, calendario, links SPA/backend e wrappers permanecem. |

## 5. Checks

Checks executados com `PYTHONDONTWRITEBYTECODE=1`:

| check | resultado |
| --- | --- |
| `tests/contract/test_frontend_priority_page_visual_foundation.py` | PASS |
| `tests/contract/test_frontend_feature_boundaries.py` | PASS |
| `tests/contract/test_frontend_page_module_boundaries.py` | PASS |
| `tests/contract/test_frontend_ux_functional_guards.py` | PASS |
| rodada focal inicial | `25 passed` |

Validacao final do bloco:

| check | resultado |
| --- | --- |
| rodada focal ampliada final | `68 passed` |
| build frontend com `--output-dir <temp fora do checkout>` | PASS em `C:\Users\FABRIZ~1.ARA\AppData\Local\Temp\controle-treinamentos-g03-7f7540846eda4b45aae5f51250bac43a` |
| CSS final do build | contem `G03: priority dashboard/tripulantes`, `.priority-page-surface`, `.dashboard-page-shell`, `.tripulantes-page-shell`, `var(--shadow-surface)` e `var(--color-state-default-surface)` |
| hygiene validator final | `success=true`, sem violacoes |

O validator de hygiene ficou vermelho durante a rodada final somente por `__pycache__` gerado/local fora das excecoes. Foram removidos apenas residuos locais de bytecode fora de `.venv/` e `archive/`: `32` diretorios `__pycache__` e `0` arquivos `.pyc/.pyo` soltos. A validacao final com `python -B` ficou verde.

## 6. Criterio de fechamento

| criterio | status |
| --- | --- |
| fundacao visual aplicada materialmente em dashboard/tripulantes | fechado |
| rotas/loaders/guards preservados | fechado |
| services/state/compat nao reabertos | fechado |
| treinamentos/relatorios fora do bloco | fechado |
| checks focais verdes | fechado |

Decisao objetiva do bloco: `fechado`.

## 7. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| CSS historico de dashboard/tripulantes ainda existe | `divida_visual_controlada` | continuar conversao por blocos incrementais de pagina/feature. |
| formulario detalhado de tripulante ainda tem familias visuais proprias | remanescente planejado | tratar em bloco especifico se virar prioridade. |
| treinamentos/relatorios | fora do G03 | aplicar fundacao visual em bloco posterior. |
| redesign amplo/design system | fora do G03 | nao autorizado por este bloco. |
