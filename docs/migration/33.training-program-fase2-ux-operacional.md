# 33 - Treinamentos por tripulante: Fase 2 UX operacional local

## Bloco

Refino local da superficie SPA `#/treinamentos` para acompanhamento e regularizacao de treinamentos por tripulante.

## Mudanca

- Resumo operacional calculado no frontend a partir da resposta ja carregada de `GET /api/v1/treinamentos-tripulantes`.
- Indicadores locais: total, vencidos, a vencer, regulares, sem informacao e sem anexo.
- Agrupamento visual de registros por tripulante ou tipo, sem novo filtro e sem novo parametro enviado ao backend.
- Evidencia documental mais legivel na coluna de anexos, usando `total_anexos`.
- Acao de regularizacao preservando o detalhe vivo `#/treinamentos/:id`.

## Contrato preservado

- Nenhuma entidade nova.
- Nenhuma rota nova.
- Nenhum endpoint novo.
- Nenhuma chamada para missoes, pernoites, produtividade, `compat/` ou SSR legacy.
- Anexos permanecem no fluxo `treinamentos/<id>/attachments`.

## Prova focal

```powershell
.venv\Scripts\python.exe -m pytest tests/contract/test_frontend_training_reports_visual_foundation.py tests/contract/test_frontend_table_form_patterns.py tests/contract/test_frontend_feature_boundaries.py tests/contract/test_frontend_ux_functional_guards.py
```

Resultado registrado no bloco: `25 passed`.

## Decisao

`fechado` como Fase 2 local, incremental e compativel com a base viva atual.
