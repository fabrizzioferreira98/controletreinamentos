# 32.resume.1 - Materializacao transitoria do env real do gate operacional final

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `32.resume.1-materializacao-env-real-gate` |
| objetivo | carregar materialmente no processo do gate as variaveis reais exigidas e provar que o preflight deixa de acusar `missing_required_env`. |
| candidato de referencia | `release_20260420_2932_210420` |
| ambiente | `homolog` |
| gate oficial | `ops\scripts\release\run_release_strict.py` |
| preflight oficial usado | `ops.scripts.release.run_release_strict._required_env()` |
| resultado do bloco | `BLOQUEADO` |
| `GO_OPERACIONAL_FINAL` emitido | nao |
| codigo de produto alterado | nao |

Esta etapa nao altera candidato, codigo de produto, manifest, checklist ou pacote externo. Nenhum valor fake, template, backup, exemplo, producao fora de escopo ou segredo inventado foi usado.

## 2. Mudanca aplicada

Foi feita apenas materializacao transitoria no processo do preflight, sem persistir segredo:

| item | acao |
| --- | --- |
| `C:\srv\controle-treinamentos\env\hml.env` | carregado no processo transitorio do preflight. |
| `DATABASE_URL` | presente no `hml.env` vivo e carregada no processo. |
| `E2E_DATABASE_URL` | nao materializada; valor real nao existe na fonte viva local carregada. |
| `E2E_LOGIN` | nao materializada; valor real nao existe na fonte viva local carregada. |
| `E2E_PASSWORD` | nao materializada; valor real nao existe na fonte viva local carregada. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | nao materializada; valor real nao existe na fonte viva local carregada. |
| `.github\workflows\release-strict-gate.yml` | identificado apenas como ponteiro indireto para `${{ secrets.* }}`, nao como valor material carregavel neste processo. |
| `.github\workflows\promotion-ci.yml` | identificado apenas como ponteiro indireto para `${{ secrets.* }}`, nao como valor material carregavel neste processo. |

Os ponteiros de workflow nao foram usados como fonte de valor porque nao expõem nem materializam segredo no processo local do gate.

## 3. Prova material

### 3.1 Fontes verificadas sem expor segredo

| fonte | tipo | prova | leitura objetiva |
| --- | --- | --- | --- |
| `C:\srv\controle-treinamentos\env\hml.env` | env vivo homolog local | SHA-256 `C8DF9499BFB524426A40C2B53B7481DDA5CF2BA253CA485C7636F94FC5DF3381` | contem `DATABASE_URL`; nao contem as quatro variaveis E2E/assinatura. |
| `Process`, `User`, `Machine` | ambiente ativo Windows | consulta por `[Environment]::GetEnvironmentVariable()` | as quatro variaveis E2E/assinatura ausentes nos tres escopos. |
| servico Windows `CT-App-Hml` | servico homolog ativo | `Running`, `Auto`, binario em `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.exe` | service XML ativo nao contem os nomes das quatro variaveis. |
| `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.xml` | service XML ativo homolog | SHA-256 `3EE9F7673C79E47835F0737589E8C363C62A786C62374EBEF72A66D98F13B001` | nomes das quatro variaveis nao encontrados. |
| Task Scheduler / `schtasks` | tasks ativas Windows | consulta elevada sem linhas relevantes | nenhuma task relevante fornecendo as quatro variaveis. |
| `.github\workflows\release-strict-gate.yml` | ponteiro de CI | SHA-256 `96EA5D99D6B384064C4F62096C4BE38E73530D8AF3B4B7E05B30B28FD799EA30` | contem referencias `${{ secrets.* }}`, nao valores materiais. |
| `.github\workflows\promotion-ci.yml` | ponteiro de CI | SHA-256 `A07C8CCCACFE70ACDD6D7D0D7B0090CEF217A76567EF76793AF9BDA806FA9063` | contem referencias `${{ secrets.* }}`, nao valores materiais. |

### 3.2 Preflight oficial redigido

Foi executada a funcao oficial de pre-condicao do gate:

```text
from ops.scripts.release.run_release_strict import _required_env
```

Saida material redigida, sem valores sensiveis:

```json
{
  "source_env_loaded": "C:\\srv\\controle-treinamentos\\env\\hml.env",
  "gate_preflight_ok": false,
  "missing_required_env": [
    "E2E_DATABASE_URL",
    "E2E_LOGIN",
    "E2E_PASSWORD",
    "RELEASE_EVIDENCE_SIGNING_KEY"
  ],
  "checked": {
    "DATABASE_URL": {
      "present": true,
      "length": 81
    },
    "E2E_DATABASE_URL": {
      "present": false,
      "length": 0
    },
    "E2E_LOGIN": {
      "present": false,
      "length": 0
    },
    "E2E_PASSWORD": {
      "present": false,
      "length": 0
    },
    "RELEASE_EVIDENCE_SIGNING_KEY": {
      "present": false,
      "length": 0
    }
  }
}
```

### 3.3 Leitura objetiva

| variavel | present | length | origem material carregada |
| --- | --- | ---: | --- |
| `DATABASE_URL` | `true` | `81` | `C:\srv\controle-treinamentos\env\hml.env` |
| `E2E_DATABASE_URL` | `false` | `0` | nenhuma fonte viva real carregavel no processo local |
| `E2E_LOGIN` | `false` | `0` | nenhuma fonte viva real carregavel no processo local |
| `E2E_PASSWORD` | `false` | `0` | nenhuma fonte viva real carregavel no processo local |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `false` | `0` | nenhuma fonte viva real carregavel no processo local |

## 4. Teste / check

| check | resultado |
| --- | --- |
| preflight oficial do `run_release_strict.py` executado | sim |
| funcao oficial usada | `_required_env()` |
| fonte viva homolog local carregada | `C:\srv\controle-treinamentos\env\hml.env` |
| valores sensiveis impressos | nao |
| ponteiro de CI tratado como segredo material | nao |
| `missing_required_env` zerou | nao |
| variaveis ausentes reportadas | `E2E_DATABASE_URL`, `E2E_LOGIN`, `E2E_PASSWORD`, `RELEASE_EVIDENCE_SIGNING_KEY` |
| resultado | `BLOQUEADO` |

O resultado nao e `FAIL funcional`, porque nenhum check funcional do produto foi executado ou reprovado. O bloqueio permanece em pre-condicao material do gate.

## 5. Criterio de fechamento

| criterio | status |
| --- | --- |
| usar apenas fonte viva real de homolog | atendido para `hml.env`; nao ha fonte viva local carregavel para as quatro variaveis externas. |
| nao usar template, backup, exemplo, producao fora de escopo ou valor fake | atendido |
| nao alterar codigo de produto | atendido |
| nao emitir GO | atendido |
| comprovar `present=true`, `length>0` para as quatro variaveis | nao atendido |
| preflight oficial sem `missing_required_env` | nao atendido |

Decisao do bloco: `BLOQUEADO`.

O criterio de `PASS` nao foi atendido porque o preflight oficial ainda acusa `missing_required_env` para as quatro variaveis exigidas pela retomada.

## 6. Divida rebaixada ou remanescente

| item | status | justificativa | encaminhamento |
| --- | --- | --- | --- |
| materializacao transitoria de `DATABASE_URL` | rebaixada/fechada | variavel presente no `hml.env` vivo e carregada no processo. | manter fonte viva HML para gate. |
| `E2E_DATABASE_URL` | remanescente bloqueante | ausente no processo e sem valor material em fonte viva local carregavel. | provisionar fonte viva real homolog carregavel pelo gate. |
| `E2E_LOGIN` | remanescente bloqueante | ausente no processo e sem valor material em fonte viva local carregavel. | provisionar usuario real de E2E homolog. |
| `E2E_PASSWORD` | remanescente bloqueante | ausente no processo e sem valor material em fonte viva local carregavel. | provisionar segredo real correspondente sem expor valor. |
| `RELEASE_EVIDENCE_SIGNING_KEY` | remanescente bloqueante | ausente no processo e sem valor material em fonte viva local carregavel. | provisionar chave HMAC real homolog. |
| ponteiros `${{ secrets.* }}` dos workflows | fora do aceite desta etapa | indicam dependencia de secret store de CI, mas nao materializam valores no processo local. | usar somente se houver execucao em ambiente onde os secrets sejam realmente injetados e redigidos. |
| gate operacional final | remanescente bloqueante | `_required_env()` segue retornando `missing_required_env`. | rerodar somente apos as quatro variaveis existirem no processo com `present=true`, `length>0`. |
