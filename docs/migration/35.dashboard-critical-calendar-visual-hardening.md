# 35.dashboard - Hardening visual de Fila critica e Calendario

## 1. BLOCO EXECUTADO

`Dashboard critical + calendar visual hardening`.

Decisao do bloco: `fechado`.

## 2. MUDANCA APLICADA

Ajuste local e seguro de composicao visual em `#/dashboard`, somente na superficie `Fila critica` + `Calendario de vencimentos`:

- reducao de bordas agressivas e refinamento de raio/sombra dos dois paineis;
- recalibragem de hierarquia tipografica para nao disputar protagonismo;
- melhoria da densidade e acabamento da grade mensal do calendario;
- refino do bloco `Proximos vencimentos` para conter nomes longos e evitar overflow;
- consolidacao no fonte oficial (`frontend/src/app.css`) do hotfix de responsividade da `Base monitorada`.

Sem alteracao em:

- contratos, dados, rotas, loaders, guards, permissoes e logica de negocio;
- sidebar e topbar global;
- estrutura de renderizacao em `features/dashboard/page.js`.

## 3. PROVA MATERIAL

Arquivo alterado:

- `frontend/src/app.css`.

Seletores com mudanca material:

- `.dashboard-reference-target .dashboard-core-grid`;
- `.dashboard-reference-target .dashboard-critical-panel.ui-surface`;
- `.dashboard-reference-target .dashboard-calendar-panel.ui-surface`;
- `.dashboard-reference-target .dashboard-critical-open-bottom`;
- `.dashboard-reference-target .dashboard-calendar-head-copy h2`;
- `.dashboard-reference-target .dashboard-calendar-shell.ui-master-pane`;
- `.dashboard-reference-target .dashboard-calendar-day*`;
- `.dashboard-reference-target .dashboard-calendar-support-block*`;
- `.dashboard-reference-target .dashboard-calendar-support-item*`;
- `.dashboard-reference-target .dashboard-monitor-list`;
- `.dashboard-reference-target .dashboard-monitor-item*`;
- media queries em `1360px`, `1180px` e `720px`.

## 4. TESTE / CHECK FOCAL

Executado:

```powershell
.\.venv\Scripts\python.exe frontend\scripts\build_frontend.py --env-file frontend\.env.example --output-dir frontend\runtime\tmp\dashboard-visual-hotfix-check
```

Resultado: build frontend concluido com sucesso no output temporario.

Observacao de ambiente:

- o build padrao para `frontend/dist` falhou por lock de arquivo (`PermissionError` em `frontend\dist\app\bootstrap.js`), sem impacto no codigo fonte alterado.

## 5. REGRESSAO MINIMA

- nenhum ajuste em `frontend/src/features/dashboard/page.js`;
- nenhuma alteracao em shell global (`frontend/src/shell/*`);
- nenhuma alteracao em backend/API;
- nenhuma alteracao de contrato de dados, endpoint ou navegacao.

## 6. REGISTRO EM MIGRATION

Este arquivo formaliza a rodada de hardening visual focada na composicao `Fila critica` + `Calendario`, com correcoes responsivas e de legibilidade sem drift estrutural.

## 7. INDEXACAO NO README

- `docs/migration/README.md` atualizado com este registro;
- `frontend/README.md` atualizado com referencia desta rodada.

## 8. CRITERIO DE FECHAMENTO

`fechado`: a composicao visual ficou mais madura, com hierarquia e contencao de conteudo consistentes, mantendo contrato funcional intacto.

## 9. DIVIDA REBAIXADA OU REMANESCENTE

Rebaixada:

- borda pesada e acabamento visual agressivo nos paineis centrais;
- excesso de destaque do calendario frente a fila critica;
- estouro/ruido de `Proximos vencimentos` com nomes longos;
- risco de regressao da `Base monitorada` na proxima publicacao.

Remanescente:

- validacao final em runtime produtivo ainda depende de publish no host `C:\srv\controle-treinamentos\frontend\{prod,hml}` e smoke pos-deploy.
