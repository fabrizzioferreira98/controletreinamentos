# 31.4.1 - Archive e snapshots de risco medio/alto

## 1. Bloco executado

Bloco: `31.4.1-archive-snapshots-risco-medio-alto`.

Escopo auditado:

- `archive/repo-snapshots`
- `archive/temp-backups`
- `archive/old-wrappers`
- `archive/local-artifacts`
- `archive/temp-backups/nested-repo-copy-20260415`

## 2. Mudanca aplicada

Nenhum item de `archive/` foi apagado, movido ou promovido a fonte viva.

Declaracao formal:

- `archive/` e historico congelado.
- `archive/temp-backups/nested-repo-copy-20260415/` nao e segunda raiz viva.
- historico legitimo nao sera apagado por conveniencia.
- remocao futura depende de perda objetiva de valor de auditoria, ausencia de referencia e, quando aplicavel, ausencia de consumidor externo.

Classificacao por grupo:

| grupo | inventario real | classificacao 31.4.1 | decisao |
| --- | --- | --- | --- |
| `archive/repo-snapshots/` | 6 snapshots frontend e `README.md` | historico legitimo congelado | manter; nao e build oficial nem fonte viva |
| `archive/temp-backups/` | 3 backups, `README.md` e nested repo copy | historico legitimo congelado | manter; nao volta para raiz, `backend/` ou docs vivas |
| `archive/temp-backups/nested-repo-copy-20260415/` | copia aninhada dentro do archive | copia aninhada congelada | manter; nao e root oficial nem segunda arvore do produto |
| `archive/old-wrappers/` | 4 wrappers antigos e `README.md` | historico legitimo congelado | manter; nao e compat vivo nem comando oficial |
| `archive/local-artifacts/` | diretorio vazio nesta medicao | area de residuo local controlado | manter vazio; se reaparecer residuo, tratar por bloco de artefatos locais |

## 3. Prova material

Inventario executado:

```powershell
Get-ChildItem -Path archive -Force
Get-ChildItem -Path archive\repo-snapshots -Force
Get-ChildItem -Path archive\temp-backups -Force
Get-ChildItem -Path archive\old-wrappers -Force
Get-ChildItem -Path archive\local-artifacts -Force
```

Resultado material:

- raiz de `archive/`: `local-artifacts`, `MANIFEST.md`, `old-wrappers`, `README.md`, `repo-snapshots`, `temp-backups`;
- `archive/repo-snapshots/`: 6 snapshots frontend e `README.md`;
- `archive/temp-backups/`: `nested-repo-copy-20260415/`, `root-wrapper-docs-backup-20260408-1635/`, `src-app-backup-20260408-154654/`, `src-app-__init__-backup-20260408-1610.py` e `README.md`;
- `archive/old-wrappers/`: 4 wrappers antigos e `README.md`;
- `archive/local-artifacts/`: vazio nesta medicao;
- `Test-Path controle-treinamentos`: `False`;
- `Test-Path archive\temp-backups\nested-repo-copy-20260415`: `True`.

Coerencia com `archive/MANIFEST.md`:

- os 6 snapshots frontend estao registrados;
- os 4 itens de `temp-backups` estao registrados;
- os 4 wrappers antigos estao registrados;
- a copia aninhada esta registrada como `copia aninhada`;
- `local-artifacts/.DS_Store` permanece registrado como item removido em `31.4.2`, nao como historico legitimo vivo.

## 4. Teste / check

Checks executados:

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B -m pytest tests\ops\test_repo_hygiene_validation.py -q -p no:cacheprovider
$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B ops\scripts\repo\validate_repo_hygiene.py --root .
```

Resultados:

- `tests/ops/test_repo_hygiene_validation.py`: `11 passed`;
- primeira medicao de `validate_repo_hygiene.py --root .`: vermelho por `__pycache__` gerado pela execucao de testes;
- cleanup restrito: 32 diretorios `__pycache__` fora de `.venv/` e `archive/` removidos apos verificacao de paths;
- segunda medicao de `validate_repo_hygiene.py --root .`: `success=true`, `violations=[]`;
- contagem final de `__pycache__` fora de `.venv/` e `archive/`: `0`.

Leitura topologica:

- a raiz `controle-treinamentos/` aninhada esta ausente;
- a copia aninhada existe apenas dentro de `archive/temp-backups/`;
- `archive/` nao participa de build, teste, release, restore oficial ou codigo vivo.

## 5. Criterio de fechamento

Fechado.

Cada grupo auditado tem decisao formal de congelamento ou controle. Nenhum item historico foi removido por conveniencia e a copia aninhada nao foi tratada como raiz oficial.

## 6. Divida rebaixada ou remanescente

| item | status |
| --- | --- |
| `archive/repo-snapshots/` | remanescente congelado ate perda objetiva de valor de auditoria. |
| `archive/temp-backups/` | remanescente congelado; remocao futura exige ausencia de valor historico e referencia. |
| `archive/temp-backups/nested-repo-copy-20260415/` | remanescente alto risco; nao e segunda raiz, mas depende de auditoria externa antes de qualquer remocao. |
| `archive/old-wrappers/` | remanescente congelado para auditoria de aliases antigos. |
| `archive/local-artifacts/` | vazio nesta medicao; reaparecimento vira artefato local, nao historico legitimo. |
