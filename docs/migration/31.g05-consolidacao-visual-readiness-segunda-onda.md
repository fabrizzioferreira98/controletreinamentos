# 31.G05 - Consolidacao visual e readiness da segunda onda

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `G05-consolidacao-visual-readiness-segunda-onda` |
| data | 2026-04-22 |
| objetivo | auditar e consolidar a segunda onda visual para decidir readiness da proxima fase. |
| superficies auditadas | `shared/ui`, shell/layout, dashboard/tripulantes, treinamentos/relatorios e CSS escopado G02-G04. |
| fora do escopo | redesign completo, terceira onda visual, SSR, runtime/deploy, design system inflado e novas paginas. |
| decisao | `fechado` |
| readiness | `pronta_para_proxima_fase_visual` |

## 2. Auditoria material

| area | achado | classificacao |
| --- | --- | --- |
| `frontend/src/shared/ui/` | contem `tokens.css`, `primitives.css` e README; primitives seguem neutros de dominio. | `consolidado` |
| shell/layout | `render-shell.js` usa `ui-app-frame`, `ui-inverse-surface`, `ui-navigation-list`, `ui-sticky-surface`, `ui-cluster` e `ui-content-region`. | `adocao_visual_controlada` |
| dashboard/tripulantes | G03 usa `priority-page-surface`, `priority-page-header`, `ui-surface`, `ui-stack`, `ui-feedback`, `ui-cluster` e CSS escopado. | `adocao_visual_controlada` |
| treinamentos/relatorios | G04 usa `priority-page-surface`, `ui-surface`, `ui-stack`, `ui-feedback`, `ui-stack-sm`, tabelas escopadas e CSS escopado. | `adocao_visual_controlada` |
| `frontend/src/app.css` | blocos G02/G03/G04 usam tokens de spacing, surface, radius, shadow, estados e transicao. | `consolidado` |
| hardcodes historicos em CSS | ainda existem valores locais em familias antigas de login, relatorios densos, formularios e operacao. | `divida_visual_controlada` |
| `backend/src/controle_treinamentos/static/styles.css` | continua como CSS legado/backend concatenado antes de `shared/ui` e `app.css`. | `baseline_fora_do_escopo` |

## 3. Verificacao de fronteiras visuais

| fronteira | resultado |
| --- | --- |
| `shared/ui` como base comum | real: tokens/primitives sao usados por shell e paginas prioritarias. |
| primitives neutros de dominio | preservado: `primitives.css` nao conhece dashboard, tripulantes, treinamentos, relatorios, API, router ou renderizacao JS. |
| convergencia shell/paginas | real: shell usa primitives estruturais; paginas usam `priority-page-surface`, `ui-surface`, `ui-stack`, `ui-feedback` e tokens. |
| acoplamento a uma pagina especifica | controlado: estilos de dominio seguem escopados em G03/G04 dentro de `app.css`, nao em `shared/ui`. |
| readiness da proxima fase | liberada com divida visual controlada e sem bloquear por hardcode historico nao tratado. |

## 4. Mudanca aplicada

| arquivo | mudanca |
| --- | --- |
| `tests/contract/test_frontend_visual_readiness_second_wave.py` | adiciona check de consolidacao G05 para convergencia da segunda onda, CSS escopado/tokenizado e readiness documentada com divida residual. |
| `docs/architecture/FRONTEND_ARCHITECTURE.md` | adiciona secao `Readiness visual apos G05` com decisao `pronta_para_proxima_fase_visual`. |
| `frontend/README.md` | registra a consolidacao da segunda onda visual e a politica de expansao futura. |
| `docs/migration/README.md` | indexa o registro G05. |

## 5. Checks

Checks executados com `PYTHONDONTWRITEBYTECODE=1`:

| check | resultado |
| --- | --- |
| `tests/contract/test_frontend_visual_readiness_second_wave.py` | PASS |
| checks visuais/arquiteturais G01-G04 | `17 passed` |
| rodada ampliada final de arquitetura/visual frontend | `75 passed` |
| build frontend com `--output-dir <temp fora do checkout>` | PASS em `C:\Users\FABRIZ~1.ARA\AppData\Local\Temp\controle-treinamentos-g05-2f2ccae6dbf24f8484aeb7bdc4db54b0` |
| CSS final do build | contem `G02: shell layout`, `G03: priority dashboard`, `G04: training/reports`, `.app-shell.ui-app-frame`, `.dashboard-page-shell`, `.tripulantes-page-shell`, `.training-reports-page-shell`, `var(--space-panel-gap)` e `var(--shadow-surface)` |
| hygiene validator final | `success=true`, sem violacoes |

O validator de hygiene ficou vermelho durante a rodada final somente por `__pycache__` gerado/local fora das excecoes. Foram removidos apenas residuos locais de bytecode fora de `.venv/` e `archive/`: `32` diretorios `__pycache__` e `0` arquivos `.pyc/.pyo` soltos. A validacao final com `python -B` ficou verde.

## 6. Criterio de fechamento

| criterio | status |
| --- | --- |
| segunda onda visual auditada materialmente | fechado |
| fronteiras `shared/ui`, shell e paginas prioritarias verificadas | fechado |
| divida visual residual explicitada | fechado |
| decisao de readiness registrada | fechado |
| nenhuma nova superficie redesenhada | fechado |

Decisao objetiva do bloco: `fechado`.

## 7. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| CSS historico com valores locais | `divida_visual_controlada` | reduzir por blocos futuros de componentes/telas, nao por limpeza estetica. |
| tabelas densas | remanescente planejado | candidata natural para proxima fase visual se houver bloco especifico. |
| formularios profundos | remanescente planejado | candidata natural para proxima fase visual se houver bloco especifico. |
| static CSS backend | `baseline_fora_do_escopo` | desacoplamento exige frente propria de build/static. |
| redesign completo | fora do G05 | nao autorizado por readiness de segunda onda. |
