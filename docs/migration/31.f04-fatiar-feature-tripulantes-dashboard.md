# 31.F04 - Fatiar feature tripulantes/dashboard

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `F04-fatiar-feature-tripulantes-dashboard` |
| data | 2026-04-22 |
| objetivo | reduzir `frontend/src/pages-dashboard-tripulantes.js` por dominio interno, preservando exports, rotas e comportamento funcional. |
| escopo | `pages-dashboard-tripulantes.js`, novos owners em `frontend/src/features/`, checks arquiteturais, docs vivas, build frontend e hygiene. |
| fora do escopo | redesign, F05 treinamentos/relatorios, F06 compat/static adapters, migracao SSR, mudanca de shell, rota, loader ou permissoes. |
| decisao | `fechado` |

## 2. Leitura da pagina antes do fatiamento

Responsabilidades antes concentradas em `frontend/src/pages-dashboard-tripulantes.js`:

| responsabilidade | conteudo antes |
| --- | --- |
| dashboard | loading, cards de alerta, calendario, fila critica, adapters de payload e tratamento parcial de erro. |
| tripulantes lista | filtros, tabela, paginacao, delete, avatar/foto, links de relatorio, produtividade, WhatsApp e file. |
| relatorio individual | entrada `renderRelatorioIndividualPage()` delegando lista em modo `report`. |
| tripulante formulario | detalhe/cadastro, validacao, foto, upload/remocao de foto, documentos PDF e delete. |
| adapters | validacao de payloads de dashboard, lista, options e files. |
| DOM/render | markup de pagina, wiring de calendario, filtros densos, feedback inline e uploads. |
| exports roteados | `renderDashboardPage`, `renderTripulantesListPage`, `renderRelatorioIndividualPage`, `renderTripulanteFormPage`. |

## 3. Mudanca aplicada

`frontend/src/pages-dashboard-tripulantes.js` virou wrapper compativel temporario para o loader atual. Ele preserva os mesmos quatro exports roteados e delega para owners de feature:

| modulo | responsabilidade |
| --- | --- |
| `frontend/src/features/dashboard/page.js` | dashboard, adapters do dashboard, calendario, fila critica e render da rota `#/dashboard`. |
| `frontend/src/features/tripulantes/avatar.js` | foto/avatar/fallback visual de tripulante. |
| `frontend/src/features/tripulantes/data-adapters.js` | adapters minimos de lista/options/files para tripulantes. |
| `frontend/src/features/tripulantes/list-page.js` | lista de tripulantes, filtros, paginacao e acoes de lista. |
| `frontend/src/features/tripulantes/form-page.js` | cadastro/detalhe, validacao, foto e documentos PDF. |
| `frontend/src/features/relatorio-individual/page.js` | entrada do relatorio individual usando lista em modo `report`. |

O bloco nao alterou `app/route-registry.js`, shell, permissoes, rotas ou links.

## 4. Prova material

Tamanho antes:

| arquivo | bytes |
| --- | ---: |
| `frontend/src/pages-dashboard-tripulantes.js` | `81083` |

Tamanho depois:

| arquivo | linhas | bytes |
| --- | ---: | ---: |
| `frontend/src/pages-dashboard-tripulantes.js` | `20` | `977` |
| `frontend/src/features/dashboard/page.js` | `483` | `21627` |
| `frontend/src/features/tripulantes/avatar.js` | `81` | `3217` |
| `frontend/src/features/tripulantes/data-adapters.js` | `37` | `1330` |
| `frontend/src/features/tripulantes/list-page.js` | `293` | `15553` |
| `frontend/src/features/tripulantes/form-page.js` | `732` | `40702` |
| `frontend/src/features/relatorio-individual/page.js` | `5` | `194` |

Exports preservados no wrapper:

```text
renderDashboardPage
renderTripulantesListPage
renderRelatorioIndividualPage
renderTripulanteFormPage
```

Build fora do checkout:

```powershell
.venv\Scripts\python.exe frontend\scripts\build_frontend.py --env-file frontend\.env.example --output-dir C:\Users\FABRIZ~1.ARA\AppData\Local\Temp\controle-treinamentos-f04-5c089744af6a441eb75cc01cb53b4c85
```

Arquivos `features/` materializados no artefato temporario:

```text
features/dashboard/page.js
features/relatorio-individual/page.js
features/tripulantes/avatar.js
features/tripulantes/data-adapters.js
features/tripulantes/form-page.js
features/tripulantes/list-page.js
```

## 5. Checks

Checks executados com `PYTHONDONTWRITEBYTECODE=1`:

| check | resultado |
| --- | --- |
| `tests/contract/test_frontend_feature_boundaries.py` | PASS |
| `tests/contract/test_frontend_page_module_boundaries.py` | PASS |
| `tests/contract/test_frontend_ux_functional_guards.py` | PASS |
| `tests/contract/test_frontend_shell_navigation_boundaries.py` | PASS |
| `tests/contract/test_frontend_shell_architecture_boundaries.py` | PASS |
| `tests/contract/test_frontend_compat_redirects.py` | PASS |
| `tests/contract/test_frontend_app_architecture_boundaries.py` | PASS |
| `tests/contract/test_frontend_service_state_boundaries.py` | PASS |
| `tests/unit/test_frontend_performance_27_fix_1.py` | PASS |
| total focal ampliado | `51 passed` |
| `frontend/scripts/build_frontend.py --output-dir <temp fora do checkout>` | PASS |
| `.venv\Scripts\python.exe -B ops/scripts/repo/validate_repo_hygiene.py --root .` | `success=true`, sem violacoes |

Tentativa adicional de `node --check` nos modulos fatiados ficou indisponivel no ambiente por `Acesso negado`, inclusive apos tentativa escalonada. O fechamento nao dependeu desse check; ficou baseado nos testes focais, build temporario e hygiene final.

O validator de hygiene ficou vermelho durante as rodadas de teste somente por `__pycache__` gerado/local fora das excecoes. Foram removidos apenas residuos locais de bytecode fora de `.venv/` e `archive/`: `89` diretorios `__pycache__` no total do bloco e `0` arquivos `.pyc/.pyo` soltos. A validacao final com `python -B` ficou verde.

## 6. Criterio de fechamento

| criterio | status |
| --- | --- |
| `pages-dashboard-tripulantes.js` significativamente menor | fechado |
| dashboard com owner explicito | fechado |
| tripulantes lista/form/avatar/adapters com owner explicito | fechado |
| relatorio individual separado | fechado |
| exports roteados preservados | fechado |
| rotas/loaders/permissoes sem alteracao | fechado |
| checks focais sem drift funcional relevante | fechado |
| hygiene verde apos limpeza restrita | fechado |

Decisao objetiva: `fechado`.

## 7. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| `pages-dashboard-tripulantes.js` como wrapper compativel temporario | remanescente controlado | manter ate decisao futura sobre loaders por feature; nao remover neste bloco. |
| `features/tripulantes/form-page.js` ainda grande | remanescente planejado | fatiamento mais fino de formulario/foto/documentos exige bloco proprio. |
| imports ainda passam por `lib.js` facade | remanescente planejado | tratar apenas quando houver bloco de `shared/` ou refinamento de services. |
| `features/treinamentos` ainda nao existe | fora do bloco | pertence ao F05. |
| compat/static/backend adapters | fora do bloco | pertence ao F06. |
| baseline `admin/routes.py:748>725` | remanescente fora do bloco | nao foi tratado nem mascarado. |
