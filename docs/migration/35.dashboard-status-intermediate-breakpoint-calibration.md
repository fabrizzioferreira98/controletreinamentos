# 35.dashboard - Calibracao do breakpoint intermediario em Visao geral dos status

## Bloco executado

`Dashboard status intermediate breakpoint calibration`.

Decisao do bloco: `fechado`.

## Escopo real

Calibracao aplicada no bloco `Visao geral dos status` em `#/dashboard`, com foco em notebook/tablet:

- grid externo da faixa intermediaria para reduzir compressao do bloco;
- resumo superior (`BASE MONITORADA`, numero, apoio, distribuicao);
- grid de cards internos;
- ritmo visual (gaps, padding, sizing e colapso).

Mantido sem alteracao:

- contratos, dados, loaders, guards, permissoes, rotas e APIs;
- estrutura macro do dashboard;
- sidebar e topbar;
- regras internas de `Base operacional` (sem alteracao de card, tipografia, conteudo ou CTA).

## Mudanca aplicada

- no `<=1200px`, a faixa intermediaria passou a priorizar largura util do painel de status no grid externo (`1.12fr / 0.88fr`) para reduzir compressao em notebook;
- o resumo superior do bloco passou a colapsar mais cedo no intermediario (stack `copy -> text`) com tipografia e distribuicao calibradas;
- os cards internos no intermediario passaram de coluna unica para grade de duas colunas com largura minima util e densidade controlada;
- em `<=1120px`, `<=1024px` e `<=900px`, foram aplicados ajustes finos progressivos de largura minima, padding, gap, tamanho do numero e line-height para manter hierarquia sem colisao.

## Prova material

- `frontend/src/app.css`:
  - `@media (max-width: 1200px)` com calibracao de grid externo e bloco `dashboard-status-*`;
  - `@media (max-width: 1120px)` com ajuste fino local de densidade e largura minima dos cards de status;
  - `@media (max-width: 1024px)` com refinamento para tablet horizontal;
  - `@media (max-width: 900px)` com estabilidade para tablet vertical.
- nenhum ajuste de markup funcional em `frontend/src/features/dashboard/page.js`;
- nenhum ajuste em contratos, rotas, loaders, guards, permissoes ou APIs.

## Validacao focal

- `.\.venv\Scripts\python.exe -m pytest tests/contract/test_frontend_priority_page_visual_foundation.py` com `4 passed`;
- `.\.venv\Scripts\python.exe frontend\scripts\build_frontend.py --output-dir .\runtime\audit-dashboard-status-intermediate-breakpoint-20260426-1`;
- revisao estrutural das regras `dashboard-status-*` por breakpoint intermediario.

## Regressao minima

- sem alteracao funcional de dados/contratos/rotas;
- sem alteracao da arquitetura do dashboard;
- sem alteracao interna em `Base operacional`.

## Registro em migration

Este arquivo formaliza a calibracao do breakpoint intermediario do bloco `Visao geral dos status`.

