# 32.3.8 - Evidencia assinada final do candidato de entrega

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `32.3.8-evidencia-assinada-final` |
| objetivo | fechar a lacuna de assinatura/HMAC da evidencia final do candidato `release_20260420_2932_210420`. |
| candidato final | `release_20260420_2932_210420` |
| ambiente | `homolog` |
| manifest alvo | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json` |
| checklist alvo | `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md` |
| resultado do bloco | `BLOQUEADO` |
| `GO_OPERACIONAL_FINAL` emitido | nao |

Este bloco nao usa chave fake, nao reaproveita segredo de quarentena, nao altera codigo de produto, nao altera candidato final e nao declara GO operacional.

## 2. Mudanca aplicada

Nao foi gerada assinatura HMAC no pacote externo, porque `RELEASE_EVIDENCE_SIGNING_KEY` nao esta materialmente disponivel em fonte real aplicavel ao ambiente `homolog`.

| item | acao |
| --- | --- |
| manifest do candidato | nao alterado; permanece sem `signature_hmac_sha256`. |
| checklist final | nao alterado. |
| `release_decision.md` | nao alterado. |
| chave de assinatura | nao provisionada, nao fabricada e nao inferida. |
| artefato assinado | nao gerado. |
| documentacao de migration | criada para registrar o bloqueio material. |
| README de migration | indexado. |

## 3. Prova material

### 3.1 Trilha oficial de assinatura

O validador oficial calcula a assinatura com base no manifest canonico, removendo `signature_hmac_sha256` antes do HMAC:

```text
ops\scripts\release\validate_operational_evidence.py
```

Trecho operacional identificado:

| funcao | regra |
| --- | --- |
| `_signature_payload` | remove `signature_hmac_sha256` do payload antes de serializar. |
| `_expected_signature` | calcula `hmac.new(signing_key, body, hashlib.sha256).hexdigest()`. |
| `--require-signature` | exige `signature_hmac_sha256` no manifest e `RELEASE_EVIDENCE_SIGNING_KEY` no ambiente. |

### 3.2 Fontes reais verificadas

| fonte | SHA-256 | leitura redigida |
| --- | --- | --- |
| `C:\srv\controle-treinamentos\env\hml.env` | `c8df9499bfb524426a40c2b53b7481dda5cf2ba253ca485c7636f94fc5df3381` | contem `DATABASE_URL` e `METRICS_TOKEN`; nao contem `RELEASE_EVIDENCE_SIGNING_KEY`. |
| `C:\srv\controle-treinamentos\services\CT-App-Hml\CT-App-Hml.xml` | `3ee9f7673c79e47835f0737589e8c363c62a786c62374ebef72a66d98f13b001` | nao contem `RELEASE_EVIDENCE_SIGNING_KEY`. |
| backups `C:\srv\controle-treinamentos\env\hml.env*` | hashes verificados individualmente durante a etapa | nao contem `RELEASE_EVIDENCE_SIGNING_KEY`, `E2E_DATABASE_URL`, `E2E_LOGIN` ou `E2E_PASSWORD`. |
| `C:\srv\controle-treinamentos\config-backup\hml.env.20260402-171924.bak` | `5b026732c10ce78aacec6b78a4886aba8e72fde9f1d93c6c49c31fc3b2a4f9bf` | nao contem `RELEASE_EVIDENCE_SIGNING_KEY`. |
| ambiente do processo | leitura redigida | `RELEASE_EVIDENCE_SIGNING_KEY` ausente, tamanho `0`. |

Material encontrado em quarentena ou referencia de CI nao foi usado como chave real de homolog:

| material | classificacao | motivo |
| --- | --- | --- |
| `.github\workflows\*.yml` | referencia de segredo CI | aponta para `secrets.RELEASE_EVIDENCE_SIGNING_KEY`, sem valor material local. |
| `C:\srv\controle-treinamentos\repo-quarantine\...\.secrets\release_evidence_signing_key.txt` | fora do aceite | material em quarentena, nao fonte viva do candidato `homolog`. |

### 3.3 Estado do manifest antes da tentativa

| campo | valor |
| --- | --- |
| `release_id` | `release_20260420_2932_210420` |
| `environment` | `homolog` |
| hash SHA-256 | `40bd6fdceb2baabcd8111285aad2efd76375375174e19e889eba0d1a5a59ba40` |
| `signature_hmac_sha256` | ausente |

## 4. Teste / check

### 4.1 Validacao oficial com assinatura obrigatoria

Comando executado:

```powershell
.\.venv\Scripts\python.exe ops\scripts\release\validate_operational_evidence.py --manifest "C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json" --profile release --max-age-hours 24 --require-hashes --require-signature --require-alert-ack --require-rollback-runtime-ids --regression-checklist "C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md"
```

Resultado bruto:

| campo | valor |
| --- | --- |
| exit code | `1` |
| `success` | `false` |
| `checked_artifacts` | `21` |
| issue 1 | `signature_missing` |
| issue 2 | `signing_key_env_missing:RELEASE_EVIDENCE_SIGNING_KEY` |
| issue 3 | `post_deploy_smoke_before_gate:2026-04-22T01:03:07.280998+00:00:gate:2026-04-22T01:22:43.871097+00:00` |

Leitura objetiva:

| requisito | resultado |
| --- | --- |
| validador deixou de acusar `signature_missing` | nao |
| validador deixou de acusar `signing_key_env_missing` | nao |
| chave real de homolog disponivel | nao |
| assinatura materialmente valida | nao |
| GO operacional final permitido por esta etapa | nao |

### 4.2 Preflight redigido do processo

| variavel | presente | tamanho |
| --- | --- | ---: |
| `DATABASE_URL` | `false` | `0` |
| `E2E_DATABASE_URL` | `false` | `0` |
| `E2E_LOGIN` | `false` | `0` |
| `E2E_PASSWORD` | `false` | `0` |
| `RELEASE_EVIDENCE_SIGNING_KEY` | `false` | `0` |

Esse preflight confirma que nao havia chave HMAC material no processo usado para a validacao.

## 5. Criterio de fechamento

| criterio | status |
| --- | --- |
| `RELEASE_EVIDENCE_SIGNING_KEY` real de `homolog` localizado | nao atendido |
| assinatura HMAC gerada ou ratificada | nao atendido |
| manifest final validado com `--require-signature` | nao atendido |
| `signature_missing` removido da validacao | nao atendido |
| `signing_key_env_missing:RELEASE_EVIDENCE_SIGNING_KEY` removido da validacao | nao atendido |
| chave fake/placeholder evitada | atendido |
| GO operacional final nao emitido | atendido |
| README indexado | atendido apos atualizacao |

Decisao do bloco: `BLOQUEADO`.

O criterio formal de fechamento material da etapa nao foi atendido, porque a validacao com assinatura obrigatoria ainda acusa `signature_missing` e `signing_key_env_missing:RELEASE_EVIDENCE_SIGNING_KEY`.

## 6. Divida rebaixada ou remanescente

| item | status | justificativa | encaminhamento |
| --- | --- | --- | --- |
| identificacao da trilha oficial de assinatura | rebaixada/fechada | regra de HMAC e validador oficial identificados. | manter `validate_operational_evidence.py --require-signature` como criterio. |
| uso de chave fake | rebaixada/fechada | nenhuma chave foi criada, inferida ou reaproveitada de material fora do aceite. | continuar bloqueando assinatura sem fonte real. |
| `signature_missing` | remanescente bloqueante | manifest nao recebeu `signature_hmac_sha256`. | provisionar chave real e assinar o manifest. |
| `signing_key_env_missing` | remanescente bloqueante | `RELEASE_EVIDENCE_SIGNING_KEY` ausente no ambiente aplicavel. | carregar segredo real de homolog no processo de assinatura/validacao. |
| ordem temporal smoke/gate | remanescente bloqueante para GO | validador ainda acusa `post_deploy_smoke_before_gate`. | rerodar trilha final coerente apos destravar gate e assinatura. |
| `GO_OPERACIONAL_FINAL` | nao emitido | assinatura material nao existe e gate final segue sem base verde suficiente. | emitir somente em etapa propria, com assinatura e demais gates coerentes. |
