# 31.6.1 - Legacy vivo

## 1. Bloco executado

Bloco: `31.6.1-legacy-vivo`.

Escopo:

- `legacy/LIVE_LEGACY.md`;
- rotas/templates SSR;
- operacoes SSR compat;
- aba File legada de tripulante;
- compatibilidade de midia/status de tripulante.

Fora de escopo:

- remover legado;
- migrar SSR;
- alterar rotas, contratos, backend, frontend ou testes;
- transformar `legacy/` em trilha principal;
- corrigir `admin/routes.py:748>725`.

## 2. Mudanca real

Atualizada a ratificacao em `legacy/LIVE_LEGACY.md` para deixar cada item de legacy vivo com:

- decisao de permanencia;
- evidencia viva concreta;
- bloqueio explicito contra feature nova;
- condicao objetiva de saida.

Nao houve mudanca funcional.

## 3. Prova real

| item | decisao de permanencia | evidencia viva | bloqueio contra feature nova | condicao objetiva de saida |
| --- | --- | --- | --- | --- |
| Rotas e templates SSR | manter como legado vivo | `legacy/domains/ssr-html.md`; uso de `render_template`; teste arquitetural detecta SSR em boundary vivo | feature nova nao nasce em SSR legado | API/frontend substituto publicado, rotas HTML fora do boundary corrente, imports/handlers migrados e testes de rota/contrato atualizados |
| Operacoes SSR compat | manter como legado vivo | `contracts/operacoes.py` marca endpoints como `ssr_compat`; `tests/contract/test_operacoes_contract.py` valida esse estado | nao ampliar superficie SSR de operacoes | consumidor API real registrado, rotas HTML deixam de ser fronteira corrente e contratos deixam de exigir `ssr_compat` |
| Aba File legada de tripulante | manter como legado vivo | `legacy/domains/tripulante-file.md`; rota `/tripulantes/:id/file`; link `files_legacy`; E2E de File em `tests/e2e/test_critical_journeys.py` | nao criar nova feature na aba File legada | consumidores migrados para `/api/v1/tripulantes/:id/files`, link `files_legacy` removido e E2E migrado para caminho canonico |
| Compatibilidade de midia/status de tripulante | manter como compat de dados vivo | `legacy/data-compat/tripulante-media-status.md`; uso/testes de `foto_base64`, `foto_storage_ref`, `db:bytea` e status historico | nao criar novo formato legado nem nova escrita em dado historico | migracao de dados comprovada, suite cobrindo apenas formatos canonicos e plano de rollback para dado legado remanescente |

Prova de escopo preservado:

- nenhum item de `legacy/` foi removido;
- nenhum codigo de produto foi alterado;
- nenhum contrato foi relaxado;
- nenhum teste foi mascarado;
- `legacy/` permanece inventario de legado vivo, nao trilha principal.

## 4. Teste / check

Checks executados ou usados neste bloco:

```powershell
Select-String -Path legacy\LIVE_LEGACY.md -Pattern 'Ratificacao 31.6.1|feature nova|condicao objetiva de saida'
Select-String -Path docs\migration\31.6.1-legacy-vivo.md -Pattern '[^\x00-\x7F]'
$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B -m pytest tests\ops\test_repo_hygiene_validation.py -q -p no:cacheprovider
$env:PYTHONDONTWRITEBYTECODE='1'; .\.venv\Scripts\python.exe -B ops\scripts\repo\validate_repo_hygiene.py --root .
```

Resultados executados:

- coerencia `LIVE_LEGACY`/migration: todos os 4 itens aparecem com permanencia, evidencia viva, bloqueio contra feature nova e condicao objetiva de saida;
- ASCII documental local: sem caracteres nao ASCII desnecessarios em `31.6.1-legacy-vivo.md`;
- `tests/ops/test_repo_hygiene_validation.py`: `11 passed`;
- `validate_repo_hygiene.py --root .`: `success=true`, `violations=[]`;
- contagem final fora de `.venv`, `archive` e `frontend/dist`: `__pycache__=0`, `.pyc/.pyo=0`;
- `admin/routes.py:748>725`: baseline arquitetural conhecido, nao corrigido nem escondido por este bloco.

## 5. Registro

Artefatos atualizados:

- `legacy/LIVE_LEGACY.md`;
- `docs/migration/31.6.1-legacy-vivo.md`;
- `docs/migration/README.md`.

## 6. Criterio de fechamento

| criterio | status esperado |
| --- | --- |
| cada item de legacy vivo tem permanencia ratificada | fechado |
| evidencia viva registrada por item | fechado |
| feature nova em legacy bloqueada por item | fechado |
| condicao objetiva de saida registrada por item | fechado |
| nenhuma remocao/migracao lateral executada | fechado |
| baseline arquitetural conhecido nao foi mascarado | fechado |

Decisao: `fechado` com checks documentais e de hygiene verdes e baseline conhecido explicitamente separado.

## 7. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| Migrar SSR para API/frontend | remanescente | frente propria de produto/frontend/API. |
| Encerrar operacoes SSR compat | remanescente | exige consumidor API real e contratos sem `ssr_compat`. |
| Remover aba File legada | remanescente | exige migracao para API canonica e E2E atualizado. |
| Remover compat de `foto_base64`, `db:bytea` e status historico | remanescente | exige migracao de dados, suite canonica e rollback. |
| `admin/routes.py:748>725` | remanescente | baseline arquitetural conhecido fora do bloco de legacy. |
