# 31.B05 - Modulos frontend nao roteados

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `B05-modulos-frontend-nao-roteados` |
| data | 2026-04-22 |
| objetivo | inventariar modulos `pages-*.js`, cruzar com router/loaders/imports/testes/docs e impedir que arquivo com aparencia de pagina fique meio vivo sem status. |
| escopo | `frontend/src/pages-*.js`, `frontend/src/app.js`, imports frontend, referencias em testes/docs vivas e check estatico de ownership. |
| fora do escopo | remocao de modulo, criacao de rota, redesign, migracao SSR, shell, build/runtime/env e wrappers. |
| decisao | `fechado` |

## 2. Inventario fisico

Arquivos `frontend/src/pages-*.js` encontrados:

| modulo | tamanho aproximado | exports de pagina |
| --- | ---: | --- |
| `pages-dashboard-tripulantes.js` | `81083` bytes | `renderDashboardPage`, `renderTripulantesListPage`, `renderRelatorioIndividualPage`, `renderTripulanteFormPage` |
| `pages-training-workspace.js` | `76145` bytes | `renderTreinamentosListPageV2`, `renderTrainingRootPageV2` |
| `pages-treinamentos-relatorios.js` | `153467` bytes | `renderTreinamentosListPage`, `renderTrainingRootPage`, `renderTreinamentoFormPage`, `renderRelatorioHabilitacoesPage`, `renderRelatorioProdutividadePage` |

Nao foram encontrados outros arquivos de superficie de pagina em `frontend/src` fora do padrao `pages-*.js`.

## 3. Cruzamento estrutural

Router oficial em `frontend/src/app.js`:

| loader | modulo carregado | rotas/export usadas |
| --- | --- | --- |
| `tripulantes` | `pages-dashboard-tripulantes.js` | `#/dashboard -> renderDashboardPage`; `#/tripulantes -> renderTripulantesListPage`; `#/relatorios/individual -> renderRelatorioIndividualPage`; `#/tripulantes/new` e `#/tripulantes/<id> -> renderTripulanteFormPage` |
| `treinamentos` | `pages-treinamentos-relatorios.js` | `#/treinamentos` e `#/treinamentos/new -> renderTreinamentosListPage`; `#/treinamentos/raiz -> renderTrainingRootPage`; `#/treinamentos/<id> -> renderTreinamentoFormPage`; `#/relatorios/habilitacoes -> renderRelatorioHabilitacoesPage`; `#/relatorios/produtividade -> renderRelatorioProdutividadePage` |

Imports frontend:

| modulo | importadores vivos em `frontend/src` |
| --- | --- |
| `pages-dashboard-tripulantes.js` | `app.js` |
| `pages-treinamentos-relatorios.js` | `app.js` |
| `pages-training-workspace.js` | nenhum |

Referencias em testes/docs:

| modulo | referencias relevantes |
| --- | --- |
| `pages-dashboard-tripulantes.js` | testes funcionais de UX e performance/lazy import; docs de arquitetura e migration historicas. |
| `pages-treinamentos-relatorios.js` | testes funcionais de UX e performance/lazy import; docs de arquitetura e migration historicas. |
| `pages-training-workspace.js` | referencia stale em `docs/architecture/ARCHITECTURE.md`, corrigida para nao promove-lo a superficie SPA oficial. |

Nota de build: `frontend/scripts/build_frontend.py` copia a arvore de `frontend/src` para o output, entao presenca em `frontend/dist` ou em build temporario nao prova rota viva.

## 4. Classificacao final

| modulo | classificacao | owner estrutural | entra na superficie oficial | por que ainda existe | condicao de saida quando nao for pagina SPA viva |
| --- | --- | --- | --- | --- | --- |
| `pages-dashboard-tripulantes.js` | `pagina_spa_viva` | frontend + tripulantes/dashboard | sim | loader `tripulantes` e exports usados por rotas SPA oficiais. | n/a |
| `pages-treinamentos-relatorios.js` | `pagina_spa_viva` | frontend + treinamentos/relatorios | sim | loader `treinamentos` e exports usados por rotas SPA oficiais. | n/a |
| `pages-training-workspace.js` | `candidata_remocao_futura` | frontend + treinamentos | nao | residual fisico com exports V2, sem loader, sem importador frontend e sem teste que o exercite como rota. | bloco futuro deve provar ausencia de consumidor externo/direto do artefato estatico antes de remover; ate la, nao promover a rota nem remover por presuncao. |

Resumo quantitativo:

| classificacao | quantidade |
| --- | ---: |
| `pagina_spa_viva` | 2 |
| `modulo_auxiliar_vivo` | 0 |
| `nao_roteado_com_owner` | 0 |
| `compat_residual_controlado` | 0 |
| `legacy_vivo_controlado` | 0 |
| `ambigua_pendente` | 0 |
| `candidata_remocao_futura` | 1 |

## 5. Mudanca aplicada

Foi alterado `docs/architecture/FRONTEND_ARCHITECTURE.md` para registrar:

- regra de que `pages-*.js` fisico nao define superficie oficial;
- tabela de ownership dos tres modulos de pagina;
- `pages-training-workspace.js` como `candidata_remocao_futura`, sem rota oficial e sem autorizacao de remocao neste bloco;
- exigencia de classificacao para novo `pages-*.js`.

Foi alterado `docs/architecture/ARCHITECTURE.md` para corrigir a referencia stale de treinamentos:

- a superficie SPA oficial de treinamentos passa a apontar para `pages-treinamentos-relatorios.js`;
- `pages-training-workspace.js` fica descrito como modulo nao roteado classificado em `FRONTEND_ARCHITECTURE.md`.

Foi adicionado `tests/contract/test_frontend_page_module_boundaries.py` para validar:

- todo `pages-*.js` tem status explicito;
- somente modulos `pagina_spa_viva` sao importados pelo router;
- exports de paginas SPA batem com as referencias do router;
- modulo nao roteado nao tem importador frontend;
- doc viva contem a classificacao de cada modulo.

Nenhum modulo foi removido. Nenhuma rota nova foi criada.

## 6. Checks

Checks executados com `PYTHONDONTWRITEBYTECODE=1`:

| check | resultado |
| --- | --- |
| `tests/contract/test_frontend_page_module_boundaries.py` | PASS |
| `tests/contract/test_frontend_shell_navigation_boundaries.py` | PASS |
| `tests/contract/test_frontend_ux_functional_guards.py` | PASS |
| `tests/unit/test_frontend_performance_27_fix_1.py` | PASS |
| total focal | `23 passed` |
| `ops/scripts/repo/validate_repo_hygiene.py --root .` | `success=true`, sem violacoes |

Os checks Python geraram `__pycache__` em uma tentativa intermediaria. Foram removidos apenas residuos locais de bytecode fora de `.venv/` e `archive/`: `32` diretorios `__pycache__` e `0` arquivos `.pyc/.pyo` soltos. A validacao final ficou verde.

## 7. Criterio de fechamento

| criterio | status |
| --- | --- |
| inventario completo de `pages-*.js` | fechado |
| cruzamento com router/loaders/imports/testes/docs | fechado |
| classificacao por modulo | fechado |
| owner e condicao de saida para modulo nao-SPA | fechado |
| `pages-training-workspace.js` tratado explicitamente | fechado |
| ambiguidades remanescentes | `0` |

Decisao objetiva: `fechado`.

## 8. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| `pages-training-workspace.js` | candidata a remocao futura | nao remover neste bloco; exige prova futura de ausencia de consumidor externo/direto do artefato estatico. |
| copia fisica de `pages-training-workspace.js` no build | remanescente permitido | decorre do build que copia `frontend/src`; nao e superficie oficial enquanto nao houver loader/rota. |
| possivel extracao modular futura dos grandes arquivos SPA | remanescente fora do bloco | tratar apenas na futura refatoracao arquitetural do frontend. |
| baseline `admin/routes.py:748>725` | remanescente fora do bloco | nao foi tratado nem mascarado. |
