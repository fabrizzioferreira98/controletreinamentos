# 31.7.1 - Backlog: refino de status

## 1. BLOCO EXECUTADO

| campo | valor |
| --- | --- |
| bloco | `31.7.1-backlog-refino-status` |
| prioridade | P2 |
| objetivo | refinar o status do backlog de remocao usando apenas decisoes ja tomadas na Frente 31. |
| escopo | `docs/governance/removal-backlog.md`, indice migration e coerencia com `31.2.1` e `31.6.1`. |
| fora do escopo | autorizar remocao, criar bloco novo, elevar P2 a P0, corrigir `admin/routes.py`, remover legacy, alterar wrappers ou alterar codigo de produto. |
| decisao | fechado como refinamento documental. |

## 2. MELHORIA APLICADA

Foi refinada a secao `Atualizacao Frente 31` em `docs/governance/removal-backlog.md`.

A melhoria aplicada:

- mantem `31.2.1-baseline-gates-estruturais` como baseline medido, sem transformar `admin/routes.py:748>725` em item de remocao fisica;
- registra que artefatos locais gerados medidos em `31.2.1` foram tratados em `31.4.2`, sem esconder o historico vermelho do baseline;
- mantem `31.6.1-legacy-vivo` como decisao de permanencia controlada, com saida condicionada a `legacy/LIVE_LEGACY.md`;
- explicita que o backlog nao autoriza remocao, nao cria prioridade nova e nao muda severidade por redacao.

Nao houve mudanca funcional.

## 3. PROVA

Prova documental:

| item | prova |
| --- | --- |
| secao de backlog | `docs/governance/removal-backlog.md` contem `Atualizacao Frente 31` e `Refino de status 31.7.1`. |
| coerencia com `31.2.1` | o backlog separa `admin/routes.py:748>725` como baseline arquitetural fora de remocao fisica. |
| coerencia com `31.6.1` | o backlog mantem `legacy/`, `legacy/domains/*` e `legacy/data-compat/*` fora de limpeza generica enquanto houver legado vivo. |
| sem autorizacao indevida | o texto declara que nao autoriza remocao e que P2 nao vira P0 por redacao. |

Checks executados:

| check | resultado |
| --- | --- |
| busca da secao nova no backlog | encontrou `Atualizacao Frente 31` e `Refino de status 31.7.1`. |
| `tests/ops/test_repo_hygiene_validation.py` | `11 passed`. |
| coerencia `31.2.1`/`31.6.1` | encontrou `admin/routes.py:748>725` como baseline separado e `legacy/LIVE_LEGACY.md` como condicao de permanencia/saida. |
| ASCII documental local | sem ocorrencias em `31.7.1`, `removal-backlog.md` e `README.md`. |
| residuos gerados pelo pytest | removidos `32` diretorios `__pycache__` e `134` arquivos `.pyc/.pyo` fora de `.venv`, `archive` e `frontend/dist`. |

## 4. REGISTRO

Artefatos atualizados:

- `docs/migration/31.7.1-backlog-refino-status.md`;
- `docs/governance/removal-backlog.md`;
- `docs/migration/README.md`.

## 5. CRITERIO DE FECHAMENTO

| criterio | status esperado |
| --- | --- |
| backlog refinado apenas com decisoes ja tomadas | fechado |
| nenhuma remocao autorizada | fechado |
| baseline `admin/routes.py:748>725` nao mascarado | fechado |
| legacy vivo mantido fora de limpeza generica | fechado |
| P2 nao convertido em P0 por redacao | fechado |
| indexacao em `docs/migration/README.md` conferida | fechado |

Decisao objetiva: `fechado` apos checks documentais e teste focal.

## 6. DIVIDA REBAIXADA OU REMANESCENTE

| item | status | encaminhamento |
| --- | --- | --- |
| `admin/routes.py:748>725` | remanescente | baseline arquitetural conhecido, fora do backlog fisico de remocao. |
| wrappers `scripts/` | remanescente controlado | remover somente com prova de migracao/ausencia de consumidor e ajuste de teste de compat. |
| archive e nested repo copy | remanescente congelado | remover somente com perda objetiva de valor de auditoria e update de `archive/MANIFEST.md`. |
| `.venv/` e `frontend/dist/` | remanescente como excecao local gerada | nao sao topologia oficial nem fonte viva. |
| legacy vivo | remanescente controlado | saida apenas pelas condicoes objetivas de `legacy/LIVE_LEGACY.md`. |
