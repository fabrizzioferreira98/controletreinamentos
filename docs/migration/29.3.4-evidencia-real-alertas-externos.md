# 29.3.4 - Evidencia real de `alerts_external_e2e`

## Objetivo do bloco

Executar `alerts_external_e2e` pela trilha oficial de release, produzir evidencia material no pacote e provar a borda externa de alerta com HTTP 2xx, ack e destino de escalonamento.

## Mudancas aplicadas

- `ops/scripts/release/run_alerts_external_release.py`
  - encapsula o drill oficial `alerts_external_drill.py`;
  - exige `acknowledged_by` e `escalation_target`;
  - grava `alerts/alerts_external_drill.json` no pacote externo;
  - enriquece o artefato com `generated_at`, `release_id`, `commit_sha`, `environment` e `evidence_manifest`;
  - atualiza `checks.alerts_external_e2e` no manifest como `PASS` ou `FAIL`;
  - atualiza o anexo `Alertas externos` no checklist do pacote.
- `tests/ops/test_release_alerts_external_entrypoints.py`
  - cobre integracao verde com manifest/checklist;
  - cobre falha sem mascarar ausencia de entrega externa como PASS.

## Execucao operacional

- ambiente: homolog;
- pacote canonico: `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\`;
- trilha usada:
  - `ops/scripts/release/run_alerts_external_release.py`;
  - motor chamado: `ops/scripts/release/alerts_external_drill.py`;
  - webhook esperado por `ALERTS_TEST_WEBHOOK_URL` ou `--webhook-url-file`.

## Resultado da tentativa

- `ALERTS_TEST_WEBHOOK_URL` nao existe em `C:\srv\controle-treinamentos\env\hml.env`;
- tambem nao foi encontrado webhook equivalente em `prod.env` ou backups operacionais consultados;
- a execucao oficial retornou `success=false`;
- `external_delivery_exercised=false`;
- `checks.alerts_external_e2e.status = FAIL`.

## Artefato produzido

- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\alerts\alerts_external_drill.json`
  - comprova a tentativa pela trilha oficial;
  - registra identidade do pacote;
  - registra que a borda externa nao foi exercitada por ausencia de webhook configurado.

## Validacao

- `python -m pytest tests/ops/test_release_alerts_external_entrypoints.py -q` -> `2 passed`
- `harden_release_manifest.py --manifest <manifest>` -> recalculou 7 hashes
- `validate_operational_evidence.py --manifest <manifest> --require-hashes --require-alert-ack --regression-checklist <checklist>` -> `FAIL`, incluindo:
  - `check_not_pass:alerts_external_e2e:FAIL`
  - `alerts_external_e2e:payload_success_false`
  - `alerts_external_e2e:http_status_not_2xx`
  - `alerts_external_e2e:acknowledged_false`
  - `alerts_external_e2e:acknowledged_by_missing`
  - `alerts_external_e2e:escalation_target_missing`

## Fechamento operacional do bloco

O bloco nao esta fechado. A trilha oficial e a integracao ao pacote foram criadas e testadas, mas nao existe webhook real configurado para homolog. Sem webhook, qualquer PASS seria simulacao ou evidencia cosmetica, o que esta proibido neste bloco.

## Divida rebaixada ou remanescente

- integracao do drill com pacote release -> eliminada;
- risco de evidenciar alerta so por configuracao/documentacao -> eliminado pelo wrapper e pelo validador;
- execucao externa real do alerta -> aberta, bloqueada por ausencia de webhook/secret real para homolog;
- ack/escalonamento real -> aberto, dependente da mesma execucao externa real;
- pacote release completo -> continua aberto tambem por `load_authenticated_30w` acima do SLO e checks posteriores pendentes.
