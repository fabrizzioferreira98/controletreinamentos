# 29.3.2 - Evidencia real de `e2e_homolog`

## Objetivo do bloco

Fechar `e2e_homolog` como execucao real de homolog, com artefatos materiais integrados ao pacote oficial de release e sem depender de `pytest` local em `APP_ENV=testing` como substituto de runtime homolog.

## Mudancas aplicadas

- `ops/scripts/release/run_e2e_homolog_live.py`
  - trocou a dependencia de base fixa `E2E` pela base ativa real do banco de homolog;
  - passou a executar a jornada real contra `http://127.0.0.1:8102`, incluindo sessao, capacidades, CRUD, File, foto, anexo de treinamento, export CSV/PDF e logout;
  - manteve a prova do export como superficie operacional real (CSV/PDF e headers corretos), sem depender da visibilidade da massa recem-semeada como criterio binario do bloco.
- `ops/scripts/release/run_e2e_homolog_rounds.py`
  - passou a atualizar o anexo `E2E` do checklist de forma idempotente na linha correta, sem duplicar apontamentos;
  - continua produzindo `e2e_run_1.txt`, `e2e_run_2.txt` e `e2e_homolog_summary.json`, e acoplando esses artefatos ao manifest/checklist oficiais.
- `ops/scripts/release/release_gate.py`
  - quando existe `--evidence-manifest`, o gate passa a enviar a saida de `e2e_homolog` para o diretorio do proprio pacote de release e repassa `--manifest` / `--regression-checklist` para o helper.
- `tests/ops/test_release_e2e_homolog_entrypoints.py`
  - cobre o helper de rounds, o encaixe do gate no pacote e o fallback `workspace-without-git` do hardener.

## Execucao real em homolog

- ambiente: runtime vivo em `http://127.0.0.1:8102` com `APP_ENV=homolog`;
- trilha usada: `ops/scripts/release/run_e2e_homolog_rounds.py` chamando `run_e2e_homolog_live.py`;
- pacote canonico produzido em `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\`.

### Artefatos gerados

- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\e2e\e2e_run_1.txt`
- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\e2e\e2e_run_2.txt`
- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\e2e\e2e_homolog_summary.json`
- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\release_evidence_manifest.json`
- `C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\regression_audit_checklist.md`

### Resultado da rodada real

- 2 rodadas consecutivas;
- 13 passos aprovados por rodada;
- `success=true` no helper oficial.

## Evidencia material no pacote

- o manifest do pacote marca `checks.e2e_homolog.status = PASS`;
- os 3 artefatos de `e2e_homolog` entram no manifest com hash SHA-256 recalculado;
- o checklist aponta `- E2E: C:\srv-data\controle-treinamentos\homolog\evidence\release_20260420_2932_210420\e2e\e2e_run_1.txt`;
- `release_id=release_20260420_2932_210420`, `environment=homolog` e `commit_sha=workspace-without-git` ficaram coerentes entre manifest e checklist.

## Validacao

### Verde do bloco

- `python -m pytest tests/ops/test_release_e2e_homolog_entrypoints.py -q` -> `3 passed`
- `run_e2e_homolog_rounds.py --rounds 2 --min-passed 10 ...` -> `success=true`

### Validador oficial do pacote release

`validate_operational_evidence.py` passou a aceitar os artefatos de `e2e_homolog` sem acusar mismatch de ambiente, identidade ou checklist para este check. O pacote completo ainda fica vermelho porque os demais checks obrigatorios do perfil `release` permanecem `PENDING` neste ponto da 29.3:

- `load_authenticated_20w`
- `load_authenticated_30w`
- `jobs_concurrency_retry_dead_letter`
- `alerts_external_e2e`
- `backup_restore_drill`
- `rollback_drill`
- `post_deploy_smoke`
- `metrics_hardening`

O vermelho restante do validador, portanto, nao e mais do `e2e_homolog`; e o backlog esperado dos blocos seguintes.

## Fechamento operacional do bloco

`e2e_homolog` deixa de depender de simulacao local e passa a ter trilha real de homolog com evidencia material integrada ao pacote oficial. O unico motivo para o perfil `release` ainda nao ficar verde e a ausencia deliberada dos checks seguintes da 29.3, fora do escopo deste bloco.
