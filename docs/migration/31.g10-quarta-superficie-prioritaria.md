# 31.G10 - Quarta superficie prioritaria

## Bloco executado

`G10 - Quarta superficie prioritaria`.

Decisao do bloco: `fechado`.

Readiness de entrada: `pronta_para_expansao_visual` apos G09.

## Superficie escolhida e justificativa

Superficie escolhida: `training_record_detail`.

Rota viva: `#/treinamentos/<id>`.

Entry/export preservado: `renderTreinamentoFormPage`.

Justificativa material:

| criterio | leitura material |
| --- | --- |
| superficie viva e relevante | `frontend/src/app/route-registry.js` registra `pattern: /^#\/treinamentos\/\d+$/` para `renderTreinamentoFormPage`. |
| risco funcional controlavel | superficie SPA ja viva, com contrato limitado ao registro individual, formulario, anexos e APIs existentes. |
| reaproveitamento G06/G08 | formulario profundo, feedbacks, actions, upload/anexos e tabela de PDFs usam `ui-form-*`, `ui-table-*`, `ui-field-help`, `ui-page-shell` e `ui-page-header`. |
| menor dependencia SSR/compat | nao depende de pagina backend/SSR para renderizar a superficie principal. |
| custo/beneficio visual | alta densidade operacional e repeticao visual residual com baixo alcance lateral. |

Superficies nao escolhidas neste bloco:

- `relatorio_individual`: relevante, mas com acoplamento maior a modo de relatorio e fluxo especifico de consulta.
- `pages-training-workspace.js`: nao roteada oficialmente; segue fora da expansao visual como modulo nao promovido.
- novas familias de UI fora de treinamentos/tripulantes: exigem bloco proprio para nao expandir varias superficies ao mesmo tempo.

## Mudanca aplicada

Aplicacao da fundacao visual na quarta superficie, sem redesign completo e sem drift funcional.

- `frontend/src/features/treinamentos/form-page.js`
  - adiciona `training-record-detail-page-shell` com `priority-page-surface`, `ui-page-shell` e `ui-stack`;
  - adiciona `ui-page-header`, `ui-surface` e `ui-cluster` no header da superficie;
  - aplica `training-record-form-panel`, `ui-surface`, `ui-stack`, `ui-form-grid`, `ui-form-actions` e `ui-field-help` ao formulario;
  - preserva IDs, submit, delete, validacao, chamadas de API e handlers existentes.

- `frontend/src/features/treinamentos/attachments.js`
  - aplica `training-record-attachment-panel`, `ui-surface`, `ui-stack`, `ui-form-toolbar`, `ui-table-wrap`, `ui-table-density-compact`, `ui-table-actions`, `ui-table-state` e `ui-field-help`;
  - preserva upload, links de visualizar/baixar, delete de anexo e estados existentes.

- `frontend/src/app.css`
  - adiciona bloco G10 escopado a `.training-record-detail-page-shell`;
  - usa tokens de spacing, surface, radius, shadow e padding mobile;
  - nao cria primitive nova nem promove regra de dominio para `shared/ui`.

- `tests/contract/test_frontend_fourth_surface_visual_foundation.py`
  - valida escolha roteada, adocao material, preservacao funcional e CSS escopado/tokenizado.

## Prova material

- `frontend/src/app/route-registry.js` contem `pattern: /^#\/treinamentos\/\d+$/`, `renderTreinamentoFormPage` e permissao `treinamentos:edit`.
- `frontend/src/features/treinamentos/form-page.js` contem `training-record-detail-page-shell priority-page-surface ui-page-shell ui-stack`.
- `frontend/src/features/treinamentos/form-page.js` contem `training-record-form ui-form-grid`, `entity-form-section ui-surface ui-stack`, `field-feedback ui-field-help` e `entity-sticky-actions ui-form-actions`.
- `frontend/src/features/treinamentos/attachments.js` contem `training-record-attachment-panel ui-surface ui-stack`, `document-upload-form ui-form-toolbar`, `ui-table-wrap ui-table-density-compact`, `actions ui-table-actions` e `empty ui-table-state`.
- `frontend/src/app.css` contem bloco `G10: training record detail adopts shared visual foundation without route or submit drift.`

## Teste / check focal

Checks executados no fechamento:

- `python -B -m pytest -q -p no:cacheprovider tests/contract/test_frontend_fourth_surface_visual_foundation.py`
- `python -B -m pytest -q -p no:cacheprovider tests/contract/test_frontend_visual_readiness_third_wave.py tests/contract/test_frontend_shared_component_consolidation.py tests/contract/test_frontend_third_surface_visual_foundation.py tests/contract/test_frontend_table_form_patterns.py tests/contract/test_frontend_training_reports_visual_foundation.py tests/contract/test_frontend_page_module_boundaries.py tests/contract/test_frontend_feature_boundaries.py tests/contract/test_frontend_ux_functional_guards.py tests/unit/test_frontend_performance_27_fix_1.py`
- `python -B frontend/scripts/build_frontend.py --env-file frontend/.env.example --output-dir <temp externo>`
- `python -B ops/scripts/repo/validate_repo_hygiene.py --root .`

Resultado material registrado no fechamento do bloco.

Resultado final:

- teste focal G10: `4 passed`;
- suite focal visual/arquitetural: `51 passed`;
- build frontend gerado fora do checkout em `C:\Users\Fabrizzio.araujo\AppData\Local\Temp\controle-treinamentos-g10-35f25451f9024ff3a534de3733946292`;
- build final contem o bloco CSS `G10: training record detail adopts shared visual foundation without route or submit drift.`;
- hygiene final: `success=true`, `violations=[]`;
- residuos gerados pelos testes removidos antes do hygiene final: `32` diretorios `__pycache__`, `0` arquivos `.pyc/.pyo`, fora de `.venv` e `archive`.

## Regressao minima

- Nenhuma rota criada, removida ou alterada.
- Nenhum loader, guard, SSR, runtime ou deploy alterado.
- Nenhum endpoint alterado.
- Nenhum fluxo de submit/delete/upload alterado.
- Nenhum componente novo em `shared/ui`.
- Nenhuma expansao simultanea para outra superficie.

## Divida rebaixada ou remanescente

Rebaixada:

- `training_record_detail` deixa de ser superficie visual residual relevante e passa a ser `adocao_visual_controlada`.
- formulario, anexos, tabela de PDFs, feedbacks e actions passam a reaproveitar G06/G08.

Remanescente:

- `renderHoursReference(template)` segue com cards locais porque tambem serve superficies de treinamento ja tratadas e nao foi alvo deste bloco.
- aliases locais como `priority-page-*`, `panel`, `form-grid`, `table-wrap` e `field-feedback` continuam como compat visual controlada.
- `legacyRenderTreinamentoFormPage` permanece intocado.
- CSS backend/static e baseline `admin/routes.py:748>725` seguem fora do escopo.

## Criterio de fechamento

`fechado`: a quarta superficie prioritaria foi escolhida por criterio material e passou a usar materialmente a fundacao visual, os padroes compartilhados e os componentes minimos ja consolidados, sem drift funcional e sem abrir escopo lateral.
