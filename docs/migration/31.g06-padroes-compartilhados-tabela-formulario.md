# 31.G06 - Padroes compartilhados de tabela e formulario

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `G06-padroes-compartilhados-tabela-formulario` |
| data | 2026-04-22 |
| objetivo | consolidar primitives reutilizaveis para tabelas densas e formularios profundos, sem redesign amplo e sem design system inflado. |
| superficies adotadas | tripulantes, treinamentos, cadastro raiz, relatorios e formularios profundos ja tratados. |
| fora do escopo | nova superficie visual, SSR, runtime/deploy, rotas, loaders, guards, reescrita de formularios e motion sofisticado. |
| decisao | `fechado` |

## 2. Leitura transversal

| achado | classificacao |
| --- | --- |
| `table-wrap`, `data-table`, `actions` e empty states apareciam repetidos em tripulantes, treinamentos, training-root e relatorios | candidato a padrao compartilhado |
| `filters-bar`, `filters-wide`, `form-grid` e `form-actions` apareciam repetidos em filtros, cadastro raiz e formularios profundos | candidato a padrao compartilhado |
| G03/G04 ja tinham tokenizacao por pagina, mas parte de tabela/form ainda dependia de aliases locais | `divida_visual_controlada` reduzida |
| `shared/ui` permanecia neutro de dominio e podia receber primitives de familia sem virar UI kit | condicao favoravel |

## 3. Mudanca aplicada

| arquivo | mudanca |
| --- | --- |
| `frontend/src/shared/ui/tokens.css` | adiciona `--size-action-min-height`. |
| `frontend/src/shared/ui/primitives.css` | adiciona `ui-table-wrap`, `ui-table-density-compact`, `ui-table-actions`, `ui-table-state`, `ui-form-toolbar`, `ui-form-grid`, `ui-form-actions` e `ui-field-help`. |
| `frontend/src/shared/ui/README.md` | registra que primitives de tabela/formulario sao de familia, nao de produto. |
| `frontend/src/lib.js` | `emptyTableRowMarkup` passa a emitir `ui-table-state` sem alterar conteudo, colspan ou contrato. |
| `frontend/src/features/tripulantes/list-page.js` | adota `ui-form-toolbar`, `ui-form-grid`, `ui-form-actions`, `ui-table-wrap`, `ui-table-density-compact` e `ui-table-actions`. |
| `frontend/src/features/tripulantes/form-page.js` | adota `ui-form-grid`, `ui-form-actions`, `ui-table-wrap`, `ui-table-density-compact`, `ui-table-actions` e `ui-table-state`. |
| `frontend/src/features/treinamentos/list-page.js` | adota `ui-form-toolbar` e `ui-form-actions` no fluxo de selecao/workbench. |
| `frontend/src/features/treinamentos/program-helpers.js` | adota `ui-table-wrap`, `ui-table-density-compact`, `ui-table-actions` e `ui-table-state` na tabela de registros. |
| `frontend/src/features/treinamentos/form-page.js` | adota `ui-form-grid` e `ui-form-actions` nos formularios profundos. |
| `frontend/src/features/treinamentos/attachments.js` | adota `ui-form-toolbar`, `ui-table-wrap`, `ui-table-density-compact`, `ui-table-actions` e `ui-table-state`. |
| `frontend/src/features/training-root/page.js` | adota `ui-form-toolbar`, `ui-form-grid`, `ui-form-actions`, `ui-table-wrap`, `ui-table-density-compact`, `ui-table-actions` e `ui-table-state`. |
| `frontend/src/features/relatorios/habilitacoes-page.js` | adota `ui-form-toolbar`, `ui-form-grid`, `ui-form-actions`, `ui-table-wrap`, `ui-table-density-compact` e `ui-table-actions`. |
| `frontend/src/features/relatorios/produtividade-page.js` | adota `ui-form-toolbar`, `ui-form-grid`, `ui-form-actions`, `ui-table-wrap`, `ui-table-density-compact` e `ui-table-actions`. |
| `frontend/src/app.css` | mantem aliases legados como compat controlado, tokeniza padding/acoes de tabela e evita que `table-wrap` sobrescreva `ui-table-wrap`. |
| `tests/contract/test_frontend_table_form_patterns.py` | adiciona check focal G06 para primitives, adocao e preservacao de contratos. |

## 4. Prova material

| prova | resultado |
| --- | --- |
| primitives de tabela | `ui-table-wrap`, `ui-table-density-compact`, `ui-table-actions`, `ui-table-state` existem em `shared/ui`. |
| primitives de formulario | `ui-form-toolbar`, `ui-form-grid`, `ui-form-actions`, `ui-field-help` existem em `shared/ui`. |
| neutralidade de dominio | `primitives.css` continua sem dashboard, tripulante, treinamento, relatorio, API, router ou renderizacao JS. |
| adocao em tabelas | tripulantes, treinamentos, training-root e relatorios usam `ui-table-wrap ui-table-density-compact` e `ui-table-actions`. |
| adocao em formularios | filtros, forms profundos e actions usam `ui-form-toolbar`, `ui-form-grid` e `ui-form-actions`. |
| contratos preservados | ids de forms, botao continue/reset, tabs, exports, prints, submits e endpoints permanecem. |

## 5. Checks

Checks executados com `PYTHONDONTWRITEBYTECODE=1`:

| check | resultado |
| --- | --- |
| `tests/contract/test_frontend_table_form_patterns.py` | PASS |
| checks visuais/arquiteturais G01-G05 + G06 focal | `18 passed` |
| rodada ampliada final de arquitetura/visual frontend | `79 passed` |
| build frontend com `--output-dir <temp fora do checkout>` | PASS em `C:\Users\FABRIZ~1.ARA\AppData\Local\Temp\controle-treinamentos-g06-185a584b19f34b1bac31bd446a111641` |
| CSS final do build | contem `ui-table-wrap`, `ui-table-density-compact`, `ui-table-actions`, `ui-table-state`, `ui-form-toolbar`, `ui-form-grid`, `ui-form-actions`, `ui-field-help`, `--size-action-min-height`, `table-wrap:not(.ui-table-wrap)` e `var(--size-action-min-height)` |
| hygiene validator final | `success=true`, sem violacoes |

O validator de hygiene ficou vermelho durante a rodada final somente por `__pycache__` gerado/local fora das excecoes. Foram removidos apenas residuos locais de bytecode fora de `.venv/` e `archive/`: `32` diretorios `__pycache__` e `0` arquivos `.pyc/.pyo` soltos. A validacao final com `python -B` ficou verde.

## 6. Criterio de fechamento

| criterio | status |
| --- | --- |
| primitives reais de tabela/formulario materializadas | fechado |
| adocao minima em pontos concretos | fechado |
| contratos funcionais preservados | fechado |
| shared/ui nao virou pasta de dominio | fechado |
| redesign amplo fora do bloco | fechado |

Decisao objetiva do bloco: `fechado`.

## 7. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| aliases legados `table-wrap`, `filters-bar`, `form-grid`, `form-actions` | `divida_visual_controlada` | manter por compat; novos pontos devem preferir `ui-table-*`/`ui-form-*`. |
| feedbacks antigos `field-feedback`/`section-feedback` | remanescente planejado | migrar gradualmente para `ui-field-help` quando houver bloco de formulario. |
| estilos backend/static de tabela/formulario | `baseline_fora_do_escopo` | desacoplamento exige frente propria. |
| redesign completo | fora do G06 | nao autorizado por padroes compartilhados. |
