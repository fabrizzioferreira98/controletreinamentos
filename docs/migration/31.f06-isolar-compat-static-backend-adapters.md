# 31.F06 - Isolar compat/static/backend adapters

## 1. Bloco executado

| campo | valor |
| --- | --- |
| bloco | `F06-isolar-compat-static-backend-adapters` |
| data | 2026-04-22 |
| objetivo | centralizar e nomear acoplamentos residuais entre frontend e backend/static/SSR sem alterar comportamento funcional. |
| escopo | links backend/SSR vivos em shell/features, export/print hrefs de relatorios, redirects backend -> hash do login, assets `/static/*` usados por modulos JavaScript, checks e docs vivas. |
| fora do escopo | redesign, design system, migracao SSR, runtime/deploy, remocao de links backend, remocao de assets staticos, registry geral de endpoints API. |
| decisao | `fechado` |

## 2. Inventario antes do isolamento

Acoplamentos residuais identificados:

| grupo | ocorrencias antes | classificacao |
| --- | --- | --- |
| assets `/static/*` em JS | logo Brasil Vida no shell/login/relatorios e foto de login | `static_asset_compat` |
| assets `/static/*` em `index.html` | favicons, apple touch icon e manifest | `bootstrap_html_static_exception` |
| links backend/SSR no shell | `/painel-tv`, `/produtividade/painel-tv`, `/bases`, `/equipamentos`, `/usuarios`, `/monitoramento`, `/notificacoes-email`, `/backups`, `/auditoria` | `backend_ssr_compat` |
| links legado vivo no shell/features | `/missoes`, `/missoes/novo`, `/pernoites`, `/pernoites/novo` | `legacy_vivo_controlado` |
| link operacional/documental | `/manual/usuario.pdf` | `externo_operacional` |
| export/print de relatorios | `/treinamentos/consolidado/*`, `/produtividade/export.pdf`, `/produtividade/adicionais*`, `/produtividade/painel-tv` | `backend_ssr_compat` |
| redirects login backend -> hash | `/dashboard`, `/tripulantes`, `/treinamentos`, `/tipos-treinamento`, `/treinamentos/consolidado`, `/produtividade` | `redirect_backend_hash_compat` |
| chamadas `/api/v1/...` | consumo de API via `api()`/services/features | fora do F06; pertence ao contrato de API/service layer, nao a SSR/static. |

## 3. Mudanca aplicada

Novos adapters:

| modulo | responsabilidade |
| --- | --- |
| `frontend/src/compat/backend-links.js` | `BACKEND_LINKS`, `BACKEND_LINK_BOUNDARIES`, `FRONTEND_HASH_BY_BACKEND_PATH`, `buildBackendHref()` e `resolveLoginDestination()`. |
| `frontend/src/compat/static-assets.js` | `STATIC_ASSETS` para assets `/static/*` usados por modulos JavaScript. |

Consumidores ajustados:

| consumidor | ajuste |
| --- | --- |
| `frontend/src/shell/navigation.js` | deixou de manter paths backend hardcoded e passou a usar `BACKEND_LINKS`. |
| `frontend/src/shell/redirects.js` | virou facade fina para o adapter `compat/backend-links.js`. |
| `frontend/src/shell/login.js` | passou a usar `STATIC_ASSETS` para foto de login e logo. |
| `frontend/src/shell/render-shell.js` | passou a usar `STATIC_ASSETS` para logo e `BACKEND_LINKS` para notificacoes. |
| `frontend/src/features/dashboard/page.js` | passou a usar `BACKEND_LINKS` para missoes, pernoites, painel TV, equipamentos e tipos. |
| `frontend/src/features/relatorios/habilitacoes-page.js` | passou a usar `BACKEND_LINKS`, `buildBackendHref()` e `STATIC_ASSETS`. |
| `frontend/src/features/relatorios/produtividade-page.js` | passou a usar `BACKEND_LINKS`, `buildBackendHref()` e `STATIC_ASSETS`. |
| `frontend/src/features/relatorios/report-ui.js` | deixou de ser owner de montagem de href backend. |

O bloco nao alterou rotas SPA, loader, shell visual, SSR backend, runtime, deploy ou assets fisicos.

## 4. Prova material

Arquivos novos:

| arquivo | linhas | bytes |
| --- | ---: | ---: |
| `frontend/src/compat/backend-links.js` | `76` | `3451` |
| `frontend/src/compat/static-assets.js` | `4` | `169` |

Inspecao material apos a mudanca:

| check material | resultado |
| --- | --- |
| `/static/*` em JS fora de `compat/static-assets.js` | zero ocorrencias |
| `/static/*` remanescente em `frontend/src/index.html` | 7 ocorrencias de favicon/manifest, classificadas como excecao bootstrap HTML |
| paths backend/SSR classificados fora de `compat/backend-links.js` | zero ocorrencias relevantes; ocorrencias restantes eram `/api/v1/.../tipos`, fora do F06 |
| `shell/redirects.js` | facade para `compat/backend-links.js` |
| build temporario fora do checkout | `C:\Users\FABRIZ~1.ARA\AppData\Local\Temp\controle-treinamentos-f06-6ec1faed48db49c0b860aadc472fca1a` com `compat/backend-links.js` e `compat/static-assets.js` materializados |

## 5. Checks

Checks executados com `PYTHONDONTWRITEBYTECODE=1`:

| check | resultado |
| --- | --- |
| `tests/contract/test_frontend_compat_adapter_boundaries.py` | PASS |
| `tests/contract/test_frontend_feature_boundaries.py` | PASS |
| `tests/contract/test_frontend_page_module_boundaries.py` | PASS |
| `tests/contract/test_frontend_ux_functional_guards.py` | PASS |
| `tests/contract/test_frontend_shell_navigation_boundaries.py` | PASS |
| `tests/contract/test_frontend_shell_architecture_boundaries.py` | PASS |
| `tests/contract/test_frontend_compat_redirects.py` | PASS |
| `tests/contract/test_frontend_build_runtime_env_boundaries.py` | PASS |
| `tests/contract/test_frontend_app_architecture_boundaries.py` | PASS |
| `tests/contract/test_frontend_service_state_boundaries.py` | PASS |
| `tests/unit/test_frontend_performance_27_fix_1.py` | PASS |
| total focal ampliado | `63 passed` |
| `frontend/scripts/build_frontend.py --output-dir <temp fora do checkout>` | PASS |
| `.venv\Scripts\python.exe -B ops/scripts/repo/validate_repo_hygiene.py --root .` | `success=true`, sem violacoes |

O validator de hygiene ficou vermelho durante a rodada de teste somente por `__pycache__` gerado/local fora das excecoes. Foram removidos apenas residuos locais de bytecode fora de `.venv/` e `archive/`: `33` diretorios `__pycache__` e `0` arquivos `.pyc/.pyo` soltos. A validacao final com `python -B` ficou verde.

## 6. Criterio de fechamento

| criterio | status |
| --- | --- |
| links backend/SSR nomeados e centralizados | fechado |
| assets `/static/*` em JS centralizados | fechado |
| redirects backend -> hash movidos para adapter compat | fechado |
| export/print hrefs de relatorios passam por adapter | fechado |
| shell continua preservando classificacoes B04 | fechado |
| rotas/loaders/SSR/runtime sem alteracao | fechado |
| checks focais sem drift funcional relevante | fechado |

Decisao objetiva: `fechado`.

## 7. Divida rebaixada ou remanescente

| item | status | encaminhamento |
| --- | --- | --- |
| `frontend/src/index.html` ainda referencia `/static/*` | remanescente controlado | excecao bootstrap HTML para favicon/manifest; nao usar como fonte para novas referencias em features. |
| links backend/SSR continuam vivos | remanescente controlado | agora estao nomeados em `compat/backend-links.js`; migracao/remocao exige bloco proprio e prova objetiva. |
| rotas SSR/legacy de missoes/pernoites/produtividade/admin | remanescente fora do F06 | continuam classificadas como compat/legacy vivo, sem migracao neste bloco. |
| chamadas `/api/v1/...` em features | fora do F06 | pertencem a API client/services/domain; nao foram mascaradas nem centralizadas artificialmente. |
| `pages-training-workspace.js` | remanescente classificado desde B05 | continua nao roteado e nao foi removido por presuncao. |
| baseline `admin/routes.py:748>725` | remanescente fora do bloco | nao foi tratado nem mascarado. |
